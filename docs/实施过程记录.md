# 实施过程记录

> 用途：记录关键实施步骤、决策、风险与验证结果，便于回归与审计。

## 记录规范
- 按时间倒序或正序均可，但全文件保持一致。
- 每条记录至少包含：日期时间、目标、变更文件、验证结果、风险/后续。
- 只记录关键节点，避免流水账。

## 模板
### YYYY-MM-DD HH:mm
- 目标：
- 关键决策：
- 变更文件：
- 验证：
- 风险与后续：

---

## 2026-04-10 10:55
- 目标：补齐 Ingest 内容去重，避免重复页面与重复任务污染。
- 关键决策：
  - 以 `content_hash` 为去重主键：若已存在对应 wiki 页面且页面文件仍存在，则直接复用并返回。
  - 重复导入不再新增 tasks/wiki_pages，也不重复写 `index.md`，仅返回“复用既有页面”结果。
  - 保持兼容：若数据库有历史记录但页面文件已丢失，则回退到正常导入流程。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - Rust 去重测试已补充（db/vault），需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前去重粒度是“全文一致”；后续可扩展到“近似重复”与“标题/段落级去重”。

## 2026-04-10 10:20
- 目标：增强 Query 保存后的可追溯性，并将引用一致性纳入 lint。
- 关键决策：
  - SQLite 新增 `citations` 表，由 Query 保存流程写入引用关系（被保存页面 -> 被引用页面）。
  - Lint 增加 `BROKEN_CITATION` 与 `BROKEN_CITING_PAGE` 规则，用于发现引用断链和失效记录。
  - Lint 执行前调用 schema 校验，兼容旧库自动补齐新表结构。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `src-tauri/src/state.rs`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查不受影响：`npm --workspace web run typecheck` 通过。
  - Rust 单元测试需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前引用记录仅保存页面路径与摘录，后续可扩展 locator/quote_hash 以支持更细粒度证据追踪。

## 2026-04-09 18:05
- 目标：推进 Query 主线，支持“问答结果保存为 Wiki 页面”。
- 关键决策：
  - 后端新增 `save_query_answer` 命令，统一完成 wiki 页面写入、`index.md` 同步、`log.md` 追加、SQLite/FTS 更新。
  - 前端 Ask 面板新增“保存回答到 Wiki”按钮，复用当前 Query 结果直接保存。
  - 保持严格本地约束：保存流程仅写本地 Vault，不引入任何外部网络调用。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `src-tauri/src/db.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - WSL：`npm --workspace web run typecheck` 可通过。
  - WSL：`npm --workspace web run test` 仍受可选依赖安装差异影响（rollup linux optional package 缺失），需以 Windows 结果为准。
  - Rust 自动化测试需在 Windows PowerShell 执行 `cargo test` 复核。
- 风险与后续：
  - 当前保存策略默认按 `query-时间戳.md` 新建页面，后续可增加“覆盖保存到指定页面”的冲突合并策略。

## 2026-04-09 17:30
- 目标：收口 Query 参数持久化（TopK）并完成前后端联动。
- 关键决策：
  - 后端将 `query_top_k` 纳入 `.runtime/app-config.json`，提供 `get_query_settings/set_query_top_k` 命令。
  - 前端启动时读取 Query 设置，Ask 面板新增“保存参数”按钮，支持持久化默认 TopK。
  - 保持 `query_ask` 与 `query_ask_with_options` 兼容：显式参数优先，否则使用持久化默认值。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - WSL：`npm --workspace web run typecheck` 通过。
  - WSL：`npm --workspace web run test` 受可选依赖安装差异影响失败（`@rollup/rollup-linux-x64-gnu` 缺失），需以 Windows PowerShell 结果为准。
  - Rust 侧需在 Windows PowerShell 执行 `cargo test` 与 `cargo tauri dev --config .\tauri.conf.json` 复核。
- 风险与后续：
  - 当前工作区仍存在 Windows/WSL 混装 `node_modules` 差异；后续建议固定“仅 Windows 安装并运行 npm”的流程。

## 2026-04-08 11:10
- 目标：将 v1 技术设计与 agents 约束改为中文，并固化记录机制。
- 关键决策：保留原语义不变，仅做中文化；新增“中文注释”和“实施记录文件”硬约束。
- 变更文件：
  - `docs/v1-technical-design.md`
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：人工校对文档结构与约束项，确认与 A+C 路线一致。
- 风险与后续：后续代码实现前仍需先完成 MCP/skills 调研与安装确认。

## 2026-04-08 11:28
- 目标：完成开工前 MCP/Skills 联网调研，形成安装前清单。
- 关键决策：
  - v1 采用 MCP 最小集（Filesystem/Git/Time）优先。
  - Obsidian 第三方 MCP 暂缓，不作为 v1 依赖。
  - Skills 先使用当前已安装集合，不做额外批量安装。
- 变更文件：
  - `docs/开工前MCP与Skills调研.md`
  - `docs/实施过程记录.md`
- 验证：对官方文档/仓库进行来源核验并完成候选分级。
- 风险与后续：待用户确认“安装批次 A”后再执行实际安装与连通性验证。

## 2026-04-08 11:36
- 目标：完成 MCP 安装批次 A（filesystem/git/time）并做连通性验证。
- 关键决策：
  - `filesystem` 使用项目本地 Node 安装路径，避免每次 npx 远程拉取。
  - `git` 与 `time` 使用项目内 Python venv 安装，避免系统 Python PEP668 限制。
  - WSL 联网使用 Windows 代理地址 `172.25.64.1:7897`。
- 变更文件：
  - `docs/开工前MCP与Skills调研.md`
  - `docs/实施过程记录.md`
- 环境变更：
  - 新增目录：`/mnt/e/llm-wiki/.tools/mcp-node`
  - 新增目录：`/mnt/e/llm-wiki/.tools/mcp-venv`
  - 已写入 `~/.codex/config.toml` 的 MCP 条目：`filesystem`、`git`、`time`
- 验证：
  - `codex mcp list` 显示 3 个 server 均为 enabled。
  - `codex mcp get filesystem|git|time` 配置正确。
  - 服务进程 3 秒超时测试退出码均为 `124`（表示服务可正常常驻启动）。
- 风险与后续：
  - 官方 servers 多为 reference 实现，后续进入编码阶段仍需做最小权限与输入边界加固。

## 2026-04-08 11:58
- 目标：按 subagents 模式完成第 1 轮代码骨架（web + src-tauri）。
- 关键决策：
  - 采用并行子代理分工：`web` 与 `src-tauri` 分离写入，主控统一集成。
  - 前端先做可运行骨架与最小 Tauri 桥接，不提前接入复杂状态管理。
  - 后端先提供命令桩与模式策略枚举，后续迭代替换真实业务。
- 变更文件：
  - 新增：`package.json`、`.gitignore`、`README.md`
  - 新增：`web/*`（React + TS + Vite 骨架）
  - 新增：`src-tauri/*`（Tauri/Rust 命令骨架）
  - 新增：`docs/v1-接口契约草案.md`
  - 更新：`docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过，生成 `web/dist`。
  - `cargo --version` 与 `rustc --version` 在当前环境不可用，未完成 Rust 编译验证。
- 风险与后续：
  - 需要在具备 Rust 工具链的环境执行 `cargo check` 与 `tauri build`。
  - 命令桩尚未接入 SQLite/FTS/Vault/Provider 路由，属于下一轮工作。

## 2026-04-08 12:09
- 目标：将 subagents 并行实施模式固化为项目执行约束。
- 关键决策：
  - 在 `agents.md` 中新增固定章节，明确主控/子代理职责与并行冲突规则。
  - 约束“每轮需产出验证与未完成项，并在下一轮前经用户确认”。
- 变更文件：
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：人工校对新增条款与当前实施方式一致。
- 风险与后续：后续每轮按该规则执行并持续记录。

## 2026-04-08 12:26
- 目标：完成第 2 轮“前后端真实联动”最小闭环（模式切换 + 最近日志 + 运行态展示）。
- 关键决策：
  - 继续采用 subagents 并行：前端交互与 Rust 状态持久化分离实现，主控统一验收。
  - `set_mode` 改为“先持久化后更新内存态”，失败时保持原模式并写警告日志。
  - 浏览器预览模式保留优雅降级，避免调用 Tauri API 报错。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/models.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/types.ts`
  - `web/src/styles.css`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - 当前 WSL 环境无 Rust 工具链，未执行 `cargo check`；Windows 端可用 `cargo tauri dev` 验证联动。
- 风险与后续：
  - 配置文件当前落点依赖运行时工作目录（`.runtime/app-config.json`），下一轮可升级为固定应用数据目录。
  - 前端出现 `web/package-lock.json` 与 `web/node_modules`（子代理本地验证产生），后续可统一为根 workspace 安装策略。

## 2026-04-08 12:33
- 目标：新增“单元测试 + 用户手动验证（PowerShell）”的长期执行约束。
- 关键决策：
  - 将测试要求固化进 `agents.md` 与技术设计文档，作为后续轮次强制执行项。
  - 新增独立规范文档，统一自动化测试与手动验证输出格式。
- 变更文件：
  - `agents.md`
  - `docs/v1-technical-design.md`
  - `docs/测试与验证规范.md`
  - `docs/实施过程记录.md`
- 验证：人工校对文档条款，确认与用户新增要求一致。
- 风险与后续：从下一轮开始，对每次功能改动同时补充单元测试与 PowerShell 验证命令。

## 2026-04-08 12:40
- 目标：完成第 3 轮“Vault 初始化 + SQLite schema + Markdown 基础摄入”最小闭环。
- 关键决策：
  - 后端新增 `init_vault` 与 `ingest_markdown` 命令，先覆盖最小可用路径。
  - 前端新增“开发操作”区域，提供 Vault 初始化与示例摄入入口。
  - 继续采用 subagents 并行开发，由主控统一验收。
- 变更文件：
  - `src-tauri/Cargo.toml`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/types.ts`
  - `web/src/styles.css`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - Rust 编译与 Tauri 运行需在 Windows 环境执行用户侧 PowerShell 验证。
- 风险与后续：
  - 当前后端单元测试尚未补齐（已列为强制要求，下一步补齐）。
  - 根目录与 `web` 子目录 lockfile 并存，后续可统一依赖管理策略。

## 2026-04-08 16:30
- 目标：修正运行配置路径并补齐前后端单元测试。
- 关键决策：
  - 将 `app-config.json` 固定到项目根 `.runtime/app-config.json`，避免 `src-tauri/.runtime` 路径歧义。
  - 将 Vault 生成文件中的固定标签改为英文，降低 PowerShell 默认编码查看乱码概率。
  - 前端引入最小 Vitest 测试框架，后端补充 Rust 单元测试。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/app-formatters.ts`
  - `web/src/app-utils.test.ts`
  - `web/package.json`
  - `web/package-lock.json`
  - `docs/实施过程记录.md`
- 验证：
  - `cd web && npm run test` 通过（5/5）。
  - `cd web && npm run typecheck` 通过。
  - `cd web && npm run build` 通过。
  - Rust 测试需在 Windows 侧执行 `cargo test` 复核。
- 风险与后续：
  - 仍需用户在 Windows 侧完成 Rust 测试与一次手动功能回归，作为本轮最终验收。

## 2026-04-08 16:45
- 目标：完成第 4 轮真实 lint 规则与前端 lint 面板。
- 关键决策：
  - 后端 lint 从示例逻辑升级为真实扫描规则（wiki/index/db/tasks）。
  - 前端新增 lint 面板与 `run_lint` 触发按钮，并保持浏览器预览降级。
  - 按新增规范补齐前端自动化测试；Rust 测试在 Windows 侧执行。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/lint-utils.ts`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `cd web && npm run test` 通过（8/8）。
  - `cd web && npm run typecheck` 通过。
  - `cd web && npm run build` 通过。
  - Rust 测试需在 Windows 侧执行 `cargo test --manifest-path src-tauri/Cargo.toml`。
- 风险与后续：
  - lint 规则当前聚焦结构一致性，后续可补内容级规则（例如重复摘要、坏链接分级修复建议）。

## 2026-04-08 18:08
- 目标：将“开发操作”输入框默认值升级为项目绝对路径，减少每次手动改路径。
- 关键决策：
  - 后端新增 `get_default_paths` 命令统一返回默认路径（`<项目根>/vault`、`<项目根>/README.md`）。
  - 前端首屏加载时并行获取总览与默认路径，在 Tauri 环境下自动填充输入框。
  - 保留浏览器预览模式降级行为，不依赖后端命令。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境无 `cargo`，未执行 Rust 侧 `cargo test` 与 `cargo fmt`，需在 Windows PowerShell 侧复核。
- 风险与后续：
  - 如用户希望“记住上次手改路径”，可在下一轮增加前端本地缓存或后端配置持久化策略。

## 2026-04-08 18:40
- 目标：完成 Query v1 最小闭环（本地规则检索 + Ask 面板联调）。
- 关键决策：
  - Query v1 不接云模型，先用本地规则检索（关键词命中计分）返回证据片段。
  - 后端返回 `query_ask` 结构化结果，前端展示答案、引用、命中页数量与检查时间。
  - 保持浏览器预览降级：非 Tauri 环境不调用后端命令并给出中文提示。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 目前 Query 仍是规则检索，后续可接 SQLite FTS5 与引用置信度评分。

## 2026-04-09 16:08
- 目标：推进 Query 到 FTS5 索引优先检索（保留文件扫描降级）。
- 关键决策：
  - `fts_pages` 作为增强索引能力，初始化失败不阻断主流程。
  - ingest 成功后尝试写入 FTS 索引，失败仅记录任务事件并允许后续降级。
  - `query_ask` 优先走 FTS 候选页，再复用现有规则评分；FTS 异常时自动回退文件扫描并写日志。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `src-tauri/src/state.rs`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 与运行验证需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 目前 FTS 查询词仍基于轻量 token，后续可增加中文分词策略与权重调优。

## 2026-04-09 16:20
- 目标：推进 Query 中文检索命中率与结果质量（中文分词 + 排序权重 + 去重）。
- 关键决策：
  - 分词改为中英混合：英文 token 保留；连续中文片段生成原片段与 2-gram token。
  - 排序分数改为“正文命中 + 标题命中加权 + 短语命中加权”。
  - FTS 与 scan 两条路径统一去重逻辑，保证结果页面唯一。
  - `PendingTaskRecord` 的 `kind/raw_path` 纳入 lint 信息，清理 dead_code warning 且不改变 lint 语义。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 中文 token 目前是轻量规则，后续可按需要接入更强中文分词词典与停用词策略。

## 2026-04-09 16:36
- 目标：推进 Query 参数化 v1（TopK 可配置 + 停用词过滤）。
- 关键决策：
  - 后端新增 `query_ask_with_options(question, options)`，保留 `query_ask` 向后兼容。
  - `top_k` 在后端统一归一化为 `1..=8`，默认值为 `3`，避免前端传参越界导致行为不稳定。
  - 分词阶段增加中英停用词过滤，降低中文问句与英文虚词对相关度的噪声影响。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 目前停用词表是最小集合，后续可改为可配置词表并引入领域词典。
