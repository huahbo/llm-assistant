# 实施过程记录

> 用途：记录关键实施步骤、决策、风险与验证结果，便于回归与审计。

## 记录规范
- 按时间倒序或正序均可，但全文件保持一致。
- 每条记录至少包含：日期时间、目标、变更文件、验证结果、风险/后续。
- 只记录关键节点，避免流水账。

## 模板
### YYYY-MM-DD HH:mm
- 目标：
- 关键决策：
- 变更文件：
- 验证：
- 风险与后续：

---

## 历史归档索引
- `docs/实施过程记录-历史归档-2026-04-17到2026-04-19.md`

---


## 2026-04-20 16:30 — OCR 错误分类修复（命令缺失 vs 语言包缺失）（Codex）

- 目标：修复“已安装 Tesseract 但 UI 仍提示未安装”的误判问题。
- 关键决策：
  - 前端收敛“未安装”判定，仅匹配命令不存在语义（`未检测到 ... 命令` / `is not recognized`）。
  - 后端新增 Tesseract 语言包缺失识别（`chi_sim` / `traineddata` / `failed loading language`），返回明确错误文案。
  - 保持现有 OCR 路径兜底逻辑不变（PATH + Windows 常见安装目录）。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
- 验证：
  - `cd web && npm run typecheck`：通过（0 errors，WSL）
  - Rust 运行验证待 Windows 复核（当前 WSL 无 cargo）
- 风险与后续：
  - 若用户安装了自定义语言包组合，仍可能出现个别语言名差异提示；后续可增加 OCR 语言选项配置化。

## 2026-04-20 16:26 — OCR 启动环境兼容修复（Tesseract PATH 未生效兜底）（Codex）

- 目标：解决“系统已安装 Tesseract，但应用仍提示未检测到 tesseract”问题。
- 关键决策：
  - 不再只依赖 PATH 的 `tesseract` 命令，新增 Windows 常见安装目录兜底候选。
  - `run_tesseract_ocr_command` 依次尝试：`tesseract`、`ProgramFiles/ProgramFiles(x86)/LOCALAPPDATA` 下 `Tesseract-OCR\\tesseract.exe`。
  - 所有候选都 `NotFound` 时，错误信息附带“已尝试命令列表”，便于用户侧快速定位环境问题。
- 变更文件：
  - `src-tauri/src/state.rs`
- 验证：
  - 本地代码审查通过；当前 WSL 无 Rust toolchain（`cargo` 不可用），待 Windows `cargo check/test` 复核。
- 风险与后续：
  - 当前兜底覆盖主流安装路径；若用户自定义非常规目录，仍建议保留 PATH 配置或后续增加设置项（自定义 OCR 可执行路径）。

## 2026-04-20 15:54 — 拖拽摄入（App 内）落地（Codex）

- 目标：实现应用窗口内文件拖拽即触发 `ingest_file`，减少手动粘贴路径与点击成本。
- 关键决策：
  - 使用 Tauri 窗口级 `onDragDropEvent` 监听（`@tauri-apps/api/window`），在 drop 时直接走现有 ingest 流程。
  - 新增 `parseDroppedIngestPaths` 纯函数：支持扩展名过滤（md/pdf/docx/pptx/txt/图片）、Windows 路径去重、返回被忽略项统计。
  - 将文件摄入主流程抽取为 `runIngestFilePaths`，复用给“手动摄入”和“拖拽摄入”，避免双份逻辑漂移。
  - 拖拽悬停时显示状态条提示（`status-bar--dragging`），释放后自动切到 Inbox 并启动摄入。
- 变更文件：
  - `web/src/App.tsx`（拖拽监听、路径解析接入、摄入流程抽取与状态提示）
  - `web/src/styles.css`（拖拽悬停状态条样式）
  - `web/src/app-utils.test.ts`（`parseDroppedIngestPaths` 单测）
- 验证：
  - `cd web && npm run typecheck`：通过（0 errors，WSL）
  - `cd web && npm run test -- src/app-utils.test.ts`：WSL 受 Rollup Linux 可选依赖缺失影响未通过启动，待 Windows 复核
- 风险与后续：
  - 当前按扩展名做前置过滤，未做文件存在性预检（依赖后端 ingest 返回）；后续可加“路径可访问性”预检查。
  - 拖拽期间若已有摄入任务运行会提示并忽略，后续可扩展为“自动加入 ingest 队列”策略。

## 2026-04-20 15:47 — 移植包 B（阶段三）异常连接降噪（置信度门槛）落地（Codex）

- 目标：降低 `surprising-link` 噪声，避免弱信号跨组边频繁误报。
- 关键决策：
  - 在 `surprising-link` 评分中加入复合置信度：`(1-Jaccard)` + 词汇距离 + 跨组边稀有度。
  - 新增阈值 `surprisingMinConfidence`（默认 0.55），作为异常连接的第二道门禁（第一道仍是 Jaccard）。
  - 阈值接入本地持久化与 UI 滑条（“异常连接置信度 ≥ x”），支持现场调噪。
  - 证据文案升级：展示命中条件与三项因子（Jaccard、词汇距离、跨组稀有度）及最终置信度。
- 变更文件：
  - `web/src/App.tsx`（置信度算法、阈值读写、UI 控件与证据）
  - `web/src/app-utils.test.ts`（置信度阈值过滤用例 + 存储裁剪用例）
- 验证：
  - `cd web && npm run typecheck`：通过（0 errors，WSL）
  - `cd web && npm run test -- src/app-utils.test.ts`：WSL 受 Rollup Linux 可选依赖缺失影响未通过启动，待 Windows 复核
- 风险与后续：
  - 当前“语义”仍为轻量词汇距离近似，未接入 embedding 语义相似度；后续可按性能预算引入后端语义打分。
  - 置信度阈值目前只在前端持久化；若需团队一致策略，可同步落库到后端 AppConfig。

## 2026-04-20 15:35 — 启动闪退修复：队列 worker 从 tokio::spawn 切换到 tauri runtime（Codex）

- 目标：修复 Windows `npm run tauri:dev` 启动后立即闪退（`there is no reactor running`）。
- 关键决策：
  - 崩溃点定位为 `src/state.rs:3758`，`setup` 阶段直接调用 `tokio::spawn`，当时不保证在 tokio reactor 上下文。
  - 将队列 worker 启动改为 `tauri::async_runtime::spawn`，使用 Tauri 托管异步运行时，避免启动阶段 panic。
  - 不改动队列状态机与消费逻辑，只修正 runtime 接入方式，降低回归面。
- 变更文件：
  - `src-tauri/src/state.rs`（`start_queue_worker` 启动方式与注释）
- 验证：
  - 代码审计：panic 栈指向 `tokio::spawn` 调用点，修复后调用链不再依赖外部 reactor 上下文。
  - 当前环境（WSL）无法执行 Rust 编译/运行；需 Windows 复核启动链路。
- 风险与后续：
  - 若后续再在 `setup` 增加异步任务，需统一使用 `tauri::async_runtime::*`，避免同类问题复发。

## 2026-04-20 15:27 — 移植包 B（阶段二）洞察参数化与异常连接落地（Codex）

- 目标：在阶段一基础上补齐“异常连接洞察 + 阈值参数化 + 可解释证据”，让图谱洞察可调可诊断。
- 关键决策：
  - 新增第四类洞察 `surprising-link`：跨组边且共同邻居 Jaccard 相似度低于阈值时触发。
  - 洞察计算引入 `GraphInsightConfig`，并对关键阈值做本地持久化（localStorage）。
  - 在图谱右侧新增“洞察阈值”控制区：稀疏密度、桥接分组下限、异常连接相似度。
  - 洞察卡片新增 `evidence` 证据列表，明确命中条件与当前指标，避免“黑盒提示”。
- 变更文件：
  - `web/src/App.tsx`（`surprising-link` 洞察、阈值读写/裁剪、UI 控件、洞察证据展示）
  - `web/src/styles.css`（洞察阈值控件与证据列表样式）
  - `web/src/app-utils.test.ts`（新增/更新洞察测试与阈值存储测试）
- 验证：
  - `cd web && npm run typecheck`：通过（0 errors，WSL）
  - `cd web && npm run test -- src/app-utils.test.ts`：WSL 受 Rollup Linux 可选依赖缺失影响未通过启动，待 Windows 复核
- 风险与后续：
  - `surprising-link` 当前采用结构启发式规则（Jaccard + 跨组），后续可引入语义相似度/引用权重进一步降噪。
  - 阈值目前仅本地持久化，尚未并入后端配置；如需团队共享需落库到 AppConfig。

## 2026-04-20 15:11 — 移植包 B（阶段一）图谱洞察层落地（Codex）

- 目标：先快速对齐 `refer_project` 图谱洞察实现，再在现有图谱面板落地最小可用洞察卡片与联动能力，降低移植偏差。
- 关键决策：
  - 先读取参考实现 `graph-insights.ts / graph-relevance.ts / wiki-graph.ts`，仅移植“洞察建模 + 交互联动”核心，不引入重型后端依赖。
  - 本轮将洞察计算放在前端纯函数 `buildGraphInsights`，避免影响现有 Rust 路径；后续如需性能提升再后端化。
  - 洞察先收敛为 3 类：`isolated-node`、`sparse-group`、`bridge-node`，并绑定图谱定位动作（筛选分组/选中节点/邻居模式）。
  - 顺手修复历史测试类型问题：`IngestQueueItem` 测试对象补类型标注，消除 TS 推断误报。
- 变更文件：
  - `web/src/App.tsx`（新增 `buildGraphInsights`、洞察类型、侧栏“图谱洞察”模块与点击联动）
  - `web/src/styles.css`（新增洞察卡片样式）
  - `web/src/app-utils.test.ts`（新增图谱洞察测试 + 队列类型标注修复）
- 验证：
  - `cd web && npm run typecheck`：通过（0 errors，WSL）
  - `cd web && npm run test -- --run web/src/app-utils.test.ts`：受 WSL Rollup 可选依赖缺失影响未通过启动（`@rollup/rollup-linux-x64-gnu` 缺失），待 Windows 复核
- 风险与后续：
  - 洞察阈值（如稀疏密度 `0.15`）目前为经验值，后续可加设置项或按图规模自适应。
  - 建议下一轮补“异常连接（surprising links）”与洞察来源解释字段，增强可解释性与可调参能力。

## 2026-04-20 14:45 — P23 移植包 A 立项：持久化 ingest 队列（Claude Code 开工）

- 目标：实现持久化 ingest 队列，支持重启恢复、失败重试、任务取消，不破坏现有 ingest 路径。
- 关键决策：
  - 新增 `ingest_queue_items` 表，状态机：`queued → running → done/failed/cancelled`。
  - Worker 在应用启动时自动恢复 `running` 状态任务（重置为 `queued` 后重新消费）。
  - 复用现有 `ingest_file_impl/ingest_pdf_impl/ingest_url_impl` 作为实际执行体。
  - 复用 outbox 事件流，新增 `ingest_queue_enqueued/started/done/failed/cancelled` 事件。
  - 前端新增队列面板 Tab，与现有 Inbox Tab 并列。
- 变更文件范围：见交接状态卡 R20260420-07 文件所有权。
- 变更文件：
  - `src-tauri/src/db.rs`（ingest_queue_items 表 + 5 个 CRUD 函数）
  - `src-tauri/src/models.rs`（IngestQueueStatus + IngestQueueItem）
  - `src-tauri/src/state.rs`（enqueue/list/cancel/retry + start_queue_worker）
  - `src-tauri/src/commands.rs`（4 条新 Tauri 命令）
  - `src-tauri/src/main.rs`（注册命令 + 启动 worker）
  - `web/src/types.ts`（IngestQueueStatus + IngestQueueItem）
  - `web/src/tauri-client.ts`（enqueueIngest/listIngestQueue/cancelIngestItem/retryIngestItem）
  - `web/src/App.tsx`（队列 Tab + QueuePanel 组件 + 入队按钮）
  - `web/src/app-utils.test.ts`（+4 前端测试）
- 验证：
  - `cargo test`: **116 passed, 0 failed**（+3 新 Rust 测试）
  - `npm run test -- --run`: **142 passed, 0 failed**（+4 新前端测试）
  - `npm run typecheck`: 0 errors
- 风险与后续：
  - Worker 目前串行消费，高并发场景够用；后续可扩展为并发 worker。
  - 下一优先：移植包 B 图谱洞察层，或脏树收口（清理 11 个 dead_code warning）。

## 2026-04-20 11:43 — Lint 收口：MISSING_INDEX_ENTRY 清零 + 删除链路自动维护 index

- 目标：
  - 直接收口截图中的 19 条 `MISSING_INDEX_ENTRY`，并修复后续持续累积的问题根因。
- 关键决策：
  - 后端新增 `prune_missing_index_links`：统一清理 `index.md` 中指向不存在 wiki 页面的链接行。
  - 在 `delete_wiki_page_impl` 和 `purge_orphaned_wiki_pages` 中自动触发该清理，避免“删文件/清 DB 后 index 残留”。
  - 补充 2 条纯函数测试，覆盖“仅移除失效链接，不误删有效/外链”与“文件级清理计数”。
  - 对当前 `vault/index.md` 立即执行一次数据收口，回填现存 `vault/wiki/*.md` 的标准链接，确保可交接状态。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `vault/index.md`
  - `docs/实施过程记录.md`
  - `docs/交接状态卡.md`
  - `agents.md`
- 验证：
  - 差集校验：`index_links=6`、`existing_wiki=6`、`missing_links=0`（收口完成）。
  - `cd web && npm run typecheck`：通过（WSL）。
  - Rust `cargo check/test`：当前 WSL 无 `cargo`，待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test prune_missing_index_links_from_content_removes_only_missing_targets -- --nocapture`
  - `cargo test prune_missing_index_links_updates_file_and_returns_removed_count -- --nocapture`
  - `cargo test`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 在 UI 里执行 Lint，确认 `MISSING_INDEX_ENTRY` 不再批量出现。
- 风险与后续：
  - 本轮对 `vault/index.md` 做了直接数据修复，历史摘要块内容未重建，仅保证链接一致性。
  - 下一位 Agent（Claude）优先做 Windows 全链路复核，再继续下个功能迭代。

## 2026-04-20 13:20 — P9-D PDF OCR 自动回退（解析失败自动转图 OCR）

- 目标：
  - 当 PDF 解析器不兼容或无可提取文本时，自动执行“PDF 页转图 + OCR”回退，减少手动转换。
- 关键决策：
  - 严格按 §14 并行：后端子代理仅改 `src-tauri/src/state.rs`；前端子代理仅改 `web/src/App.tsx` 与 `web/src/app-utils.test.ts`；主控负责合并验收与文档同步。
  - 后端在 `ingest_pdf_impl` 中新增 OCR 回退分支：命中“解析器不兼容/扫描件/无文本”后，调用 `pdftoppm` 转 PNG，再复用现有 `extract_text_from_image_with_fallback`（tesseract/paddle 双路回退）。
  - OCR 首选 provider 读取 `default_ocr_provider`（未配置回退 `tesseract`）；回退成功写出 `ingest_pdf_ocr_fallback` 事件并在返回文案追加“已自动 OCR 回退”。
  - 前端 `formatPdfIngestErrorMessage` 新增 `pdftoppm/Poppler` 缺失映射，并修正分支优先级：若消息包含“已自动 OCR 回退”，使用“提示”前缀而非“失败”前缀。
  - 按用户指令，本轮不做打包与完整测试链，执行可用的最小验证。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
  - `docs/交接状态卡.md`
  - `agents.md`
- 验证：
  - `cd web && npm run typecheck`：通过（WSL）。
  - Rust `cargo check/test`：当前 WSL 仍为 `cargo: command not found`，待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test should_fallback_to_pdf_ocr_matches_supported_error_patterns -- --nocapture`
  - `cargo test build_pdf_ocr_fallback_failure_message_contains_install_hints -- --nocapture`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- src/app-utils.test.ts --run`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 手动验证点：
    - 在设置好 `tesseract` 或 `paddleocr` 的机器上导入“解析器不兼容 PDF”，应自动继续摄入并显示“已自动 OCR 回退”。
    - 卸载或临时移除 `pdftoppm` 后再次导入，应看到 Poppler 安装指引。
- 风险与后续：
  - `pdftoppm` 依赖 Poppler 外部安装，Windows 安装路径差异可能导致找不到命令，需要文档化安装指引。
  - 下一轮建议先做 Windows 侧 Rust/前端全链路复核，再进入打包或移植包 A（持久化 ingest 队列）。

## 2026-04-20 12:30 — P21-E 图谱聚合交互深化（展开成员页 + 一键切回明细）

- 目标：
  - 完成聚合节点交互闭环：点击聚合节点后可直接展开成员页，并支持一键切回明细模式。
- 关键决策：
  - 新增聚合节点选择态 `graphSelectedAggregateId`，与普通节点选择态解耦。
  - 右侧节点详情面板新增“聚合节点详情”分支：展示成员数、成员页面列表、`展开查看成员页` 和 `切回明细模式` 按钮。
  - `展开查看成员页` 行为定义：自动关闭聚合模式、切到目标分组筛选、定位首个成员节点。
  - 搜索结果列表增加聚合节点激活态高亮，避免仅普通节点可见反馈。
  - 按用户要求，本轮不执行打包与完整测试，仅执行 `typecheck`。
- 变更文件：
  - `web/src/App.tsx`
- 验证：
  - `cd web && npm run typecheck`：通过（WSL）。
- 风险与后续：
  - 仍需在 Windows 侧补 `npm run test -- --run` 与 `npm run build` 复核图谱交互无回归。

## 2026-04-20 12:05 — Ask 检索调试区增强（全局开关默认关 + 复制 JSON）

- 目标：
  - 实现用户选定的两项体验增强：`1) 复制 JSON`、`2) 全局开关（默认关闭）`。
- 关键决策：
  - 新增 `ASK_SEARCH_DEBUG_VISIBLE_STORAGE_KEY`，默认值为 `false`，并持久化到 `localStorage`。
  - Ask 高级设置区新增复选开关“显示检索调试区”，作为全局显示门控。
  - 检索调试块内新增“复制 JSON”按钮，优先走 `navigator.clipboard`，失败时降级 `execCommand("copy")`。
  - 按用户要求，本轮不执行打包与完整测试链路，仅做 `typecheck` 验证。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
- 验证：
  - `cd web && npm run typecheck`：通过（WSL）。
- 风险与后续：
  - 尚未执行 `npm run test -- --run` / `npm run build`（按当前轮次指令跳过），进入下一轮时再补。

## 2026-04-20 11:45 — 实施记录瘦身 + P20-6 检索可解释性增强（RRF 路径贡献）

- 目标：
  - 降低 `docs/实施过程记录.md` 的上下文体积，减少多 Agent 接手时的 token 消耗。
  - 落地 P20-6：将 RRF 各路贡献写入 Query 调试信息并在 Ask 面板可视化。
- 关键决策：
  - 主文件瘦身：将 2026-04-19 17:40 及更早记录迁移到独立归档文件，主文件保留“规范 + 最近关键记录 + 归档索引”。
  - 后端新增 `QuerySearchDebug/QuerySearchRouteDebug`，在 Query 返回与 outbox 事件中附带 `search_debug`。
  - RRF 调试字段覆盖：`fts/linked/popular/embedding` 各路候选数、候选示例、最终贡献路径、`rrf_k` 与融合 Top 路径。
  - 前端 Ask 面板新增“检索调试”折叠区，展示各路径贡献，避免污染主回答内容。
  - 并行规则说明：本轮未启用子代理并行写码，原因是当前会话规则限制“未显式请求不启动 subagents”，已按 §14 记录并采用最接近方式执行。
- 变更文件：
  - 文档：
    - `docs/实施过程记录.md`
    - `docs/实施过程记录-历史归档-2026-04-17到2026-04-19.md`（新增）
    - `docs/交接状态卡.md`
    - `agents.md`
  - 后端：
    - `src-tauri/src/models.rs`
    - `src-tauri/src/state.rs`
  - 前端：
    - `web/src/types.ts`
    - `web/src/App.tsx`
    - `web/src/styles.css`
- 验证：
  - `wc -l docs/实施过程记录.md docs/实施过程记录-历史归档-2026-04-17到2026-04-19.md`：主文件 256 行（由 3074 行瘦身）。
  - `cd web && npm run typecheck`：通过（WSL）。
  - `cd web && npm run test -- --run web/src/app-utils.test.ts`：失败（WSL 缺少 `@rollup/rollup-linux-x64-gnu` 可选依赖）。
  - Rust `cargo check/test`：当前 WSL 无 `cargo`，待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - Ask 手动验证点：
    - 发起一个命中多页的问题，回答消息下应出现“检索调试”折叠块。
    - 折叠块中应显示 `FTS/链接扩展/引用热度/Embedding` 的“候选/贡献”统计。
    - 展开后“贡献路径/候选示例”显示为可读路径文本。
- 风险与后续：
  - 当前 Rust 侧改动尚未在 Windows 上编译复核；未复核前不标记最终完成。
  - 后续优先进入 `P22 Windows 打包`，但建议先完成本轮 Windows 基线验证再打包。

## 2026-04-20 11:05 — 三方 Agent 通信与交接协议落地（Claude/Codex/Gemini）

- 目标：
  - 建立可执行的三方 Agent 通信与交接协议，确保不并发改代码前提下仍可高效接手。
- 关键决策：
  - 新增《多 Agent 通信与交接协议》，采用“单写者接力（Writer Baton）”机制，明确接手流程、交接包模板、冲突处理与可安全接手判定。
  - 新增 `docs/交接状态卡.md` 作为“当前轮次+接力状态+下一步”单点快照，避免在长文档中反复定位。
  - 更新 `agents.md` §16.4 与 §18.0，将协议与状态卡纳入新 Agent 启动必读和快速恢复步骤。
- 变更文件：
  - `docs/多Agent通信与交接协议.md`（新增）
  - `docs/交接状态卡.md`（新增）
  - `agents.md`（§16.4、§18.0 更新）
  - `docs/实施过程记录.md`（本条）
- 验证：
  - `rg -n "多Agent通信与交接协议|交接状态卡" agents.md docs/多Agent通信与交接协议.md docs/交接状态卡.md`：通过。
  - `sed -n '130,240p' agents.md`：确认 §16.4 已纳入 5 文件必读。
  - `sed -n '198,218p' agents.md`：确认 §18.0 快速恢复步骤已纳入协议与状态卡读取。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki`
  - `Get-Content .\docs\多Agent通信与交接协议.md`
  - `Get-Content .\docs\交接状态卡.md`
  - `Select-String -Path .\agents.md -Pattern "多Agent通信与交接协议|交接状态卡"`
- 风险与后续：
  - 本轮为流程与文档治理改动，未触及业务代码；后续每轮需严格执行状态卡更新，否则协议会退化为“有文档无执行”。

## 2026-04-20 10:30 — P19-4 内链补全体验收口（定位/竞态/插入）

- 目标：
  - 完成 `[[` 内链补全的可用性收口，重点修复下拉定位与光标插入体验。
- 关键决策：
  - 新增自动补全触发解析与插入纯函数（`resolveWikiAutocompleteMatch` / `applyWikiAutocompleteSelection`），避免直接正则替换导致的边界错位。
  - 新增光标锚点定位：基于 textarea 镜像测量 caret 坐标，将自动补全层定位到当前输入点附近，并在点击/键盘移动/滚动时同步更新。
  - 新增查询竞态保护：`requestId` 防抖，快速输入时仅采用最后一次查询结果，避免旧结果覆盖新输入。
  - 交互增强：补全支持 `Tab` 选中，空结果显示“未找到匹配页面”，`onMouseDown` 防止点击候选导致焦点提前丢失。
  - 本轮在可用环境内并行执行：代码改造与前端类型检查并行推进。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
- 验证：
  - `cd web && npm run typecheck`：通过（WSL）。
  - `cd web && npm run test -- --run`：WSL 失败（`@rollup/rollup-linux-x64-gnu` 可选依赖缺失，环境问题）。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 手动验证点：
    - 在 Wiki 编辑器输入 `[[sp`，下拉应出现在光标附近而非固定角落。
    - 按 `ArrowUp/Down` + `Enter/Tab` 能插入 `[[path]]`，光标落在闭合 `]]` 后。
    - 快速连续输入（如 `[[s`→`[[sp`→`[[spi`）时，结果应跟随最后输入，不出现旧结果回跳。
    - 编辑器滚动后自动补全位置随光标同步，不漂移。
- 风险与后续：
  - 当前未做“列表自动滚动到选中项”；若候选很多且键盘连续移动，建议下一轮补充。

## 2026-04-19 20:10 — BugFix：PDF 兼容性增强（二级解析引擎 + 原始流兜底）

- 目标：
  - 解决“阅读器可打开但应用解析失败”的结构兼容问题，避免要求用户手工另存为。
- 关键决策：
  - 在 `lopdf` 解析链路之外，新增独立解析引擎 `pdf-extract` 作为二级兜底（内存字节直接提取）。
  - 保留并强化原始流兜底：扫描 `stream/endstream`，尝试 `FlateDecode` 解压后按 PDF 操作符提取文本。
  - 提取顺序调整为：`lopdf(结构化)` → `pdf-extract(独立解析)` → `raw stream(低层兜底)`，最大化兼容率。
  - 本轮按用户要求完成“网络调研后选型并集成”，后续高兼容上限方案为 `pdfium-render`（需内置/打包 pdfium 二进制）。
- 变更文件：
  - `src-tauri/Cargo.toml`（新增 `flate2`、`pdf-extract`）
  - `src-tauri/src/state.rs`（解析链路重构与兜底实现）
- 验证：
  - 当前 WSL 会话无法执行 `cargo check/test`（`cargo: command not found`），待 Windows 复核。
  - 前端无需改动，现有 `npm run typecheck` 通过。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 复测文件：`E:\llm-wiki\vault\raw\s00586-025-08897-8.pdf`
- 风险与后续：
  - 若极少数 PDF 仍失败，下一步落地 P9-D：`pdfium-render` 或 MuPDF 路线，做“页渲染→OCR 自动回退”无感处理。

## 2026-04-19 19:40 — BugFix：有效 PDF 被误判“无效文件”兼容修复

- 目标：
  - 修复“PDF 阅读器可打开，但应用报 `文件内容可能不是有效 PDF`”的问题。
  - 在通用文件摄入入口对 PDF 错误给出准确可执行提示。
- 关键决策：
  - 后端新增 `load_pdf_document_with_fallback`：标准加载失败后，按 `%PDF-` 头定位与 `%%EOF` 尾截断进行多级重试（内存直读、头修复、尾修复、头尾联合修复）。
  - 不再吞掉底层错误并统一报“无效 PDF”，改为“解析器暂不兼容该文件结构”，并给出“另存为后重试 / OCR”引导。
  - 前端 `formatPdfIngestErrorMessage` 新增“解析器兼容性”映射，避免误导为“文件类型错误”。
  - 通用文件摄入（`ingest_file`）catch 分支对单个 PDF 失败走统一 PDF 友好映射。
  - 本轮采用并行模式执行调试与校验（并行探测 PDF 文件结构 + 并行前端验证命令）。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
- 验证：
  - `cd web && npm run typecheck`：通过（WSL）。
  - `cd web && npm run test -- --run web/src/app-utils.test.ts`：WSL 失败（`@rollup/rollup-linux-x64-gnu` 可选依赖缺失，环境问题）。
  - Rust `cargo check/test`：本会话无法执行（WSL 无 cargo），待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run tauri:dev`
  - 用同一文件 `E:\llm-wiki\vault\raw\s00586-025-08897-8.pdf` 复测“文件摄入”。
- 风险与后续：
  - 若目标 PDF 仍失败，说明该文件超出 `lopdf` 可恢复范围；下一步建议补“PDF 页渲染→OCR 自动回退”链路（P9-D 候选）。

## 2026-04-19 19:05 — BugFix：PDF/URL 失败后 ingesting 卡住 + 图片型 PDF 提示增强

- 目标：
  - 修复“文件摄入后顶部长期显示正在处理”的卡住问题。
  - 对图片型 PDF（扫描件）给出明确处理引导。
- 关键决策：
  - 前端 Outbox 轮询新增 `ingest_failed` 结束态处理：收到失败事件后立即清除 ingesting。
  - 后端在 ingest 关键失败路径补发 `ingest_failed` 事件（`ingest_markdown`、`ingest_pdf_impl`、`ingest_url_impl`）。
  - `ingest_pdf_impl` 在“扫描件/无可用文本”错误时附加引导：先转图片再走“文件摄入 + OCR（tesseract/paddle）”。
  - 前端 ingest 操作 `finally` 兜底 `setIngesting(false)`，避免事件延迟导致的长时间悬挂。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
- 验证：
  - `cd web && npm run typecheck`：通过。
  - `cd web && npm run test -- --run`：WSL 环境因 Rollup 可选依赖缺失失败（与 Windows 已知问题一致）。
  - `cd web && npm run build`：同上失败（环境依赖问题，非业务编译错误）。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki`
  - `npm install --no-save @rollup/rollup-win32-x64-msvc`
  - `Set-Location .\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
- 风险与后续：
  - 当前 PDF 仍是“文本提取优先”，未实现原生 PDF 页图像 OCR；扫描件建议走图片+OCR 路径。
  - 后续可评估增加“PDF 转图片后自动 OCR”链路，减少手动转换步骤。

## 2026-04-19 18:35 — P21-D 图谱收口 + P20-5 Embedding 检索接入

- 目标：
  - 完成图谱模块收口（自动刷新、可见范围搜索、稳定渲染、Ctrl+F、导出 JSON、>200 节点聚合模式）。
  - 接入 Embedding 检索到 Query 的 RRF 融合路径。
- 关键决策：
  - 对齐 `external/article.txt` 与 `external/llm-wiki-main` 的“多路融合”方向：保留现有 FTS/链接/热度路径，新增 embedding 作为扩展召回路由并并入同一 RRF。
  - 图谱搜索改为仅对“当前可见图数据”检索；移除 `GraphErrorBoundary key=节点数-边数`，避免筛选变化触发重挂载抖动。
  - 图谱自动刷新通过 Outbox `ingest_completed` 触发 `refreshAppData(includeGraph=true)`，并在 graph 模块激活时同步刷新图数据。
  - 大图聚合模式仅在节点数超过 200 时可启用，聚合节点只用于可视探索，不直接跳转页面。
  - 并行执行说明：本轮在当前会话未启用独立子代理，采用“前端图谱修复”和“后端检索增强”双轨交替推进 + 命令并行校验；原因是当前轮次需持续在同文件集快速联调。
- 变更文件：
  - 前端：
    - `web/src/App.tsx`
    - `web/src/app-utils.test.ts`
  - 后端：
    - `src-tauri/src/db.rs`
    - `src-tauri/src/search.rs`
    - `src-tauri/src/state.rs`
  - 文档：
    - `docs/实施过程记录.md`（本条）
    - `agents.md`（§18 状态同步）
- 验证：
  - `cd web && npm run typecheck`：通过。
  - `cd web && npm run test -- --run`：132/132 通过。
  - `cd web && npm run build`：通过。
  - WSL 环境 `cargo` 不可用（`cargo: command not found`），Rust 编译/测试待 Windows 侧复核。
- 用户侧 PowerShell 复核命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
- 风险与后续：
  - 本轮已接入 embedding 路由，但未引入 typed edge / retention_strength（仍在后续参考项）。
  - 图谱聚合节点当前不支持“展开到具体节点”交互，建议作为下一轮 UX 迭代项。
