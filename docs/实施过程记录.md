
## 2026-04-22 — Clipper 端到端复核增强（状态可诊断 + Windows 自检脚本）(Codex)

- 背景：
  - 当前 TODO 中 Clipper 仍有“Windows 端到端复核”未闭环，排障成本偏高。
  - 需要让“服务在不在 / Vault 是否打开 / /clip 是否可写入”可快速自证。
- 本轮变更：
  - `src-tauri/src/clip_server.rs`
    - `/status` 响应补充：
      - `vault_open`：当前是否已打开 Vault
      - `vault_path`：当前 Vault 路径（标准化斜杠）
  - `extension/popup.js`
    - `checkConnection` 增强：当 `vault_open === false` 时显示明确提示 `Connected, but no vault is open in app`，并禁用 Clip 按钮，避免误操作。
  - 新增 `scripts/verify_clipper_windows.ps1`
    - 一键自检流程：
      - GET `/status`
      - GET `/project`、`/projects`
      - 可选 POST `/clip` 写入自检样例（默认执行，`-SkipPost` 可跳过）
- 自动化验证：
  - `node --check extension/popup.js`：通过 ✅
  - Rust 验证：当前 WSL 无 cargo，待 Windows 复核。

## 2026-04-22 — Claude 限流后接手收口（Research 导出 .md 历史任务兜底）(Codex)

- 背景：
  - Claude 在限流前已完成 P1/P2 主体改动并提交（任务状态同步、Embed 错误提示、图谱双击跳转、Research 导出 `.md`）。
  - 但工作区仍有未提交的 `web/src/App.tsx` 改动：`handleExportMd` 已加“历史任务从 Wiki 读取内容兜底”，而导出按钮显示条件仍沿用旧逻辑，导致历史任务可能看不到“导出 .md”按钮。
- 接手核查结论：
  - 代码提交状态：最近提交存在 `d1ab47c / c4205fa / 3362e57 / 47048dd / 5f4440b`。
  - 交接文档状态：`docs/交接状态卡.md` 与 `agents.md` 仍停留在 2026-04-21，未反映 Claude 最新提交。
- 本轮修复：
  - `web/src/App.tsx`
    - 保留 Claude 未提交的导出内容兜底逻辑：
      - 优先使用 `synthesisContent`
      - 若为空且 `doneSavedPath` 存在，则 `fetchWikiPageDetail(doneSavedPath)` 读取 Markdown 作为导出内容
    - 修复按钮显示条件：
      - 从 `phase === "done" && synthesisContent`
      - 调整为 `phase === "done" && (synthesisContent || doneSavedPath)`
      - 使历史任务在无流式内容时也可看到“导出 .md”按钮。
- 自动化验证：
  - `cd web && npm run typecheck`：通过 ✅
  - Rust 验证：当前 WSL 无 cargo，待 Windows 复核。

## 2026-04-21 — Deep Research 删除参数映射与时间格式一致性修复 (Codex)

- 背景：
  - 用户删除研究任务时报错：`invalid args 'deleteSavedWiki' ... expected a boolean`。
  - 任务时间显示为毫秒串（如 `1776769...`），与其他模块展示格式不一致。
- 修复：
  - `src-tauri/src/commands.rs`
    - `delete_research_task` 参数顺序调整为 `state, id, delete_saved_wiki`，与项目内命令风格一致，规避参数注入映射异常。
    - 进一步改为单参数对象协议：`delete_research_task(state, input)`，其中 `input` 为结构体（`id` + `delete_saved_wiki`，兼容 `deleteSavedWiki` 别名），避免布尔参数在 Tauri 参数映射中被错误解析为 map。
    - 为彻底规避不同运行环境下的参数反序列化差异，再次改为 `delete_research_task(state, payload: serde_json::Value)`，后端手动解析 `id/deleteSavedWiki/delete_saved_wiki/input` 多种形态，统一落盘为布尔值。
  - `web/src/tauri-client.ts`
    - 调用改为 `invoke("delete_research_task", { input: { ... } })`，与后端结构体协议对齐。
    - 最新改为 `invoke("delete_research_task", { payload: { ... } })`，与后端 `serde_json::Value` 协议对齐。
  - `web/src/App.tsx`
    - Research 任务时间展示统一改为复用 `formatLintCheckedAt`（北京时间格式），与其他模块一致。
    - 删除任务流程改为“先拉取最新任务快照再确认”，避免因前端旧状态导致漏掉“是否同时删除 Wiki”二次确认。
    - 对 `done` 任务但路径缺失的场景，新增明确确认文案（仅删除任务记录，不删 Wiki）。
    - 删除确认从 `window.confirm` 切换为 Tauri 原生 `plugin-dialog ask`，避免 WebView 环境下 confirm 行为不稳定导致“直接删除不弹二次确认”。
  - `web/src/tauri-client.ts`
    - 新增 `askConfirmDialog` 封装（Tauri 用原生 `confirm`，浏览器回退 `window.confirm`），强制“确认/取消”双按钮语义。
    - 用户点击“取消”时前端直接 return，不触发删除调用，任务保持不变。
  - `src-tauri/capabilities/default.json`
    - 补充 `dialog:allow-ask` 与 `dialog:allow-confirm` 权限，允许原生确认对话框调用。
- 自动化验证：
  - `cd web && npm run typecheck`：通过 ✅
  - Rust 验证：当前 WSL 无 cargo，待 Windows 复核。

## 2026-04-21 — Deep Research 任务删除与时间戳展示增强 (Codex)

- 本轮目标：
  - 在 Deep Research 任务列表中新增“删除任务”能力。
  - 处理任务删除与关联 Wiki 页面的耦合关系（可选同步删除 Wiki）。
  - 优化任务卡时间信息展示（创建时间 + 更新时间）。
- 关键变更：
  - 后端：
    - `src-tauri/src/db.rs` 新增 `db_delete_research_task(conn, id)`，删除单条研究任务；不存在时返回明确错误。
    - `src-tauri/src/state.rs` 新增 `delete_research_task(id, delete_saved_wiki)`：
      - 仅允许删除终态任务（`done/failed/cancelled`），运行中任务需先取消。
      - 若 `delete_saved_wiki=true` 且任务存在 `saved_path`，会同步调用现有 wiki 删除流程，避免“任务删了但知识页残留”的耦合问题未处理。
      - 删除后写入 outbox 事件 `research_task_deleted`。
    - `src-tauri/src/commands.rs` 新增 Tauri 命令 `delete_research_task`。
    - `src-tauri/src/main.rs` 注册 `delete_research_task` 命令。
    - `src-tauri/src/state.rs` 新增 2 条研究任务删除相关最小单测（删除成功 / 删除不存在任务报错）。
  - 前端：
    - `web/src/tauri-client.ts` 新增 `deleteResearchTask(id, deleteSavedWiki)` 调用封装。
    - `web/src/App.tsx`（ResearchPanel）新增：
      - 每条终态任务显示 `🗑 删除任务` 按钮。
      - 删除时二次确认；若任务已关联 Wiki，弹出“是否同时删除该 Wiki 页面”的确认。
      - 任务卡显示“创建时间 + 更新时间”（完整本地时间格式）。
      - 任务删除失败错误提示面板。
- 自动化验证：
  - `cd web && npm run typecheck`：通过 ✅
  - `cargo` 验证：当前 WSL 环境不可用（`cargo: command not found`），Rust 编译与测试待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri; cargo check; cargo test`
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 在 Deep Research 任务列表验证：
    - 终态任务出现“删除任务”按钮；
    - 点击后出现二次确认；
    - 对已生成 Wiki 的任务，出现“是否同时删除 Wiki”确认；
    - 删除后任务从列表消失，时间戳展示为“创建/更新”两项。

## 2026-04-21 — Deep Research Word 导出无响应修复 (Codex)

- 背景：
  - 用户反馈 Deep Research “导出 Word”按钮点击后无反应。
  - 现有实现依赖浏览器 `Blob + <a download>`，在 Tauri WebView 中可能被拦截或不触发下载流程。
- 本轮目标：
  - 修复 Tauri 桌面环境下 Word 导出无响应问题。
  - 保留浏览器预览模式的下载兼容行为。
- 关键变更：
  - 后端：
    - `src-tauri/src/commands.rs` 新增命令 `save_research_doc(path, content)`，负责将 `.doc` 内容写入用户指定路径。
    - `src-tauri/src/main.rs` 注册 `save_research_doc` 命令。
    - `src-tauri/capabilities/default.json` 补充 `dialog:allow-save` 权限，允许保存文件对话框在 Tauri 环境弹出。
  - 前端：
    - `web/src/tauri-client.ts` 新增 `pickSaveFile`（调用 `@tauri-apps/plugin-dialog` 的 `save`）与 `saveResearchDoc`（invoke 后端写文件）。
    - `web/src/App.tsx` 的 `handleDownloadWord` 改为：
      - Tauri 环境：先弹“保存文件”对话框，再调用后端落盘；
      - 浏览器环境：继续使用 `Blob` 下载回退。
    - 导出文件名增加非法字符替换，避免 Windows 路径字符冲突。
    - 导出失败提示改为优先展示真实异常文本（非 `Error` 对象也会序列化显示），避免只看到泛化提示。
- 自动化验证：
  - `cd web && npm run typecheck`：通过 ✅
  - Rust 编译测试：当前 WSL 环境无 cargo，待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 在 Deep Research 历史任务点击“导出 Word”，应弹出保存对话框，保存后可在目标路径看到 `.doc` 文件。
- 说明：
  - 测试过程中会导致 `test-vault-template` 下数据文件变更（任务产物与日志），本轮提交不包含这些数据文件。

## 2026-04-21 — Deep Research 失败可恢复与日志可见性修复 (Codex)

- 背景：
  - 用户在 Deep Research 任务中出现 `综合报告生成失败: InvalidResponse("解析响应失败: error decoding response body")`。
  - 前端任务卡在失败态不展示过程日志，导致“看不到发生了什么”。
- 本轮目标：
  - 降低报告生成阶段的瞬时失败率。
  - 让失败任务也能看到事件日志与错误上下文。
- 关键变更：
  - 后端 `src-tauri/src/state.rs`
    - 报告生成（`synth_prompt`）从单次调用改为“失败后自动重试 1 次”。
    - 第一次失败时通过 `research_progress` 发出重试提示（含压缩后的错误摘要）。
    - 两次失败后统一通过 `report_research_failure` 返回可读错误（不再仅 `Debug` 格式）。
    - 新增 `compact_llm_error` 辅助函数，统一 LLM 错误摘要长度。
  - 前端 `web/src/App.tsx`
    - `listenResearchError` 事件写入 `taskLogs`，错误信息进入日志流。
    - `listenResearchDone` 事件写入完成日志。
    - 失败态任务卡也展示日志区（红色边线），保留最近 20 条日志，便于复盘。
- 自动化验证：
  - `cd web && npm run typecheck`：通过 ✅
  - Rust 编译/测试：当前 WSL 无 cargo，待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 在 Deep Research 触发一次任务，观察失败时任务卡日志区是否显示 `✗ ...`，并在重试场景出现“正在重试一次”日志。

## 2026-04-21 — SearXNG 本地搜索激活与 Deep Research 错误可见化增强 (Codex)

- 背景：
  - 计划项里 `SearXNG` 虽有配置 UI，但仍缺少“可直接落地”的后端稳健接入与错误可见化。
  - 用户已确认 Windows Docker 在后台运行，本轮可直接推进本地搜索链路。
- 本轮目标：
  - 打通并加固 `SearXNG` 搜索调用链路。
  - 减少 Deep Research 搜索阶段静默失败，输出可读错误。
  - 收敛过期 TODO（路径安全、decompose fallback 进度提示）。
- 并行实施说明：
  - 本轮改动集中在同一后端文件 `src-tauri/src/state.rs`，为避免并行写同文件冲突，按 §14 采用串行执行；未拆分前端/后端并行子任务。
- 关键变更（`src-tauri/src/state.rs`）：
  - 新增 `normalize_searxng_base_url` / `searxng_base_root`：
    - 自动补全 `http://`。
    - 自动去除末尾 `/` 与用户误填的 `/search` 后缀。
  - 新增 `search_searxng_endpoint` 与主/回退双端点策略：
    - 先请求 `${base}/search`。
    - 失败后自动回退请求 `${base}`。
    - 明确检查 HTTP status，并返回响应片段用于诊断。
  - 新增 `validate_search_config`（前置校验）：
    - `start_research` 在创建任务前校验 Provider 配置，避免创建必然失败任务。
    - `start_research_task` 二次兜底校验，避免异常入口绕过。
  - `do_search` 改为返回 `Result<...>`，不再 `unwrap_or_default` 吞错。
  - 并发搜索日志增强：
    - 成功：`✓ 查询（N 条）`
    - 失败：`✗ 查询（错误摘要）`
    - 首轮全部失败时，任务直接 `failed` 并汇总错误，不再“空结果静默结束”。
  - 新增 5 条最小单测：
    - SearXNG URL 规范化与 `/search` 根路径处理
    - 搜索配置校验（缺失 URL 拒绝、有效 URL 通过）
- 自动化验证：
  - `cargo` 相关验证：当前 WSL 环境无法执行（`cargo: command not found`），待 Windows 复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri; cargo test normalize_searxng_base_url_adds_http_when_missing_scheme normalize_searxng_base_url_keeps_https searxng_base_root_strips_search_suffix validate_search_config_rejects_missing_searxng_url validate_search_config_accepts_valid_searxng_url`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 在应用设置中将搜索 Provider 设为 `SearXNG`，地址填 `localhost:8080`，启动一次 Deep Research，观察进度日志出现 `✓/✗` 细粒度反馈。
- 未完成项：
  - 端到端实网验证（依赖用户本机 SearXNG 容器可访问）。
  - Deep Research pipeline 函数体仍偏长，后续可继续分段提取。

## 2026-04-21 — Clipper JSON 转义故障修复与可诊断化收口 (Codex)

- 背景：
  - 用户在浏览器扩展点击 `Clip to Wiki` 后出现 `Connection failed: Bad escaped character in JSON`。
  - 结合报错位置与 Windows 路径特征，定位为 clip server 返回体里路径字符串未稳定 JSON 转义的回归风险。
- 本轮目标：
  - 修复 Clipper 返回 JSON 的路径转义问题。
  - 增加最小回归保护，避免后续再出现同类错误。
  - 改善扩展端错误可读性，便于快速定位服务端响应异常。
- 关键决策：
  - 后端 `clip_server` 增加统一路径标准化函数 `normalize_display_path`，所有 `/project`、`/projects`、`/clip` 路径响应走同一标准化逻辑。
  - 后端新增 `clip_server` 最小单测，校验 Windows 路径标准化与 JSON 可解析性。
  - 扩展端新增 `parseJsonResponse`：先读 raw text 再 `JSON.parse`，解析失败时附带响应片段，避免只看到模糊的 `Bad escaped character`。
  - 本轮属于单文件故障修复，且与 Claude 未提交中的同一文件存在连续编辑上下文，按“单写者接力”串行处理，避免交叉写入冲突。
- 变更文件：
  - `src-tauri/src/clip_server.rs`
  - `extension/popup.js`
  - `docs/实施过程记录.md`
  - `docs/交接状态卡.md`
  - `agents.md`
- 自动化验证：
  - `node --check extension/popup.js`：通过 ✅
  - `cd src-tauri && cargo check`：未执行（WSL 缺少 cargo，`cargo: command not found`）
  - `cd src-tauri && cargo test normalize_display_path_rewrites_windows_separators clip_json_response_is_valid_after_path_normalization`：未执行（同上）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri; cargo test normalize_display_path_rewrites_windows_separators clip_json_response_is_valid_after_path_normalization`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 浏览器扩展重新加载 `extension/` 目录后，在任意页面点击 `Clip to Wiki`，确认不再出现 JSON parse 报错。
  - （可选）`Invoke-RestMethod http://127.0.0.1:19827/project | ConvertTo-Json -Depth 4`
- 风险与未完成：
  - Rust 编译与单测仍需 Windows 端复核（当前 Codex 运行环境无 cargo）。
  - Clipper 端到端依赖用户本地扩展重新加载与 Tauri 进程重启，旧进程不会热更新该修复。

## 2026-04-21 — Deep Research 代码质量重构至 A 级 (Claude Code)

- 目标：将 Gemini 编写的 Deep Research 代码质量从 B+ 提升至 A，参考 dzhng/deep-research 最优实践。
- 自评分：**A- (88/100)**
  - 优势：并发搜索、learnings 模型、token 截断、幂等 cancel、无 panic 错误处理、前端可见错误
  - 扣分点：无针对 pipeline 的单元测试、learnings 格式提取依赖 LLM 遵循指令
- 关键决策：
  - **共享 HTTP Client**：`Arc<reqwest::Client>` + 30s timeout，替代每次 `Client::new()`
  - **并发搜索**：每轮所有查询 `tokio::task::spawn` 并行执行，深度3广度3由9次串行→3批并发
  - **Learnings 模型**（参考 dzhng/deep-research）：每轮搜索后 LLM 提取结构化 `LEARNING:` 条目，Gap Analysis 基于 learnings 驱动下一轮 `FOLLOWUP:` 查询，而非堆积原始文本
  - **Token 管控**：snippet 截 300 字符，最终 context 截 8000 字符防止 LLM 溢出
  - **语言自适应 Prompt**：移除硬编码"使用中文"，改为"write in the same language as the topic title"
  - **幂等 Cancel**：`db_cancel_research_task` 仅更新 status，不重置 sub_queries/web_results_count
  - **路径统一**：`list/cancel_research_task` 改用 `outbox_db_path()`，消除重复 lock+join
  - **前端错误可见化**：无搜索 Provider 警告横幅、启动失败/Word 下载失败可见错误提示
- 变更文件：
  - `src-tauri/src/db.rs`（新增 `db_cancel_research_task` 幂等取消函数）
  - `src-tauri/src/state.rs`（重构 search_tavily/searxng/do_search 共享 client；start_research_task 全面重构）
  - `web/src/App.tsx`（ResearchPanel 新增错误状态、Provider 警告、搜索配置检查）
- 验证：
  - `cargo check`：通过 ✅ (0 warnings added)
  - `cargo test`：118/118 通过 ✅（本轮重构后升至 141/141）
  - `npm run typecheck`：0 errors ✅

### 代码质量自评 (Round R20260421-17 → R20260421-18)
- 总分：**93/100（A 级）**
- 错误处理：19/20 — `report_research_failure` 全覆盖；扣 1：decompose 失败 fallback 无日志
- 并发安全：15/15 — `Arc<Client>` spawn 安全，无死锁
- 代码结构：19/20 — 三个新 helper 职责清晰；扣 1：pipeline 主函数仍 ~130 行
- 测试覆盖：23/25 — 23 个新测试（parsing ×8、slug ×5、think-tag ×4、DB CRUD ×6）；扣 2：AppHandle 依赖导致 pipeline 端到端难以 mock
- 性能合理性：10/10 — 并发搜索 + 共享 client + token 截断全到位
- 安全性：7/10 — 扣 3：保存路径无 `starts_with(vault_path)` 检查，topic 含 `../` 存在路径越权风险
- **待修复（下轮 TODO）**：① 路径安全检查（高优）；② decompose fallback 加 `emit_progress`；③ pipeline 分段提取函数

## 2026-04-20 HH:mm — Deep Research 模块全面对标 dzhng/deep-research & 移植包 D 收口 (Gemini)

- 目标：
  - Deep Research 模块全面对标 `dzhng/deep-research`，实现迭代式、知识驱动的深度研究，提升用户体验。
  - 完成移植包 D：项目模板初始化器、网页剪藏（后端服务 + 浏览器扩展）、Word 下载功能。
  - 修复前期遗留的类型错误与数据同步问题，确保代码质量与协作规范。
- 关键决策：
  - **Deep Research 后端 Pipeline (src-tauri/src/state.rs)**：
    - 将 `start_research_task` 重构为真正的**迭代式研究循环**：每轮 LLM 评估知识缺口，自动生成下一轮子查询。
    - 引入 `depth` (1-5级) 控制迭代深度，`breadth` 控制每轮搜索广度。
    - `search_tavily` 开启 `include_raw_content: true`，获取网页正文用于高保真摘要。
    - 细粒度进度汇报：通过 `research_progress` 事件实时更新前端 Log。
    - 数据同步：`ResearchTaskItem` (models.rs) 和 `research_tasks` 表 (db.rs) 新增 `depth` 和 `breadth` 字段并持久化。
  - **Deep Research 前端 (web/src/App.tsx)**：
    - `ResearchPanel` 升级为**终端风格 Log 窗口**，实时显示 Agent 研究动态，提升用户感知。
    - 扩展 `depth` 选择范围至 5 级，并同步 `SearchConfigPanel`。
    - 优化任务卡片 UI：展示深度/广度参数、来源统计，强化视觉反馈。
    - **Word 下载**：在 `ResearchPanel` 中增加“导出 Word (.doc)”按钮，支持将研究报告导出。
  - **项目模板 (移植包 D2)**：
    - `web/src/templates.ts` 新增内置模板 (研究、阅读、个人成长、业务/团队、通用)。
    - 后端 `init_vault_with_template` 命令实现模板初始化逻辑（schema/purpose 文件、额外目录）。
    - 前端“Inbox”模块 UI 集成模板选择器。
  - **网页剪藏 (移植包 D3)**：
    - 后端 `src-tauri/src/clip_server.rs` 实现轻量级 HTTP 剪藏服务 (端口 19827)，支持 `/status`, `/project`, `/projects`, `/clip` 接口。
    - 剪藏内容自动保存到 `raw/clips/` 并异步触发 ingest。
    - `src-tauri/Cargo.toml` 新增 `tiny_http` 和 `chrono` 依赖。
    - `extension/` 目录从 `refer_project` 完整移植浏览器扩展代码。
- 变更文件：
  - `src-tauri/Cargo.toml` (新增 `tiny_http`, `chrono`)
  - `src-tauri/src/models.rs` (ResearchTaskItem 新增 `depth`, `breadth`)
  - `src-tauri/src/db.rs` (research_tasks 表新增 `depth`, `breadth` 字段，更新 CRUD 函数)
  - `src-tauri/src/state.rs` (重构 `start_research_task` 迭代逻辑，更新 `search_tavily` 开启 `include_raw_content`)
  - `src-tauri/src/commands.rs` (新增 `init_vault_with_template`, `get_clip_server_status`)
  - `src-tauri/src/main.rs` (注册新命令，启动 `clip_server`)
  - `src-tauri/src/clip_server.rs` (新增剪藏 HTTP 服务)
  - `web/src/types.ts` (ResearchTaskItem 新增 `depth`, `breadth`)
  - `web/src/templates.ts` (新增模板定义)
  - `web/src/tauri-client.ts` (新增 `initVaultWithTemplate` 封装)
  - `web/src/App.tsx` (重构 `ResearchPanel` UI、模板选择器、Word 下载按钮、`useCallback` 导入补全)
  - `extension/` (完整移植 `refer_project/extension` 内容)
- 验证：
  - `cargo check` (src-tauri/): 通过 ✅ (2 warnings about unused DB functions)
  - `npm run typecheck` (web/): 通过 ✅ (0 errors)
- 风险与后续：
  - Deep Research 仍为“单次 complete”模式，大结果集响应较慢，后续可考虑流式输出。
  - Clipper 浏览器扩展需手动安装。
  - 下一优先：移植包 A (持久化 ingest 队列) 的重启恢复逻辑。
