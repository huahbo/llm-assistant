## 2026-05-07 — H7 Phase 2.6：AskModule 提取（Codex）

- 背景：Wiki 列表模块已外移，继续按 H7 计划提取 Ask 模块渲染边界。
- 本轮完成：
  - 新增 `web/src/modules/ask/AskModule.tsx`。
  - 将 Ask 模块 UI（会话管理、消息流、引用折叠、检索调试、高级参数、输入区）从 `web/src/App.tsx` 外移。
  - 保持会话状态、流式查询状态、保存与取消逻辑仍在 `App.tsx`，通过 props 向 `AskModule` 接入，避免一次迁移状态归属引发竞态。
  - 新增 `handleCancelQuery`，将原 JSX 内联的取消流程收敛为可复用 handler。
  - 更新 H7 计划进度：Phase 2.6 已完成。
- 验证结果：
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.7：提取 `GraphModule`。

---

## 2026-05-07 — H7 Phase 2.5：WikiModule 提取（Codex）

- 背景：Lint 模块已外移，继续按 H7 计划拆分 Wiki 模块渲染边界。
- 本轮完成：
  - 新增 `web/src/modules/wiki/WikiModule.tsx`。
  - 将 Wiki 列表模块（搜索/排序、标签筛选、文件树、卡片入口、AI 新建弹窗）从 `web/src/App.tsx` 外移。
  - 当前保留 `App.tsx` 中的 Wiki 详情预览、编辑、历史版本、frontmatter、citations 等状态与核心 handler，仅通过 props + render callback 接入 `WikiModule`，降低单步迁移风险。
  - 更新 H7 计划进度：Phase 2.5 已完成。
  - 本轮并行：主线本地实现 Wiki 拆分，同时启用子代理只读梳理 Ask 模块依赖清单，为下一步 Phase 2.6 提速。
- 验证结果：
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.6：提取 `AskModule`。

---

## 2026-05-04 — H7 Phase 2.4：LintModule 提取（Codex）

- 背景：概览首页已完成外移，继续按计划处理中风险的 lint 模块。
- 本轮完成：
  - 新增 `web/src/modules/lint/LintModule.tsx`。
  - 将 Lint 面板、最近补丁记录、补丁建议 JSX 从 `web/src/App.tsx` 外移。
  - `App.tsx` 保留 lint 报告、筛选条件、补丁预览与应用状态，仅通过 props 向 `LintModule` 提供桥接。
  - 为保持原有行为，补充 `handleCreateLintTargetPage` 与 `handleOpenLintPatchPage` 两个桥接 handler。
  - 受当前会话子代理调用策略约束，本轮继续按单前端临界步骤串行执行；未改后端，避免与其他 Agent 潜在并行面冲突。
  - 更新 H7 计划进度：Phase 2.4 已完成。
- 验证结果：
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.5：提取 `WikiModule`。

---

## 2026-05-04 — H7 Phase 2.3：InboxModule 提取（Codex）

- 背景：运行与设置模块已外移，继续按计划提取概览首页。
- 本轮完成：
  - 新增 `web/src/modules/inbox/InboxModule.tsx`。
  - 将概览首页的运行模式、Vault 操作、URL/文件摄入、网页剪藏扩展、最近日志 JSX 从 `web/src/App.tsx` 外移。
  - 当前仍由 `App.tsx` 持有摄入状态、模板初始化状态、模式切换与日志数据，通过 props 接入 `InboxModule`，避免本步同时迁移状态归属。
  - 更新 H7 计划进度：Phase 2.3 已完成。
- 验证结果：
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.4：提取 `LintModule`。

---

## 2026-05-04 — H7 Phase 2.2：OperationsModule 提取（Codex）

- 背景：Settings 模块已完成外移，继续按低风险顺序提取运行模块。
- 本轮完成：
  - 新增 `web/src/modules/operations/OperationsModule.tsx`。
  - 将“运行”模块的队列页签与统计页签 JSX 从 `web/src/App.tsx` 外移。
  - 继续复用已独立的 `QueuePanel`。
  - 队列刷新/取消/重试、统计加载和跳转 Wiki 的行为通过 props 接入，保持现有状态来源不变。
  - 更新 H7 计划进度：Phase 2.2 已完成。
- 验证结果:
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.3：提取 `InboxModule`。

---

## 2026-05-04 — H7 Phase 2.1：SettingsModule 提取（Codex）

- 背景：Phase 1 的 5 个 Context 已完成，开始进入 Phase 2 模块提取。
- 本轮完成：
  - 新增 `web/src/modules/settings/SettingsModule.tsx`。
  - 将 Settings 模块的 JSX 从 `web/src/App.tsx` 外移到独立模块。
  - Settings 内部直接使用 `ShellPolicyContext`，减少 App 对 Shell 策略表单的接线。
  - LLM Provider 表单状态与拖拽模式仍由 App 传入，避免本步同时迁移业务状态导致风险扩大。
  - `web/src/App.tsx` 改为通过 `<SettingsModule />` 渲染 Settings 分支。
  - 更新 H7 计划进度：Phase 2.1 已完成。
- 验证结果：
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.2：提取 `OperationsModule`。

---

## 2026-05-04 — H7 Phase 1.5：ToastContext 抽取（Codex）

- 背景：H7 Phase 1.4 已完成 Shell 策略 Context，继续收口最后一个全局提示状态 Context。
- 本轮完成：
  - 新增 `web/src/contexts/ToastContext.tsx`。
  - `ToastContext` 接管 `statusMessage` 与 `agentStatusMessage` 两条现有提示状态。
  - `web/src/main.tsx` 接入 `ToastProvider`。
  - `web/src/App.tsx` 改用 `useToast()`，保留现有 `setStatusMessage` / `setAgentStatusMessage` 调用形态，避免一次性改动上百处调用。
  - 更新 `docs/h7-app-tsx-refactor-plan.md` 进度勾选：Phase 1.5 已完成。
- 验证结果：
  - `cd web && npm run typecheck` ✅
- 下一步：
  - H7 Phase 2.1：提取 `SettingsModule`。

---

## 2026-05-04 — H7 Phase 1.4：ShellPolicyContext 抽取（Codex）

- 背景：Claude/Sonnet 4.6 已完成 H7 Phase 0 与 Phase 1.1~1.3 后限流；Codex 接手继续前端模块重构。
- 本轮完成：
  - 清理 `web/src/App.tsx` 行尾噪声，确认 `--ignore-space-at-eol` 下无语义 diff，再继续开发。
  - 新增 `web/src/contexts/ShellPolicyContext.tsx`：
    - 迁出 Shell 策略默认值、档位配置、选项列表。
    - 迁出策略 reload/save/applyProfile/applyAndSaveProfile/setField 逻辑。
    - 暴露 `config/saving/dirty/message`，供 Settings 与 Agent 共享同一数据源。
  - 更新 `web/src/main.tsx`，在 `ModeProvider` 内接入 `ShellPolicyProvider`。
  - 更新 `web/src/App.tsx`，删除原 `agentShellPolicy*` 本地状态与 handler，改用 `useShellPolicy()`。
  - 更新 `docs/h7-app-tsx-refactor-plan.md` 进度勾选：Phase 0 与 Phase 1.1~1.4 已完成。
- 串行说明：
  - 本轮仅改前端 Context 和 `App.tsx/main.tsx` 接线，后端无改动。
  - 该步骤依赖 Claude 已提交的 `ModeContext` 接线，且写同一 `App.tsx`，不并行拆分以避免跨代理写同一文件冲突。
- 验证结果：
  - `cd web && npm run typecheck` ✅
  - `cd web && npm run test -- --run` ⚠️ 当前 WSL 侧缺失 Rollup 可选依赖 `@rollup/rollup-linux-x64-gnu`，Vitest 启动前失败。
  - `cd src-tauri && cargo test` ⚠️ 当前 WSL 侧缺失 `pkg-config`，GTK/GLib 系统依赖探测失败，未进入业务测试。
- 下一步：
  - H7 Phase 1.5：抽取 `ToastContext`，统一 `statusMessage / agentStatusMessage`。

---

## 2026-05-04 — H7 App.tsx 拆分重构计划（Claude Opus 4.7 架构）

- 背景：H6-P1 收口后用户提出 App.tsx（13858 行 / 单组件 9500 行 / 235 useState）已成长期债，需要架构级拆分。用户选择 A 方案：先做完整架构设计文档，再交 Sonnet 4.6 增量执行。
- 本轮完成（架构设计阶段，不动代码）：
  - 新增计划文件：`docs/h7-app-tsx-refactor-plan.md`（约 540 行）
    - §0 摘要 + write_wiki/edit_wiki 自动化覆盖决策
    - §1 目标/非目标/DoD
    - §2 现状诊断（量化 + 模块边界 line 范围 + 状态分布画像）
    - §3 目标架构（目录结构 + 5 个 Context 契约 + 状态/handler 归类规则）
    - §4 阶段化执行计划（Phase 0/1/2/3，含每个 step 的源 line / 目标文件 / 状态数 / 风险）
    - §5 进度勾选表（Sonnet 4.6 执行时填写）
    - §6 风险与回滚（含紧急止损规则）
    - §7 给 Sonnet 4.6 的执行清单（启动前必读、约束、异常处理、进度记录、完成判定）
    - §8 关键代码片段模板（Module 骨架 / App.tsx 收敛后骨架 / commit message 模板）
    - §9 FAQ + §10 引用
  - 关键架构决策：
    - **不引入新依赖**（不上 Zustand/Redux/Router），纯 React Context + 模块组件
    - 5 个 Context：Runtime / Vault / Mode / ShellPolicy / Toast
    - 9 个模块独立到 `web/src/modules/<name>/`
    - Agent Studio 因复杂度高，单独再分 5-6 个 commit
    - 每个 commit 独立 typecheck + cargo test 验证，可断点续作
- write_wiki / edit_wiki E2E 决策：
  - 用户决定跳过手测，以已有自动化测试（4 条 Rust 测试在 `state.rs`）为准
  - 决策记录入 `docs/h7-app-tsx-refactor-plan.md` §0.1
- 同步更新：
  - `docs/dev-status.md`：新增 2026-05-04 快讯 + TODO 表 P0 改为 H7 拆分
  - `docs/实施过程记录.md`：本条
  - `docs/交接状态卡.md`：当前接力指向 Sonnet 4.6 执行 H7 Phase 0
- 验证结果：
  - `cargo test`：233 通过 ✅（无后端改动，复核基线）
  - `npm run typecheck`：零错误 ✅（无前端代码改动）
  - 仅文档新增，无回归风险
- 下一步：Sonnet 4.6 接手按 `docs/h7-app-tsx-refactor-plan.md` §4 Phase 0 起手（4 个内嵌组件外移）

---

## 2026-04-30 — 运行模块视觉微调收口：页签头、间距、空态提示（Codex）

- 背景：用户要求对“运行”模块做一轮视觉微调收口，聚焦页签头信息层级、内容间距与空态提示可读性。
- 本轮完成：
  - `web/src/App.tsx`
    - 运行模块主面板增加页签说明文案（根据当前页签动态切换）。
    - 为页签内容增加 `operations-content` 容器，统一内容区节奏。
    - 优化队列空态文案，改为“标题 + 指引”两行结构，降低理解成本。
    - 运行统计空态文案改为更明确的下一步引导。
    - `QueuePanel` 收敛为嵌入式内容块，去除重复的二级大标题与外层 panel 包裹，减少视觉冗余。
  - `web/src/styles.css`
    - 新增并优化运行模块样式：`operations-module`、`operations-tabs__hint`、`operations-content`、`queue-panel`、`operations-empty-state`。
    - 压缩页签区域上下间距，增强信息密度与层级清晰度。
    - 运行统计空态样式改为边框提示卡（非裸文本），提升空态可见性。
  - 交接文档
    - 更新 `docs/交接状态卡.md`（同步到 2026-04-30 最新状态与下一步）。
    - 新增 `docs/claude-handoff-2026-04-30.md`（Claude 直接接手执行清单）。
- 验证结果：
  - `web`: `npm run typecheck` ✅

## 2026-04-30 — 运行模块链路收敛：移除 queue/stats 旧路由冗余（Codex）

- 背景：用户确认“运行模块可见”，要求进一步去除旧链路冗余，避免双路径维护。
- 本轮完成：
  - `web/src/types.ts`
    - `ModuleId` 移除旧模块标识：`queue`、`stats`。
  - `web/src/App.tsx`
    - `handleNavModuleSelect` 移除 `queue/stats -> operations` 兼容分支。
    - 删除旧页面渲染分支：
      - `activeModule === "queue"`
      - `activeModule === "stats"`
    - 保留唯一入口：`activeModule === "operations"`（内部 `queue/stats` 页签）。
- 验证结果：
  - `web`: `npm run typecheck` ✅
  - 文本检索确认：不存在旧 `activeModule === "queue/stats"` 分支 ✅

## 2026-04-30 — 运行模块空白修复：队列+统计成功合并到“运行”（Codex）

- 背景：用户手测反馈“运行模块右侧空白”，说明仅完成侧栏重排，未完成页面渲染合并。
- 本轮修复：
  - `web/src/App.tsx`
    - 新增 `activeModule === "operations"` 页面分支。
    - 在“运行”模块内加入双页签：
      - `任务队列`（复用原 `QueuePanel`）
      - `运行统计`（复用原 stats 展示块）
    - 页签切换时分别触发：
      - 队列刷新：`listIngestQueue`
      - 统计刷新：`loadVaultStats`
  - `web/src/styles.css`
    - 新增运行页签样式：`operations-tabs*`。
- 验证结果：
  - `web`: `npm run typecheck` ✅

## 2026-04-30 — H6-S3 历史 Runs 细节修复：按钮横排 + 管理模式可见差异（Codex）

- 背景：用户反馈 2 个问题：
  - “归档”按钮出现竖排文字；
  - 勾选/取消“管理模式”时视觉与结果差异不明显，疑似逻辑异常。
- 本轮修复：
  - `web/src/styles.css`
    - 对历史 run 操作按钮增加 `white-space: nowrap` + `min-width`，确保“归档”横向显示。
  - `web/src/App.tsx`
    - 增加 `agentVisibleRunCards`（非管理模式仅显示未归档 run）。
    - 增加 `agentArchivedRunCount` 与提示文案，明确“默认隐藏已归档 / 管理模式显示全部”。
    - 将“归档/恢复”操作按钮仅在管理模式显示，强化模式差异和防误触。
- 验证结果：
  - `web`: `npm run typecheck` ✅

## 2026-04-30 — H6-S3 历史 Runs 收敛：B 方案 + 软删除（Codex）

- 背景：用户要求将“历史 Runs”从横向长滚动改为节省空间方案，并增加删除能力且处理好与其他模块耦合。
- 本轮完成项：
  - 后端（软删除）：
    - `src-tauri/src/db.rs`
      - `agent_runs` 增加归档字段：`archived_at`、`archived_reason`（含迁移补列）。
      - `list_agent_runs(limit, include_archived)` 支持默认隐藏归档数据。
      - 新增 `archive_agent_run` / `restore_agent_run`，并写入系统事件。
    - `src-tauri/src/state.rs`
      - `list_agent_runs_impl` 新增 `include_archived` 参数。
      - 新增 `archive_agent_run_impl` / `restore_agent_run_impl`。
      - 归档安全约束：`running/reviewing` 禁止归档；存在待审批写入时禁止归档。
    - `src-tauri/src/commands.rs` + `src-tauri/src/main.rs`
      - 新增 Tauri 命令：`archive_agent_run` / `restore_agent_run`。
  - 前端（B 方案）：
    - `web/src/App.tsx`
      - 历史 Runs 折叠面板展开后改为“竖向限高列表 + 内部滚动”。
      - 新增“管理模式（显示已归档）”开关。
      - 每条 run 新增“归档/恢复”按钮（软删除）。
    - `web/src/styles.css`
      - 历史 runs 竖向列表样式、管理模式样式、归档标记样式。
    - `web/src/tauri-client.ts` + `web/src/types.ts`
      - `listAgentRuns` 增加 `includeArchived` 参数。
      - 新增 `archiveAgentRun` / `restoreAgentRun` 调用。
      - `AgentRunItem` 新增 `archived_at` 字段。
- 验证结果：
  - `web`: `npm run typecheck` ✅
  - `src-tauri`: `cargo fmt` ✅
  - `src-tauri`: `cargo check` ⚠️（当前 WSL 缺少 `pkg-config` 与 gtk/glib 依赖，无法完成）
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
  ```powershell
  # Agent Studio 手测
  # 1) 左侧“历史 Runs”默认收起，展开后为竖向滚动列表（非横向长滑）
  # 2) 开启“管理模式（显示已归档）”，可看到已归档 run
  # 3) 对已完成 run 点击“归档”后，默认视图隐藏；管理模式中可“恢复”
  # 4) 对 running/reviewing run 归档应被拒绝，并给出状态提示
  ```

## 2026-04-30 — H6-S3 下一步：Agent 历史 Run 快速切换条（Codex）

- 背景：用户要求“测试通过后继续推进”；当前活跃 TODO 中“Agent 运行历史 UI”虽有聊天气泡基础，但缺少显式 Run 列表入口，状态可见性弱。
- 本轮完成项：
  - `web/src/App.tsx`
    - 新增 `agentRunCards`（按更新时间倒序）用于历史 run 可视化。
    - 聊天区顶部新增“历史 Runs”横向卡片条：
      - 展示 `#run_id + topic`
      - 展示状态标签（生成中/待审阅/已写入/失败/排队）
      - 展示更新时间
      - 点击任一卡片可切换当前 run（右侧任务/草稿/工具联动）
    - 新增 `getAgentRunStatusTone(...)`，统一 run 状态到颜色语义映射。
  - `web/src/styles.css`
    - 新增 run 条样式：`agent-studio__run-strip*`、`agent-studio__run-card*`、状态色标签。
    - 采用横向滚动卡片，避免侵占聊天主区纵向空间。
- 验证结果：
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # Agent Studio 手测
  # 1) 左侧聊天区顶部看到“历史 Runs”卡片条
  # 2) 点击不同 run 卡片，右侧标题与工具时间线随 run 切换
  # 3) 检查状态标签颜色：生成中(蓝)/待审阅(黄)/已写入(绿)/失败(红)
  ```

## 2026-04-30 — H6-S3 UI 收敛：设置模块拆分 + 工具页策略瘦身（Codex）

- 背景：用户要求将“拖拽行为 / Shell 策略”从 LLM Provider 区独立出来，并要求 Agent 工具页删除重复策略下拉，保留紧凑档位切换，释放终端空间。
- 本轮完成项：
  - `web/src/App.tsx`
    - Settings 从单一大块拆分为 3 个独立模块：
      - `LLM Provider 配置`
      - `拖拽行为`
      - `Shell 策略（全局）`
    - Tool 页 `Shell 策略` 区改为紧凑模式：仅保留 `严格/平衡/高能力` 档位按钮，不再显示 5 个详细下拉。
    - 新增 `handleApplyAndSaveShellPolicyProfile(...)`，工具页点击档位后立即保存全局策略。
  - `web/src/styles.css`
    - 新增设置模块拆分样式（单列字段、章节标题）。
    - 新增 `agent-studio__shell-policy--compact` 样式，压缩策略区占用高度。
- 验证结果：
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # 设置页：
  # 1) 确认“拖拽行为”独立于 LLM Provider 面板
  # 2) 确认“Shell 策略（全局）”独立面板可刷新/保存
  # 工具页：
  # 3) 确认只显示“档位”按钮，不再显示 5 个下拉
  # 4) 点击“严格/平衡/高能力”后，看到“已切换为...策略”提示
  ```

## 2026-04-30 — H6-S3 P1 小步推进：Shell 策略扩展到 5 维（Codex）

- 背景：在 P0 可配置基础上，继续提高“高能力可控性”，避免只有 3 项旋钮导致策略过粗。
- 本轮完成项：
  - `src-tauri/src/models.rs`
    - `ShellPolicyConfig` 新增：
      - `manual_write_decision`
      - `agent_read_decision`
    - 补齐默认值（均为 `auto_allow`，保持向后兼容）。
  - `src-tauri/src/agent_policy.rs`
    - 分类器新增两类决策分支：
      - `manual + write -> manual_write_decision`
      - `agent + read -> agent_read_decision`
    - 增加对应单测（可强制审批）。
  - `web/src/types.ts`
    - 前端类型同步 5 维策略结构。
  - `web/src/App.tsx` + `web/src/styles.css`
    - 工具页 Shell 策略面板从 3 项扩展到 5 项（含新增 `manual: write`、`agent: read`）。
    - 预设档位（严格/平衡/高能力）同步升级到 5 维。
- 验证结果：
  - `src-tauri`: `cargo fmt` ✅
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd src-tauri; cargo test
  cd ../web; npm run typecheck
  ```
  ```powershell
  # Agent Studio -> 工具 -> Shell 策略
  # 1) 将 agent: read 改为“需要审批”，保存
  # 2) 触发 agent 读命令（任务模式），观察 policy_decision=require_approval
  # 3) 将 manual: write 改为“需要审批”，保存后执行 mkdir test-x，观察决策变化
  ```

### 补充收口（同轮）

- `web/src/App.tsx` + `web/src/styles.css`
  - 在 Settings 模块新增“Shell 策略（全局）”配置区：
    - 支持刷新/保存
    - 支持严格/平衡/高能力档位预设
    - 支持 5 维策略下拉编辑
  - 与 Agent 工具页策略面板共用同一份状态与后端配置，避免双入口配置不一致。

## 2026-04-29 — H6-S3 P0 开工：Shell 策略可配置骨架（Codex）

- 背景：用户要求“在保证系统安全下把能力最大化”，并要求先写可交接计划文件，确保限流后其他模型可继续推进。
- 计划落盘：
  - 新增 `docs/h6-shell-max-safety-plan.md`，定义 P0~P3 路线、验收、接手顺序与风险约束。
- 本轮代码落地（P0 核心骨架）：
  - `src-tauri/src/models.rs`
    - 新增 `ShellPolicyDecision`、`ShellPolicyConfig`。
    - `AppConfig` 增加 `shell_policy` 可选字段（持久化到 app-config）。
  - `src-tauri/src/agent_policy.rs`
    - 新增 `classify_shell_policy_with_config(...)`。
    - 保留 `classify_shell_policy(...)` 作为默认配置包装。
    - 新增可配置策略单测（manual unknown / agent write）。
  - `src-tauri/src/state.rs`
    - `AppStateData` 增加 `shell_policy` 运行时字段。
    - `new/new_with_path/persist_config` 全链路接入 `shell_policy` 读写。
    - `run_shell_impl` 改为读取当前策略配置做决策。
    - 新增 `get_shell_policy_config` / `set_shell_policy_config`。
  - `src-tauri/src/commands.rs` + `src-tauri/src/main.rs`
    - 暴露并注册 `get_shell_policy_config` / `set_shell_policy_config` 命令。
  - `web/src/types.ts` + `web/src/tauri-client.ts`
    - 新增 `ShellPolicyConfig` 类型与 Tauri 调用封装，供后续 UI 直接接线。
  - `src-tauri/src/vault.rs`
    - 初始化配置时补 `shell_policy: None`，保持配置结构一致。
- 验证：
  - 当前轮已完成前端 `npm run typecheck`（详见下一轮收口验证）。
  - Rust 全量测试需在 Windows 环境执行并回传。
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd src-tauri; cargo test
  cd ../web; npm run typecheck
  ```

### 补充收口（同轮）

- `web/src/App.tsx` + `web/src/styles.css`
  - Agent 工具页新增 Shell 策略面板（3 个决策旋钮）：
    - `manual_unknown_decision`
    - `agent_write_decision`
    - `agent_unknown_decision`
  - 已接入读取/保存命令：`get_shell_policy_config` / `set_shell_policy_config`
  - 保存后状态提示“后续命令将按新策略执行”
  - 新增一键策略档位：`严格 / 平衡 / 高能力`，减少逐项手工切换成本

## 2026-04-29 — H6-S2 继续推进：Shell 会话交互增强 + 深色终端皮肤（Codex）

- 背景：用户要求在“方案 B + 深色终端皮肤”基础上继续优化交互，重点解决命令输入被挤压、常用命令兼容性和结果可读性问题。
- 本轮完成项：
  - `web/src/App.tsx`
    - Shell 工具区新增快捷命令（`pwd/ls/ls -a/cd ../git status`）一键执行。
    - 新增 `↑/↓` 历史回填、历史清空、单条命令输出复制。
    - 每条命令增加状态徽标（运行中 / exit code）与流式显示占位（`执行中...`）。
    - 输入区改为底部固定容器，长历史滚动时输入框保持可见。
  - `web/src/styles.css`
    - 深色终端皮肤重绘（背景层次、边框对比、输出底色、元信息颜色）。
    - Shell 历史区与输入区分层，优化视觉密度和可读性。
    - 补充快捷命令、状态徽标、复制按钮样式。
  - `src-tauri/src/state.rs`
    - `normalize_shell_command_for_executor` 增强：支持 `ls/ls -a/ls -la/ls --all` 到 PowerShell 的稳定翻译。
    - `parse_cd_target` 增强：支持 `cd` 与 `cd /d <path>`，会话内更新 cwd。
- 验证结果：
  - `web`: `npm run typecheck` ✅
  - `web`: `npm run test -- --run` ❌（当前 WSL 环境缺失可选依赖 `@rollup/rollup-linux-x64-gnu`）
  - `src-tauri`: `cargo test` ❌（当前 WSL 缺失 `pkg-config` 与 gtk/glib 相关系统依赖）
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
  ```powershell
  # Agent Studio -> 工具 Tab（深色）
  # 1) 依次点击快捷命令 pwd / ls / ls -a，确认输出正常、无 PowerShell 参数歧义报错
  # 2) 输入 cd .. 回车，再执行 pwd，确认 cwd 已切换
  # 3) 连续执行两条命令后，用 ↑/↓ 选择历史命令并再次执行
  # 4) 点击“复制”验证单条输出可复制；点击“清空历史”后历史区重置
  ```

## 2026-04-29 — Agent Studio 右侧功能区重排：主区 + 底部 Shell 抽屉（Codex）

- 背景：用户反馈右侧工具区在常规窗口高度下被任务区挤压，Shell 输入框不可见；并要求按“方案2”优化（主内容 + 底部抽屉）。
- 本轮完成项：
  - `web/src/App.tsx`
    - 右侧结构改造为：
      - `agent-studio__right-main` 主内容滚动区（承载任务模式、工具时间线、草稿预览）
      - `agent-studio__shell-drawer` 底部抽屉（固定底部，不再随上方内容挤压）
    - 工具入口调整：
      - 右上“打开/收起工具能力”显式按钮
      - 抽屉头部二级开关（展开/收起 Shell 抽屉）
    - 空历史状态补充提示，避免“已展开但看起来像空白”误判
  - `web/src/styles.css`
    - 新增 `agent-studio__right-main`、`agent-studio__shell-drawer*` 样式
    - 抽屉内容区支持垂直 resize（默认 220px，最小 170px，最大 55vh）
    - 清理未使用旧样式（`tools-panel/tools-body/shell-head/shell-body`）
  - 文档同步：
    - 更新 `docs/dev-status.md`
    - 更新 `docs/交接状态卡.md`
- 结果：
  - 无需拉高窗口即可看到 Shell 输入框
  - 任务区与工具区职责分离，右侧信息密度更稳定
- 验证结果：
  - `web`: `npm run typecheck` ✅
  - `web`: `npm run test -- --run` ❌（WSL 环境缺失 `@rollup/rollup-linux-x64-gnu`；`npm i` 受 `@tauri-apps/cli-win32-x64-msvc` 平台限制）
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # Agent Studio 右侧回归：
  # 1) 点击“打开工具能力”，确认底部 Shell 抽屉展开且输入框可见
  # 2) 在抽屉中输入 Get-Date 并执行，确认历史区出现结果
  # 3) 拖拽抽屉底边，确认高度可调
  # 4) 收起抽屉，确认任务模式与草稿预览区可完整显示
  ```

> 历史记录（2026-04-20 ~ 2026-04-27）已归档至 `docs/实施过程记录-归档-2026-04.md`

## 2026-04-29 — H6-S2 继续推进：工具调用结构化时间线（Codex）

- 背景：用户要求继续推进，优先提升任务模式可观察性；原执行日志仅文本流，无法直观看到工具配对与耗时。
- 本轮完成项：
  - `web/src/App.tsx`
    - 新增 `agentExecTimeline` 解析逻辑：从 `agentEvents` 中提取 `tool_start/tool_end`
    - 按轮次配对 start/end，计算耗时（ms），保留 `awaiting_approval` 标记事件
    - 任务区 UI 改为“工具时间线”：标题、摘要、时间、耗时、详情折叠
  - `web/src/styles.css`
    - 新增时间线行布局样式（标题/侧边时间、详情折叠 pre、级别着色）
    - 优化日志可读性（换行策略、行内间距）
- 结果：
  - 任务模式可直接观察每次工具调用的起止与耗时
  - 细节默认折叠，信息密度可控
- 验证结果：
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # Agent Studio -> 任务模式
  # 1) 运行一个会触发 search/read/write/edit 的任务
  # 2) 检查“工具时间线”是否显示 #轮次、工具名、耗时
  # 3) 展开详情确认 tool_end 原始输出可查看
  ```

## 2026-04-29 — H6-S2 继续推进：任务续跑最小闭环（Codex）

- 背景：用户要求继续推进；当前任务失败/中断时恢复成本高，需要最小续跑能力。
- 本轮完成项：
  - `web/src/App.tsx`
    - 抽象 `executeAgentTask(instructionInput, statusLabel)`，统一任务执行路径
    - 新增 `handleContinueAgentTask`：
      - 复用当前 run
      - 自动拼接原始指令、最近 `tool_start/tool_end` 轨迹、上次结果摘要
      - 生成续跑指令并触发任务模式
    - 任务模式按钮区新增“继续任务”按钮（无轨迹/无结果时禁用）
- 结果：
  - 任务中断后可一键续跑，无需手工重写完整上下文
- 验证结果：
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # Agent Studio -> 任务模式
  # 1) 运行任务后，确保有工具时间线或任务结果
  # 2) 点击“继续任务”
  # 3) 观察状态提示“续跑任务已完成”或失败信息；检查事件流追加新轮次记录
  ```

## 2026-04-29 — H6-S2 继续推进：E2E 审批验证快捷入口（Codex）

- 背景：继续推进验证收敛，需要降低 write/edit 审批链路手测成本。
- 本轮完成项：
  - `web/src/App.tsx`
    - 任务模式新增两枚快捷按钮：
      - `填充：写入审批验证`
      - `填充：编辑审批验证`
    - 点击后自动写入推荐测试指令，减少手工输入成本
  - `web/src/styles.css`
    - 新增 `.agent-studio__task-presets` 样式，保证按钮在窄屏可换行
- 验证结果：
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # Agent Studio -> 任务模式
  # 1) 点击“填充：写入审批验证”并执行，确认进入 awaiting_approval
  # 2) 批准后检查 wiki/agent-e2e-write.md 写入
  # 3) 点击“填充：编辑审批验证”并执行，确认进入 awaiting_approval
  # 4) 批准后检查标题已改为 Agent E2E Edited
  ```

## 2026-04-29 — H6-S2 断点续做：任务模式注入记忆/技能上下文（Codex）

- 背景：Claude 最后停在 TODO #3（“上下文面板实质注入”）判定，确认 `build_loop_prompt` 只使用了 `instruction/wiki_excerpt/history`，未使用记忆与技能。
- 本轮完成项：
  - `src-tauri/src/commands.rs`
    - `run_agent_task` 命令新增 `memory_context: Option<String>`
  - `src-tauri/src/state.rs`
    - `run_agent_task_impl` 签名新增 `memory_context` 并透传到 service
  - `src-tauri/src/agent_service.rs`
    - `run_agent_task` 新增 `memory_context` 参数，裁剪到 2400 字符
    - 调用 `run_agent_task_loop` 时传入上下文
  - `src-tauri/src/agent_loop.rs`
    - `run_agent_task_loop` 与 `build_loop_prompt` 签名新增 `memory_context`
    - prompt 增加“上下文配置（记忆/技能）”段落
    - 单测更新：`loop_prompt_contains_tool_contract` 和审批暂停测试调用参数
  - `web/src/tauri-client.ts`
    - `createRunAgentTaskArgs` / `runAgentTask` 新增 `memoryContext/memory_context` 透传
  - `web/src/App.tsx`
    - `handleRunAgentTask` 运行前构造上下文：
      - 记忆列表格式化为 `- key: value`
      - 激活 skill 模板做变量替换：`{{topic}}`、`{{memories}}`
      - 合成 `memory_context` 发送给后端任务模式
- 验证结果：
  - `src-tauri`: `cargo fmt` ✅
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
  ```powershell
  # Agent Studio -> 任务模式
  # 1) 选中一个带 skill 的 run，并确保“记忆上下文”有内容
  # 2) 输入同一任务指令，观察输出是否体现 skill 语气与记忆事实
  # 3) 清空 skill 或记忆后重试，比较输出差异
  ```

## 2026-04-29 — Agent Studio 布局压缩与交互收口（Codex）

- 背景：用户反馈 Agent Studio 上下文配置区域占用过大，挤压对话空间；同时希望 Shell 不干扰主流程。
- 本轮完成项：
  - `web/src/App.tsx`
    - 新增 `agentContextOpen` 状态（默认 `false`）
    - 左侧将“记忆上下文 + 技能模板”合并为可折叠区：`上下文配置（记忆 / 技能）`
    - 右侧 Shell 标题改为 `调试 Shell（可选）`，并增加提示：任务模式会自动调用工具，手工 Shell 仅作调试
  - `web/src/styles.css`
    - 新增 `.agent-studio__context*` 折叠容器样式
    - 新增 `.agent-studio__shell-hint` 辅助提示样式
    - 修正 `.agent-studio__skill-item` 背景变量为 `--bg-card`（避免未定义变量导致背景异常）
- 结果：
  - Agent 主聊天区与任务区可视空间明显增加
  - Shell 入口保留，但默认不占据核心操作视图
- 验证结果：
  - `web`: `npm run typecheck` ✅
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  ```
  ```powershell
  # 打开 Agent Studio，检查：
  # 1) 左侧“上下文配置（记忆/技能）”默认折叠，可展开编辑；
  # 2) 右侧“调试 Shell（可选）”默认折叠；
  # 3) 任务模式正常可用，不受折叠区影响。
  ```

## 2026-04-28 — H6-S2 最小下一步：write_wiki 审批前置链路（Codex）

- 背景：用户要求“继续推进并收口可交接状态”；在现有 `run_shell/search_wiki/read_wiki` 基础上，先接入 `write_wiki` 的安全链路，避免直接写盘带来回归风险。
- 本轮完成项：
  - `src-tauri/src/agent_tools.rs`
    - `AgentToolAction` 新增 `WriteWiki { path, content }`
    - `parse_agent_loop_decision` 新增 `write_wiki` 解析
    - 新增测试：`parse_agent_loop_decision_supports_write_wiki`
  - `src-tauri/src/agent_loop.rs`
    - 工具启动日志新增 `write_wiki` 分支（展示 path + chars）
    - 循环 prompt 增加 `write_wiki` 工具契约与“审批前置，不直接落盘”规则
    - 主循环新增审批暂停语义：识别 `decision=require_approval` 后写入 `awaiting_approval` 事件并提前结束循环
    - 测试 `loop_prompt_contains_tool_contract` 增加 `write_wiki` 断言
    - 新增测试：`run_agent_task_loop_stops_on_approval_required`
  - `src-tauri/src/agent_runtime.rs`
    - `execute_tool_action` 新增 `write_wiki` 分支
    - 新增 `resolve_agent_write_target_path`：复用 `validate_agent_write_path` 做路径校验
    - 新增 `execute_write_wiki_action`：当前统一返回 `decision=require_approval`，只记录 preview，不执行真实写盘
    - 新增测试：
      - `resolve_agent_write_target_path_allows_wiki_markdown`
      - `resolve_agent_write_target_path_rejects_non_markdown`
- 关键决策：
  - 本轮不引入真实写盘，先完成“解析 -> 校验 -> 事件可观测 -> 审批前置拒绝”闭环。
  - 这样下一轮只需接审批确认入口与写盘动作，不改主循环与工具协议。
- 验证结果：
  - `src-tauri`: `cargo fmt` ✅
  - `web`: `npm run typecheck` ✅
  - `src-tauri`: `cargo test` ❌（WSL 缺 `pkg-config/gtk/glib`，属于环境阻断，非本轮逻辑错误）
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
  ```powershell
  # Agent Studio -> 任务模式（Beta）示例指令
  # “把 wiki/rust-actor.md 改写成三段结构，并输出结果”
  # 预期：事件流出现 write_wiki，tool_end 中包含 decision=require_approval，且文件不变
  ```
- 下一步：
  1. 接入 `write_wiki` 的审批确认入口（approve/reject）；
  2. 审批通过后执行真实写盘（保留 PathGuard + 事件日志 + 最小回滚信息）；
  3. 新增 `edit_wiki` patch 工具，继续保持默认审批前置。

## 2026-04-28 — H6-S2 模块化第一步（Codex）

- 背景：用户要求“修完图标后直接推进下一步”，本轮先做低风险模块化收敛。
- 本轮完成项：
  - 新增 `src-tauri/src/agent_tools.rs`：
    - `AgentToolAction`
    - `AgentLoopDecision`
    - `parse_agent_loop_decision`（含 `action/actions[0]` 兼容）
    - 3 条解析单测迁移到该模块
  - `src-tauri/src/state.rs`：
    - `run_agent_task_impl` 改为调用 `agent_tools::parse_agent_loop_decision`
    - 删除 `state.rs` 内重复的 JSON/动作解析实现
  - `src-tauri/src/main.rs`：
    - 增加 `mod agent_tools;`
  - 新增 `src-tauri/src/agent_loop.rs`：
    - 抽离 `build_loop_prompt` / `build_final_prompt`
    - `state.rs` 改为调用模块函数，减少主文件字符串拼装噪音
  - 主循环迁移（继续收敛）：
    - `agent_loop.rs` 新增 `AgentLoopRuntime` trait 与 `run_agent_task_loop`
    - `state.rs` 实现 `AgentLoopRuntime`（LLM 调用、工具执行、事件写入）
    - `run_agent_task_impl` 改为委托 `run_agent_task_loop`，入口层只保留参数校验/收尾
  - 总结阶段迁移（继续收敛）：
    - `agent_loop.rs` 新增 `summarize_agent_task`
    - `state.rs` 不再拼装/调用总结 prompt，统一委托 `agent_loop`
    - 增加 `summarize_agent_task_uses_runtime_response` 单测
  - Runtime 实现迁移（继续收敛）：
    - 新增 `src-tauri/src/agent_runtime.rs`，承载 `impl AgentLoopRuntime for AppState`
    - `state.rs` 删除 runtime trait 实现块，仅保留业务能力方法
    - `state.rs` 对 runtime 需要的方法改为 `pub(crate)`：`get_llm_provider`、`outbox_db_path`、`read_wiki_page_for_agent`、`strip_think_tags`、`current_timestamp_ms`
  - Runtime 内部继续解耦：
    - `agent_runtime.rs` 将 `execute_tool_action` 拆分为 `execute_shell_action` / `execute_search_wiki_action` / `execute_read_wiki_action`
    - 降低后续接审批流与新工具时的冲突概率
  - PathGuard 最小落地（read 路径）：
    - `agent_policy.rs` 新增 `validate_agent_read_path(vault_path, target_path)`
    - `read_wiki_page_for_agent` 接入 PathGuard，强制仅可读取 `vault/wiki/*.md`
    - 补充 `agent_policy` 单测：wiki 内 md 允许、wiki 外路径拒绝
  - read_wiki 继续下沉（state 再瘦身）：
    - `read_wiki_page_for_agent` 从 `state.rs` 迁移到 `agent_runtime.rs`
    - `state.rs` 仅暴露最小能力：`vault_path_or_err`、`resolve_existing_wiki_page_path`
    - `agent_runtime` 统一串联 `PathGuard + 页面读取 + 输出裁剪`
  - runtime 日志结果统一化（继续收敛）：
    - 新增 `ToolExecOutcome` 与 `outcome_info/outcome_warn`，统一三路工具返回结构
    - 抽取 `truncate_chars` 复用，减少重复裁剪逻辑
  - runtime 集成测试补强：
    - `agent_runtime.rs` 新增 `read_wiki_page_for_agent_allows_wiki_markdown`
    - `agent_runtime.rs` 新增 `read_wiki_page_for_agent_rejects_outside_wiki`
  - 任务模式主服务下沉（继续收敛）：
    - 新增 `src-tauri/src/agent_service.rs`，承载 `run_agent_task` 主流程
    - `state.rs` 的 `run_agent_task_impl` 变为单行委托，职责回归“状态入口”
  - PathGuard write 前置（为后续 write/edit 工具铺路）：
    - `agent_policy.rs` 新增 `validate_agent_write_path(vault_path, target_path)`
    - 规则：仅允许写入 `vault/wiki` 且扩展名必须是 `.md`
    - 新增单测：`pathguard_write_allows_markdown_under_wiki` / `pathguard_write_rejects_non_markdown`
- 验证结果：
  - `src-tauri`: `cargo fmt` ✅
  - `web`: `npm run typecheck` ✅
- 下一步：
  - 继续抽离 `agent_loop.rs`，把 `run_agent_task_impl` 主循环从 `state.rs` 迁出（保持行为不变）。

## 2026-04-28 — Windows 任务栏图标修复（Codex）

- 背景：用户反馈应用启动后任务栏按钮图标为白色方块。
- 根因：
  - `src-tauri/icons/icon.ico` 是 1x1 占位图（70 bytes），导致 Windows 任务栏无法显示正常图标。
- 本轮完成项：
  - 用 `refer_project/src-tauri/icons/icon.ico` 替换 `src-tauri/icons/icon.ico`（6 尺寸多分辨率 ico）。
  - `src-tauri/tauri.conf.json` 的 `app.windows[0]` 增加 `"icon": "icons/icon.ico"`，显式绑定主窗口图标。
- 验证：
  - `file src-tauri/icons/icon.ico` 输出：`MS Windows icon resource - 6 icons`。
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd src-tauri
  cargo tauri dev
  ```
  - 启动后确认任务栏按钮和 Alt+Tab 预览均显示 LLM Wiki 图标，不再是白方块。
  - 若仍旧图标缓存，退出应用后重启资源管理器：
  ```powershell
  taskkill /f /im explorer.exe
  Start-Process explorer.exe
  ```
- 下一步：
  - 继续 H6-S2 模块化（`agent_tools.rs` / `agent_loop.rs` 抽离）。

### 补充修正（同日）

- `tauri.conf.json` 回退 `app.windows[0].icon`：Tauri v2 schema 不支持该字段（会导致 `cargo tauri dev` 启动报错）。
- `build.rs` 增加 `cargo:rerun-if-changed=icons/icon.ico`，确保图标替换后可触发资源重建。
- 任务栏图标素材改为与前端左上角一致：以 `web/src/assets/LLM_Wiki.png` 重新生成 `src-tauri/icons/icon.ico`（60x60 PNG 内嵌 ICO）。

## 2026-04-28 — H6-S2 多轮工具循环推进（Codex）

- 背景：用户要求“继续展开”，并已回传上一轮测试正常；本轮目标是把 S2 从单次规划推进到可迭代工具循环。
- 本轮完成项：
  - 后端：
    - `run_agent_task_impl` 升级为多轮决策（预算 1-8 轮），每轮由 LLM 输出单个 JSON action 或 done 信号。
    - 工具集合从仅 `run_shell` 扩展为：`run_shell / search_wiki / read_wiki`。
    - 每轮写入 `tool_start/tool_end` 事件，保留 tool 输出摘要用于最终总结。
    - 新增 `read_wiki_page_for_agent`（只读 md，按字符截断）。
    - 解析器升级：支持 `action` 与 `actions[0]` 两种 JSON 形态，兼容 `final/answer` 字段。
  - 策略收敛：
    - `classify_shell_policy` 统一改为 `src-tauri/src/agent_policy.rs`，删除 `state.rs` 重复实现。
    - `src-tauri/src/main.rs` 增加 `mod agent_policy;`，模块注册完整。
- 验证结果：
  - `src-tauri`: `cargo fmt` ✅
  - `web`: `npm run typecheck` ✅
  - `src-tauri`: `cargo test` ❌（WSL 环境缺 `pkg-config/openssl`，非本轮业务逻辑错误）
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
- 任务模式手动验证（建议）：
  1. 打开 Agent Studio，进入“任务模式（Beta）”；
  2. 输入：`先检索 wiki 中与 Rust actor 相关页面，再读取最相关页面并给出实现建议`，预算 4；
  3. 点击“运行任务”，确认事件区出现 `search_wiki/read_wiki/run_shell` 的 `tool_start/tool_end`；
  4. 确认最终结果仍为四段结构化输出（任务理解/执行计划/风险与护栏/当前结论）。
- 下一步：
  1. Windows 侧验证多轮 multi-tool 行为；
  2. 在不改行为前提下抽离 `agent_tools.rs` 与 `agent_loop.rs`，继续 S2 模块化收口。

## 2026-04-27 — H6-S2 最小骨架开工（Codex）

- 背景：用户确认 S1/S1.5 验证全绿后，要求“直接开干”下一步开发。
- 本轮完成项（最小闭环）：
  - 后端：
    - 新增 Tauri 命令 `run_agent_task(run_id, instruction, max_iterations?) -> String`
    - `run_agent_task_impl` 升级：LLM 规划 JSON（actions）-> 受控执行 `run_shell(source=agent)` -> LLM 总结
    - 状态机修复：`complete_agent_run_impl` 支持 `reviewing`；`reviewing` 不写 `completed_at`
  - 前端：
    - `tauri-client.ts` 新增 `runAgentTask` 与参数构造器
    - Agent Studio 新增“任务模式（Beta）”面板：任务输入、预算轮次、结果展示
  - 说明：
    - 当前为轻量工具循环（仅 `run_shell`），下一轮再接入 `agent_tools + policy + callbacks` 标准化抽象
- 验证结果：
  - `web`: `npm run typecheck` ✅
  - `src-tauri`: `cargo fmt` ✅
  - `src-tauri`: 本环境不做完整构建（WSL 缺 gtk/pkg-config）；待用户 Windows 侧复测
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
- 任务模式手动验证：
  1. 打开 Agent Studio，选择一个已有 run；
  2. 在“任务模式（Beta）”输入：`检查当前项目状态并给出下一步建议`，预算轮次设为 3；
  3. 点击“运行任务”，确认事件区出现 `tool_start/tool_end`，结果区有四段结构化输出。
- 下一步：
  1. Windows 侧验证 `run_agent_task` 可用；
  2. 进入 S2 第二步：接入受控工具调用循环。

## 2026-04-27 — H6 计划重排 + S1.5 安全前置开工（Codex）

- 背景：用户要求 Agent 模块支持可控 shell 能力，并降低多模型接力成本；同时要求评估与 Deep Research 的边界重叠并收敛实施路线。
- 本轮决策：
  - H6 从两段改为三段：`S1 收口 -> S1.5 安全前置 -> S2 Agentic Loop`。
  - `refer-rust-daerwen-agent` 继续作为主参考（tool loop + PathGuard + policy），`pi-mono` 不作为主实现参考。
  - 在 S2 之前先把 shell 命令策略化，避免仅靠黑名单。
- 已实施（进行中）：
  - 后端：
    - `ShellResult` 增加 `policy_action / policy_decision / executor`。
    - `run_shell` 命令增加 `source` 参数（manual/agent）。
    - `run_shell_impl` 新增最小策略分类：
      - destructive -> deny（阻断）
      - source=agent 且 write/unknown -> require_approval（先阻断，待审批流）
      - 其余 -> auto_allow
  - 前端：
    - `runShell` 客户端函数增加 `source` 入参。
    - Agent Studio shell 历史增加策略元信息展示。
    - 修复 `React.useRef` 模块导入写法（改为 `useRef`）。
    - 补齐 `.agent-studio__shell*` 样式。
    - 技能表单增加提示：同名 `skill_key` 会覆盖并升版本（降低“无法自由选择”误解）。
  - 文档：
    - `docs/dev-status.md` / `docs/交接状态卡.md` 同步为 S1/S1.5/S2 结构化交接版本。
- 验证结果：
  - `web`: `npm run typecheck` ✅
  - `web`: `npm test -- --run` ❌（环境缺少可选依赖 `@rollup/rollup-linux-x64-gnu`）
  - `src-tauri`: `cargo check` ❌（当前 WSL 缺少 `pkg-config/gtk` 依赖，属于环境阻断，非本轮逻辑错误）
  - 用户 Windows 复测：`cargo test` 首次失败，报错 `tokio::process` feature 未开启
- 热修复：
  - `src-tauri/Cargo.toml`：`tokio` features 增加 `"process"`，匹配 `run_shell_impl` 的 `tokio::process::Command` 用法
- 用户侧 PowerShell 验证命令：
  ```powershell
  cd web; npm run typecheck
  cd ../src-tauri; cargo test
  ```
  ```powershell
  # 打开 Agent Studio > Shell 面板后依次执行
  Get-Date
  Get-ChildItem
  rm -rf /
  NotARealCommand123
  ```
- 下一步：
  1. 用户侧 Windows 手动验证 shell 行为（含策略阻断）；
  2. 若验证通过，冻结 S1/S1.5 并进入 S2 接口设计。

## 2026-04-27 — 方向 H H5：Auto-Memory + Draft Rewrite + Ask 联动 + Skill 模板变量（Claude）

- 背景：H4 验证通过后，用户选定 B→A→C→D 顺序实施 H5 四项功能。
- 本轮完成项：
  - **H5-B：审批后自动提炼全局记忆**
    - `approve_agent_draft_impl` 写盘后异步调用 `extract_and_save_memories_from_draft`
    - LLM 从草稿内容提炼 3-5 条 `key: value` 全局记忆，upsert 到 `agent_memories`（run_id=None）
    - 事件日志记录提炼数量；LLM 不可用时静默降级
  - **H5-A：基于批注重写草稿**
    - 新增 `rewrite_agent_draft_impl(draft_id, comment)`：原草稿（6000字）+ 批注 → LLM → 新草稿记录
    - 新增 Tauri 命令 `rewrite_agent_draft`，120s 超时（LLM 重写耗时较长）
    - 前端草稿头增加"批注重写"输入栏，Enter 触发，生成后自动选中新草稿
  - **H5-C：Ask 联动（ask_first）**
    - `generate_agent_draft_impl` 新增 `ask_first: bool` 参数
    - ask_first=true 时先调 `query_ask_with_options(top_k=3)` 获取现有知识库答案（≤1200字），注入生成 prompt
    - 前端 composer 新增"Ask 联动"勾选框；事件消息加 `ask: on` 标记
  - **H5-D：Skill 模板变量**
    - Skill prompt 支持 `{{topic}}` / `{{memories}}` 占位符，生成时自动展开
    - `memories_context` 整体替换到 `{{memories}}`，`topic` 替换到 `{{topic}}`
  - 前端 `tauri-client.ts`：`generateAgentDraft` / `createGenerateAgentDraftArgs` 新增 `askFirst/ask_first` 双键；`rewriteAgentDraft` 函数（120s 超时）
  - 测试：`app-utils.test.ts` 更新 `createGenerateAgentDraftArgs` 断言含 `researchMode`
- 提交：
  - `efe238e`：feat(H5): auto-memory + draft rewrite + ask-first injection
  - `2bdd80c`：feat(H5-D): skill 模板变量 `{{topic}}` `{{memories}}`
- 验证：`npm run typecheck` ✅；`cargo test` 待 Windows 复核
- 下一步：用户 Windows 端到端验证 H5 四项功能后，由用户定 H6 方向

## 2026-04-27 — 方向 H H4：Research Mode 检索增强（Claude）

- 背景：H3 验证通过后，按计划推进 H4——在生成草稿时增加可选的"检索增强"模式，用更丰富的 wiki 内容上下文提升 LLM 输出质量。
- 本轮完成项：
  - 后端 `state.rs`：
    - `generate_ai_wiki_markdown_draft_impl` 新增 `research_mode: bool` 参数
    - 新增 `extract_content_after_frontmatter(text, limit)` 辅助函数，跳过 YAML frontmatter 提取正文
    - `research_mode=true` 时：搜索条数 5→8，读取 wiki 文件实际正文前 400 字（原为摘要 120 字）
    - `generate_agent_draft_impl` 新增 `research_mode: bool`，事件消息加 `research: on` 标记
  - 后端 `commands.rs`：`generate_agent_draft` 新增 `research_mode: Option<bool>`
  - 前端 `tauri-client.ts`：`generateAgentDraft` / `createGenerateAgentDraftArgs` 支持可选 `researchMode`
  - 前端 `App.tsx`：新增 `agentResearchMode` state；composer-tools 区新增"检索增强"勾选框
  - 前端 `styles.css`：`.agent-studio__research-toggle` 样式
  - 测试：`createGenerateAgentDraftArgs` 断言更新含 `researchMode`
- 验证：`npm run typecheck` ✅；`cargo test` 待 Windows 复核
- 下一步：用户 Windows 验证后，由用户定 H5 方向
