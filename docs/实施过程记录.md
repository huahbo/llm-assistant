# 实施过程记录

> 用途：记录关键实施步骤、决策、风险与验证结果，便于回归与审计。

## 记录规范
- 按时间倒序或正序均可，但全文件保持一致。
- 每条记录至少包含：日期时间、目标、变更文件、验证结果、风险/后续。
- 只记录关键节点，避免流水账。

## 模板
### YYYY-MM-DD HH:mm
- 目标：
- 关键决策：
- 变更文件：
- 验证：
- 风险与后续：

---

## 2026-04-19 16:30 — P1 健康度复核 + PDF摄入稳定性修复 + Embed架构拆分

- 目标：接手 Gemini 遗留构建问题，修复 PDF 摄入不稳定，拆分 LLM/Embed Provider 架构。
- 关键决策：
  - **Gemini 遗留构建修复**：删除重复 `searchWikiPaths`，补 `getAppOverview()` 封装，补 MockQueryProvider `embed()` 实现。
  - **PDF 摄入超时**：根因为前端 `withTimeout` 默认 15s，PDF 需两次 LLM 调用（摘要+实体），改为 5 分钟（300s）。
  - **LLM 输入截断**：`generate_summary()` 截断到 8000 字符，防止长 PDF 超云端 token 限制。
  - **Embed 架构拆分**：新增 `get_embed_provider()`，始终走本地 Ollama embedding 模型（默认 `nomic-embed-text:latest`），与云端 LLM 解耦。embed 内容截断 4096 字符。
  - **Settings UI**：新增"Embedding 模型（本地 Ollama）"配置项，可配置 embed 模型和 base_url。
- 变更文件：
  - 后端：`models.rs`（embed_ollama_model/base_url 字段）、`state.rs`（get_embed_provider + generate_summary截断 + embed步骤改写 + set_llm_config/persist_config同步）、`vault.rs`（AppConfig补字段）
  - 前端：`types.ts`（LlmProviderConfig加embed字段）、`tauri-client.ts`（INGEST_TIMEOUT_MS=300s + saveLlmConfigFromRaw补embed字段）、`App.tsx`（Settings embed配置项 + handleSaveLlmConfig同步）、`app-utils.test.ts`（修复类型断言）
- 验证：`cargo test` 102/102，`npm test` 130/130，`typecheck` 零错误，`build` 通过。
- 风险与后续：Embed 向量目前存储但 `search_wiki_matches_rrf` 未接入向量检索路径（P20-5）；Settings UI 未加 Ollama LLM 模型/base_url 输入框（可按需补）。

---

## 2026-04-17 17:12 — P21-C1 实施完成：Global/Local + Hop 深度 + 布局冻结

- 目标：在 B 方案基础上推进 C 的第一阶段能力，补齐 Global/Local 模式与局部子图探索能力，同时保留 C 后续迭代空间。
- 关键决策：
  - 并行拆分执行：
    - 子任务 A（后端评估）：判断 C1 是否必须新增后端接口，给出阈值与接口草案（结论：C1 可纯前端实现）。
    - 子任务 B（前端实现）：落地 Global/Local、Hop 深度、冻结布局、偏好持久化与对应测试。
  - C1 先走前端 BFS 局部子图，不改后端接口；当图规模超过阈值（参考 `nodes > 2k / links > 8k`）或交互延迟超标时再上后端局部子图接口。
  - 抽取 `buildGraphLocalData` 与图谱模式持久化工具函数，保证后续 C2 迭代可复用。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck`：通过（WSL）。
  - `npm --workspace web run test -- --run`：WSL 阻塞（缺少 `@rollup/rollup-linux-x64-gnu` 可选依赖）。
  - `npm --workspace web run build`：同上阻塞。
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 验证点：
    - Global/Local 切换可用；Local 未选中中心节点时给出提示。
    - Hop 深度滑条在 Local 模式生效（1→3 时可见节点范围扩大）。
    - “冻结布局/恢复布局”可正确暂停/恢复图谱运动。
    - 图谱模式与 Hop 深度重启后可恢复（localStorage 持久化）。
- 风险与后续：
  - 当前 Local 子图是无向拓扑 BFS；C2 再补方向过滤（in/out/both）与后端 `get_knowledge_subgraph`。
  - WSL 测试链路仍被 Rollup 可选依赖阻塞，需 Windows 侧最终收口。

---

## 2026-04-17 16:55 — P21-B 实施完成：图谱主区 + 右侧详情面板

- 目标：落地用户已确认的 B 方案，解决图谱底部留白与信息不足问题，并保持错误可见与可操作性。
- 关键决策：
  - 并行拆分执行：
    - 子任务 A（前端实现）：图谱布局、过滤控件、节点详情、邻居模式与交互优化（`web/`）。
    - 子任务 B（文档交接）：同步 `agents.md` 与本记录，明确 C 方案进入后续迭代（非排除）。
  - 图谱布局升级为“左侧工作区 + 右侧详情面板”，左侧高度改为视口自适应，优先消除底部空白。
  - 节点交互改为“点击选中 + 右侧详情操作打开页面”，避免频繁跳页影响探索；保留一键跳转 Wiki。
  - 抽取 `buildGraphVisibleData` 纯函数，覆盖分组筛选/孤儿页过滤/邻居模式，并补最小单元测试。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck`：通过（WSL）。
  - `npm --workspace web run test -- --run`：WSL 阻塞（缺少 `@rollup/rollup-linux-x64-gnu` 可选依赖）。
  - `npm --workspace web run build`：同上阻塞。
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 验证点：
    - 图谱区占比明显增大，底部留白显著减少。
    - 右侧面板显示节点标题/路径/入度/出度/总连接与邻接列表。
    - 分组筛选、隐藏孤儿页、邻居模式、适配视图、重置筛选均可用。
    - 异常时保持可见错误提示，不出现整页白屏。
- 风险与后续：
  - 当前 `test/build` 在 WSL 受依赖形态影响，需要 Windows 侧收口复核。
  - P21-C（Global/Local 双模式 + hop 深度 + 局部子图）保留为后续迭代，不排除。

---

## 2026-04-17 16:20 — 图谱迭代方案确认：B 立即实施，C 纳入后续（非排除）

- 目标：根据用户反馈（图谱可显示但底部留白大、信息不足）冻结下一轮图谱优化路线，并同步三方交接口径（Claude Code / Codex / Gemini）。
- 关键决策：
  - 用户已明确选择 **方案 B** 作为下一轮实施目标：图谱主区 + 右侧详情面板，优先提升空间利用率与信息可读性。
  - **方案 C 不排除**，已写入后续迭代计划：Global/Local 双模式与 hop 深度控制在 B 完成后推进。
  - 本轮按用户要求仅做文档与计划更新，不改业务代码。
  - 下轮并行拆分（执行约束）：
    - 子任务 A（前端实现）：负责 P21-B UI/交互落地，仅改 `web/` 图谱相关文件。
    - 子任务 B（文档交接）：负责计划更新、验证记录与 CC/Gemini 交接，仅改 `agents.md` 与 `docs/实施过程记录.md`。
  - 对标口径保持一致：`external/article.txt` 与 `external/llm-wiki-main` 作为功能对标参考（功能优先，技术实现第二）。
- 变更文件：
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：
  - 本轮无业务代码改动，未触发构建/测试。
  - 手动验证重点（供下一轮 P21-B 完成后执行）：
    - 图谱区域应显著增大并占满模块主要可视区，底部留白问题消除。
    - 点击节点后右侧详情面板显示标题/路径/分组/入度/出度/邻接页面，并可一键打开 Wiki 页面。
    - 过滤/重置等控制项可用，异常场景保持可见错误提示，不得出现“空白无提示”。
- 风险与后续：
  - 若直接在 B 中引入过多高级交互，可能导致单轮范围膨胀；建议先以“可视区 + 详情面板 + 基础过滤”为收口线。
  - C 阶段建议在 B 验证通过后再启动，避免与当前迭代并行耦合。

---

## 2026-04-17 15:55 — P21 图谱点击白屏修复与可见错误提示

- 目标：修复“图谱节点点击后界面空白且无提示”的问题，确保跳转链路与 Wiki 主流程一致。
- 关键决策：
  - 并行模式执行：前端子代理审查点击链路与 Promise 异常路径；后端子代理审查 `node.id -> get_wiki_page_detail` 契约一致性；主控负责实现与收口。
  - 图谱节点点击统一走 `handleOpenWikiPage`，不再直接 `fetchWikiPageDetail(...).then(...)`，避免状态流断裂（`wikiActivePagePath/loading/error/citations` 不一致）。
  - 增加 `graphError` 状态与 `graphData === null` 可见提示，消除“空白无提示”场景。
  - 新增 `resolveGraphNodePagePath` 纯函数并补最小单测，覆盖节点路径健壮性（仅接受非空字符串）。
- 变更文件：
  - `web/src/App.tsx`：新增 `graphError` 状态、`resolveGraphNodePagePath`、`handleGraphNodeClick`，图谱渲染分支增加错误/空数据提示，节点点击改走统一打开流程。
  - `web/src/app-utils.test.ts`：新增 `resolveGraphNodePagePath` 单测。
- 验证：
  - `npm --workspace web run typecheck`：通过（WSL）。
  - `npm --workspace web run test -- --run`：受环境阻塞，WSL 缺少 `@rollup/rollup-linux-x64-gnu`（当前 `node_modules` 为 Windows 形态，可选依赖未匹配 Linux）。
  - `npm --workspace web run build`：同上阻塞（Rollup 可选依赖缺失）。
- 风险与后续：
  - 该轮逻辑修复已完成，但前端 test/build 需要在 Windows PowerShell 复核收口。
  - 建议后续补一条后端回归测试：从 `get_knowledge_graph_impl` 取 `node.id` 直接喂给 `wiki_page_detail`，覆盖路径形态一致性。

---

## 2026-04-17 15:40 — P21 完成：知识图谱可视化

- 目标：基于 `react-force-graph-2d` 实现 Obsidian 风格知识图谱可视化，节点 = wiki 页面，边 = citations。
- 关键决策：
  - 库选型：`react-force-graph-2d`（Canvas，非 WebGL），Tauri WebView2 兼容，集成难度最低。
  - 懒加载：`React.lazy()` 导入 ForceGraph2D，避免 vitest 中 `window is not defined` 崩溃（force-graph 在模块初始化时访问 window）。
  - 节点分组：从 frontmatter entity 字段提取，`groupColor()` 哈希着色。
  - 节点点击：调用 `fetchWikiPageDetail` / `setWikiPageDetail` 跳转至 wiki 模块。
  - Resize 响应：`ResizeObserver` 监听容器尺寸，动态更新图谱宽高。
  - 并行开发：后端/前端子代理独立 worktree，文件所有权无交叉。
- 变更文件：
  - 后端：`src-tauri/src/models.rs`（KnowledgeGraphNode/Link/Data）、`db.rs`（list_all_wiki_pages）、`state.rs`（get_knowledge_graph_impl + 单测）、`commands.rs`（get_knowledge_graph 命令）、`main.rs`（命令注册）
  - 前端：`web/package.json`（react-force-graph-2d）、`web/src/types.ts`（graph 类型 + ModuleId）、`web/src/tauri-client.ts`（getKnowledgeGraph）、`web/src/App.tsx`（图谱模块 JSX + groupColor + 状态管理）、`web/src/styles.css`（.graph-module 样式）
- 验证：`cargo test` 102/102，`npm test` 118/118，`typecheck` 零错误，`build` 通过（ForceGraph2D lazy chunk 62KB gzipped）。
- 风险与后续：大型 vault（>1000节点）可能影响渲染性能，可在 `get_knowledge_graph_impl` 加节点数量上限；后续可增加节点过滤/搜索高亮功能。

---

## 2026-04-17 15:30 — P20-4a 完成：Wiki 页面 stale 标记

- 目标：给 Wiki 页面 frontmatter 增加 `stale` 字段，lint 检测并上报，UI 提供标记/取消操作。
- 关键决策：
  - 不引入完整 Claim 模型，仅在 frontmatter 层面操作，改动最小、风险最低。
  - `set_frontmatter_stale_field` 为字符串操作，不依赖 YAML 库，保持与现有 frontmatter 处理方式一致。
  - `stale=false` 时移除 `stale:` 行（不写 `stale: false`），保持 frontmatter 简洁。
  - Lint 检测作为规则型（同步）检查加入 `lint_report()`，不依赖 LLM。
  - 路径安全检查：禁止操作 wiki 目录之外的文件。
  - 并行开发：后端/前端文件所有权无交叉，同时完成。
- 变更文件：
  - `src-tauri/src/models.rs`：`WikiPageFrontmatter` 加 `stale: Option<bool>`
  - `src-tauri/src/state.rs`：`parse_wiki_frontmatter` 解析 stale；新增 `set_frontmatter_stale_field`、`AppState::set_page_stale`；`lint_report` 加 `STALE_PAGE` 规则；+2 单元测试
  - `src-tauri/src/commands.rs`：`mark_page_stale` 命令
  - `src-tauri/src/main.rs`：注册命令
  - `web/src/types.ts`：`WikiPageFrontmatter` 加 `stale?: boolean | null`
  - `web/src/tauri-client.ts`：`markPageStale(pagePath, stale)`
  - `web/src/App.tsx`：stale 横幅 + 标记/取消按钮 + `handleToggleStale`
  - `web/src/styles.css`：`.wiki-stale-banner` + `.wiki-detail__action-btn--stale-active`
- 验证（Windows 本机）：
  - `cargo test`：**101 通过，0 失败**（+2 条新测试）
  - `npm run typecheck`：零错误
  - `npm run test -- --run`：**118 通过，0 失败**
- 风险与后续：
  - `stale` 标记目前为手动操作，后续可在 lint 的 LLM 语义分析阶段自动检测矛盾/陈旧内容并建议标记。
  - 下一步：P21 知识图谱可视化（`react-force-graph-2d`）。

---

## 2026-04-17 15:20 — P20-3 完成：Query RRF 三路融合检索

- 目标：将单路 FTS5 检索升级为三路 RRF 融合，提升多关键词问答的召回覆盖率。
- 关键决策：
  - 三路设计：FTS5 BM25（现有） + 链接扩展（citations 双向一跳） + Citation 热度（被引次数降序）；无需向量库，全部基于现有 SQLite 表。
  - RRF 实现为纯函数（`search.rs`），逻辑借鉴 `external/llm-wiki-main/crates/wiki-core/src/search.rs`，独立可测。
  - 降级安全网：三路全空时自动回退 `search_wiki_matches_with_fts`，保证空 vault 行为不退步。
  - 更新了 3 条已有测试断言（`"fts"` → `"rrf"`）以匹配新策略标签。
  - 前端 `searchStrategyLabels` 新增 `rrf: "RRF 融合检索"`，无其他前端改动。
- 变更文件：
  - `src-tauri/src/search.rs`（新建）：`reciprocal_rank_fusion()` 纯函数 + 3 条单测
  - `src-tauri/src/db.rs`：新增 `query_linked_page_paths()` + `query_citation_popular_paths()`
  - `src-tauri/src/main.rs`：注册 `mod search;`
  - `src-tauri/src/state.rs`：新增 `search_wiki_matches_rrf()`，替换两处调用点，新增 1 条降级测试
  - `web/src/App.tsx`：`searchStrategyLabels` 加 `rrf` 标签
- 验证（Windows 本机）：
  - `cargo test`：**99 通过，0 失败**（+4 条新测试）
  - `npm run test -- --run`：**118 通过，0 失败**
- 风险与后续：
  - 链接扩展路径（路径2）依赖 `citations` 表有数据；新 vault 无 citations 时路径2为空，等同于单路 FTS，无功能退步。
  - P20-4a 下一步：给 Wiki 页面 frontmatter 增加 `stale` 字段 + lint 检测 + UI 提示。

---

## 2026-04-17 14:55 — 计划冻结：P20-3 / P20-4a / P21 路线确认

- 目标：复核 Codex 遗留计划、对标 external/llm-wiki-main 亮点、确定下一轮路线并写入开发计划。
- 关键决策：
  1. **基线复核通过**：`cargo check` 零错误，`cargo test` 95/95，`npm test` 118/118，`npm run build` 通过（2026-04-17，Claude Code 在 Windows 完成复核）。P20-1/P20-2 Codex 代码已通过 Windows 验证，正式收口。
  2. **P20-3 确认**：Query 多路召回 + RRF 融合，三路设计：FTS5 BM25 + 实体图邻居 + Citation 频率，纯 Rust 无需向量库，直接借鉴 `external/llm-wiki-main/crates/wiki-core/src/search.rs` 的 RRF 纯函数逻辑。
  3. **P20-4 收窄**：放弃完整 Claim 模型（架构侵入性过高），改为 P20-4a——仅在 frontmatter 增加 `stale` 字段，lint 检测时标记，UI 显示过时提示。P20-4b 暂缓。
  4. **P21 知识图谱可视化**：选定 `react-force-graph-2d`（Canvas、Tauri WebView 兼容最佳、集成难度最低），排在 P20 全部完成后推进。放弃 reagraph（Three.js WebGL 在旧 WebView2 有兼容风险）、vis-network（维护不稳定）、Cytoscape.js（API 非 React 原生）。
- 变更文件：
  - `agents.md`（§18.2 TODO 更新：P20-3/P20-4a 细化、P20-4b 暂缓、P21 加入）
  - `docs/实施过程记录.md`（本条）
- 验证：无代码变更，仅计划文档更新。
- 风险与后续：
  - P20-3 RRF 三路中"实体图邻居"依赖 ingest 时写入的 entities 字段，需确认 `wiki_pages` 表的 entities 数据覆盖率；若覆盖率低，第三路召回效果有限。
  - P21 `react-force-graph-2d` 需新增后端命令 `get_knowledge_graph()`，依赖 `citations` 表数据质量（链接密度）。
  - 下一轮开始前仍需按 §16.3 检查清单执行基线验证。

---

## 2026-04-17 13:25 — P20-2 后端完成：wiki-link 级 lint + 最小可用补丁流

- 目标：在不改前端的前提下，完成 `broken_wikilink / orphan / xref_missing` 三类 wiki-link 级 lint，并接入现有 patch preview/apply 流程。
- 关键决策：
  - 仅改后端文件所有权范围：`src-tauri/src/state.rs`；`models.rs/db.rs` 本轮不改。
  - `orphan` 从“仅 index 未引用”升级为“index 未引用且无其他页面入链”，减少误报。
  - `broken_wikilink` 补丁策略采用最小可用自动修复：将失效 `[[...]]` 降级为纯文本，避免继续指向不存在页面。
  - `xref_missing` 补丁策略：对来源页已引用目标页补齐 `See Also` 反向链接（幂等，复用 `append_see_also_link`）。
  - 并行说明：本轮按用户指定仅后端单文件所有权实施；通过并行检索/审计推进，不并行写同一文件，避免冲突。
- 变更文件：
  - `src-tauri/src/state.rs`
    - `lint_report` 新增 wiki-link 图分析与三类问题产出：
      - `broken_wikilink`
      - `orphan`
      - `xref_missing`
    - `build_lint_patch_suggestion` 新增三类问题补丁建议
    - `apply_lint_patch` 新增三类问题自动应用分支（兼容 `ORPHAN_WIKI_PAGE` 历史 code）
    - 新增辅助函数：
      - `collect_wiki_link_graph`
      - `collect_xref_missing_sources`
      - `rewrite_broken_wiki_links_in_page`
      - `rewrite_broken_wiki_links`
      - `apply_missing_xref_backlinks`
    - 新增最小单元测试：
      - `lint_report_detects_wikilink_level_broken_orphan_and_xref_missing`
      - `apply_lint_patch_supports_broken_wikilink_and_xref_missing`
- 自动化验证：
  - 本端 WSL：`cargo check` 无法执行（`cargo: command not found`）。
  - 其余前端验证本轮未触及（按用户要求未改前端）。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo test lint_report_detects_wikilink_level_broken_orphan_and_xref_missing`
  - `cargo test apply_lint_patch_supports_broken_wikilink_and_xref_missing`
  - `Set-Location ..`
  - `npm run tauri:dev`
- 风险与后续：
  - `orphan` 现在使用“无 index 且无入链”定义，若后续希望“仅看 index”可在策略层增加可配置开关。
  - `broken_wikilink` 当前是“降级为纯文本”的保守修复；后续可升级为“候选页面重定向”交互式修复。
  - 下一轮建议进入 `P20-3`（多路召回 + RRF），并保持与 `external/llm-wiki-main` 功能对标。

---

## 2026-04-17 12:55 — P20-1 热修复：修复 `cargo check` 的 `E0063` 配置字段缺失

- 目标：修复 Windows PowerShell 回传的 Rust 编译错误（`AppConfig` / `LlmProviderConfig` 初始化缺失 `ollama_model`、`ollama_base_url`）。
- 关键决策：
  - 按并行模式执行：
    - 主控：修复 `state.rs` 配置读写链路与测试初始化。
    - 子任务 A：修复 `vault.rs` 中 `AppConfig` 初始化缺失字段。
    - 子任务 B：只读审计全量初始化点，避免漏改。
  - 修复不只补编译点，同时把 `ollama_*` 字段贯通到状态持久化链路，避免“能编译但配置丢失”。
- 变更文件：
  - `src-tauri/src/state.rs`
    - `AppStateData` 新增 `ollama_model` / `ollama_base_url`
    - `new()` / `persist_config()` / `get_llm_config()` / `set_llm_config()` 补齐字段读写
    - 测试中的 `LlmProviderConfig` 初始化补齐字段
  - `src-tauri/src/vault.rs`
    - `initialize_vault()` 的 `AppConfig` 初始化补齐 `ollama_model: None` / `ollama_base_url: None`
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过
  - `cargo check/cargo test`：当前 Codex WSL 环境不可执行（`cargo: command not found`），待 Windows PowerShell 复核
- 用户侧 PowerShell 复核命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
- 风险与后续：
  - 当前修复已覆盖已知报错点；是否完全收口以 Windows 侧 `cargo check`/`cargo test` 回传为准。
  - 若仍有新增 `E0063`，继续按“初始化点全量扫描 + 最小补丁”收敛。

---

## 2026-04-17 12:35 — P20-1 完成（后端基础）：Outbox 事件流落地与命令暴露

- 目标：完成 `P20-1` 的后端基础能力，支持事件增量导出与消费确认（ack），并在关键操作写入 outbox。
- 关键决策：
  - 采用独立 `wiki_outbox` 表，不复用 `task_events`，避免“任务状态日志”与“可消费事件流”语义混用。
  - 关键路径事件先覆盖：`init_vault`、`ingest_markdown`、`run_lint`、`query_ask_with_options`、`query_ask_session`、`save_query_answer`、`save_wiki_page`、`rename_wiki_page`、`delete_wiki_page`。
  - 新增 Tauri 命令：`get_outbox_events(last_id, limit)`、`ack_outbox_events(up_to_id, consumer_tag)`。
  - Outbox 写入失败仅告警不阻断主流程（保持现有 ingest/query/save 可用性）。
  - 并行约束说明：本轮受当前执行策略限制（当前会话未显式启用 subagents），采用主控串行收口，并在本记录中明确说明。
- 变更文件：
  - `src-tauri/src/db.rs`（`wiki_outbox` 表、导出/ack API、outbox 单元测试）
  - `src-tauri/src/models.rs`（`OutboxEventItem`、`OutboxAckResult`）
  - `src-tauri/src/state.rs`（关键路径写 outbox、`run_lint_with_outbox`、outbox 读写状态方法）
  - `src-tauri/src/commands.rs`（新增 outbox 命令、`run_lint` 调整为写 outbox 版本）
  - `src-tauri/src/main.rs`（注册 outbox 命令）
  - `AGENTS.md`（`§18` 更新 P20-1 状态）
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过（本轮 Rust 改动未影响前端）
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（118/118）
  - `Set-Location E:\llm-wiki\web; npm run build`：通过
  - `Set-Location E:\llm-wiki; npm --workspace web run dev -- --host 127.0.0.1 --port 4173`：可启动（WSL 需提权端口监听）
  - `Set-Location E:\llm-wiki; npm run tauri:dev`：当前 WSL 失败（`@tauri-apps/cli-linux-x64-gnu` 缺失 + 当前会话无 cargo）
  - `cargo check/cargo test`：当前 Codex WSL 环境不可执行（`cargo: command not found`），**待 Windows PowerShell 复核**
- 用户侧 PowerShell 验证命令（本轮收口必跑）：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 在应用中验证（可用 devtools 或后续前端接线）：
    - 连续执行 ingest/query/save/rename/delete 后调用 `get_outbox_events(last_id=0)`，应返回事件递增列表
    - 调用 `ack_outbox_events(up_to_id, consumer_tag)` 后再次查询，`processed_at/consumer_tag` 应有值
- 风险与后续：
  - 目前 outbox 仅后端能力可用，前端尚未提供可视化消费面板（可在 P20-1 前端补齐）。
  - `run_lint` 改为统一走 `run_lint_with_outbox`，需 Windows cargo 回归确认无借用/生命周期回归。
  - 下一步进入 `P20-2`（wiki-link 级 lint：`broken_wikilink/orphan/xref_missing`）。

---

## 2026-04-17 12:05 — 对标评审完成：external/llm-wiki-main 功能亮点映射与下一轮计划冻结

- 目标：基于 `external/article.txt` 与 `external/llm-wiki-main` 做功能对标，形成可直接执行的下一轮路线，并同步给 Claude Code / Gemini 交接使用。
- 关键决策：
  - 对标优先级采用“功能价值优先、技术实现第二”：先吸收对用户价值直接可见的能力，再决定是否复用其实现细节。
  - 将 `external/llm-wiki-main` 设为**功能对标参考样本**（不是实现绑定）；允许本项目继续使用 Tauri + React + SQLite 架构独立落地。
  - 与当前项目差异聚焦四项能力缺口：
    1. 可消费事件流（outbox offset 导出 + ack）；
    2. wiki-link 级健康检查（broken/orphan/xref）；
    3. Query 多路召回融合（RRF，先不强依赖向量库）；
    4. Claim 生命周期最小模型（supersede/stale，可追溯）。
  - 进入下一轮开发前执行 §10 调研闸门：先给出 MCP/skills/workflow 候选与取舍，待用户确认后安装/实施。
  - 执行方式沿用 §14 并行规则：每轮至少拆成前后端子任务，禁止多 agent 同文件并行写入；若必须串行，需在记录中说明原因和影响。
- 变更文件：
  - `AGENTS.md`（更新 §18.2 TODO 与 Claude Code 入手点，加入 P20 对标计划与 external 参考说明）
  - `docs/实施过程记录.md`（新增本条记录）
- 自动化验证：
  - 本轮为计划冻结与文档更新，不涉及代码逻辑变更，未执行构建/测试。
  - 按交接规范，后续编码轮次仍需先由 Windows 侧执行：`cargo check`、`cargo test`、`npm run typecheck`、`npm run test -- --run`、`npm run build`。
- 用户侧 PowerShell 验证命令（供下一轮开始前基线复核）：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
- 风险与后续：
  - `outbox` 与现有 `tasks/task_events` 语义边界需先定义清楚，避免重复记账。
  - RRF 第一期若只用 FTS + 链接 + 实体，会有召回上限；后续再评估向量路由。
  - Claim 生命周期上线会引入新数据模型，需分阶段灰度，避免一次性改动过大。

---

## 2026-04-17 11:10 — P19-1 完成：Ask 历史管理增强（时间显示/关键词过滤/清空入口）

- 目标：在 P18 Ask 流式对话完成后，补齐 Ask 历史可管理能力，支持“可读时间 + 快速过滤 + 一键清空”。
- 关键决策：
  - 历史数据从“字符串列表”升级为 `AskHistoryItem[]`（`id/question/created_at`），但保留旧 `localStorage` 字符串数组兼容读取。
  - 前端历史存储改为对象数组写入，同步新增筛选函数与紧凑时间格式化函数（`MM-DD HH:mm`）。
  - 后端新增 `clear_ask_history` 命令，清空动作优先落库，不再仅清前端状态。
  - 三方协作收口约定继续沿用：Claude Code / Codex / Gemini 交接以 `agents.md §18 + docs/实施过程记录.md` 为准。
  - 本轮属于收口型强耦合改动（同一轮同时调整 `App.tsx` 状态结构与 Rust 命令契约），按 §14 采用短时串行实施；影响范围限定在 Ask 历史链路。
- 变更文件：
  - `src-tauri/src/db.rs`（新增 `clear_ask_history` + db 测试）
  - `src-tauri/src/state.rs`（新增 `clear_ask_history_impl`）
  - `src-tauri/src/commands.rs`（新增 `clear_ask_history` Tauri 命令）
  - `src-tauri/src/main.rs`（注册 `clear_ask_history` 命令）
  - `web/src/tauri-client.ts`（新增 `clearAskHistory`）
  - `web/src/App.tsx`（Ask 历史对象化、筛选、清空交互、时间展示）
  - `web/src/styles.css`（Ask 历史筛选栏/清空按钮/chip 时间样式）
  - `web/src/app-utils.test.ts`（历史对象化与兼容逻辑测试）
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（118/118）
  - `Set-Location E:\llm-wiki\web; npm run build`：通过
  - **环境说明**：本轮由 Codex 在 WSL/Linux 执行（路径 `/mnt/e/llm-wiki`），`cargo` 在当前会话不可用（`cargo: command not found`）；Rust 侧编译/测试需由 Windows PowerShell（Claude Code / Gemini）复核。
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..`
  - `npm run tauri:dev`
  - 在 Ask 空态中验证：输入筛选关键词可过滤历史；点击“清空历史”后历史列表清空；新提问后历史重新出现并带时间。
- 风险与后续：
  - 当前“清空历史”仅清 Ask 问题，不影响会话中已显示消息（会话清空仍由“新对话”负责）。
  - 若后续需要“按条删除/收藏/固定历史”，建议扩展后端为软删除字段而非物理删除。

---

## 2026-04-17 09:55 — P18-UI 完成：Ask 面板 Chat-first UI 全面重构

- 目标：将 Ask 面板从"表单 + 结果列表"升级为现代 Chat-first 布局，输入框固定底部，消息区独立滚动。
- 关键决策（方案 A + B/C 融合）：
  - 方案 A 主体：`.module-viewport--ask` 覆盖 padding/overflow，`ask-layout` flex column 充满高度，`ask-messages` flex:1 滚动，`ask-input-area` 底部固定。
  - 方案 C 融合：Citations 折叠 toggle（`📎 N 个引用 ▸`）挂在 assistant 消息气泡下方，点击展开。
  - 方案 B 融合：元信息 pills（模式/策略/命中页数）作为 assistant 消息完成后的 meta 行显示。
  - "保存到 Wiki"按钮迁移到最后一条 assistant 消息下方，不再独立 panel。
  - TopK 等高级设置折叠在 `⚙ 高级` toggle 后，减少界面噪音。
  - Enter 发送 / Shift+Enter 换行；自动滚动到最新消息（`messagesEndRef`）。
  - 停止按钮（`⏹ 停止`）替换发送按钮，streaming 期间突出红色边框。
  - 历史 chip 迁移到空状态区"最近提问"，有消息后不显示。
- 变更文件：
  - `web/src/App.tsx`：AskMessage 扩展 citations+meta；新增 showAskAdvanced/expandedCitationIds/messagesEndRef；auto-scroll effect；handleQueryAsk 附加 citations+meta；完整替换 Ask JSX；module-viewport 条件 class
  - `web/src/styles.css`：module-viewport--ask；全量 Ask Chat-first CSS
- 自动化验证：
  - `npx vitest run`：**112 通过**
  - `tsc --noEmit`：零错误
- 手动验证步骤：
  - `npm run tauri:dev`
  - 进入 Ask 模块，确认底部输入框固定，消息区可滚动
  - 提问，观察流式气泡从左侧输出，发送按钮变为"⏹ 停止"
  - 回答完成后点 `📎 N 个引用` 展开引用卡片
  - 点 `⚙ 高级` 展开 TopK 设置
  - 点顶部"↺ 新对话"清空对话

---

## 2026-04-17 09:35 — P18-3 完成：真流式多轮会话（新对话 + 软取消）

- 目标：在 P18-2 真流式基础上新增多轮会话历史上下文、取消、以及"新对话"入口。
- 关键决策：
  - 会话历史在后端 `AppState` 中 in-memory 存储（`HashMap<session_id, Vec<AskTurn>>`），不持久化到 DB（减少复杂度）。
  - 软取消：`Arc<AtomicBool>` per session；chunk forwarder 检查 flag，取消时静默丢弃 chunk 并返回 `Err("查询已取消")`。
  - 历史 context：最多保留最近 6 轮（3 问 + 3 答），注入 `build_query_prompt_with_history`，不改现有 `build_query_prompt`。
  - 旧 `query_ask_with_options` 保持不变，向后兼容；前端 Ask 面板改为调用 `query_ask_session`。
  - Frontend session ID 用 `crypto.randomUUID()` 初始化，"新对话"按钮重新生成 UUID + 清空消息 + 通知后端 `clear_ask_session`。
  - 并行子代理：后端（models/state/commands/main）+ 前端（types/tauri-client/App/styles）同步实施。
- 变更文件：
  - `src-tauri/src/models.rs`：新增 `AskTurn { role, content }`
  - `src-tauri/src/state.rs`：`ask_sessions` + `ask_cancel_flags` 字段；`query_ask_session` / `cancel_ask_session` / `clear_ask_session` 方法；`build_query_prompt_with_history` 函数；2 个单元测试
  - `src-tauri/src/commands.rs`：3 个新命令
  - `src-tauri/src/main.rs`：注册 3 个新命令
  - `web/src/types.ts`：新增 `AskTurn` 接口
  - `web/src/tauri-client.ts`：新增 `queryAskSession` / `cancelAskSession` / `clearAskSession`
  - `web/src/App.tsx`：`askSessionId` 状态；取消按钮；新对话按钮；`handleQueryAsk` 改用 `queryAskSession`
  - `web/src/styles.css`：`.ask-cancel-btn` / `.ask-new-session-btn` 样式
- 自动化验证：
  - `cargo test`：**91 通过，0 失败**（+2 新测试）
  - `npx vitest run`：**112 通过，0 失败**
  - `npm run typecheck`：零错误
- 手动验证步骤（Windows PowerShell）：
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 在 Ask 面板提问 2 次，确认第 2 次问答带有第 1 次的历史上下文。
  - 点击"取消"按钮中断进行中的查询，确认状态恢复正常。
  - 点击"新对话"清空消息，确认 sessionId 已重置（下次问答不带旧历史）。
- 风险与后续：
  - 软取消不中断底层 HTTP 流，LLM 仍会继续生成直到完成；仅前端停止更新 UI。
  - in-memory 会话在应用重启后丢失——如需持久化可后续扩展到 DB。
  - P19 候选方向：Ask 历史管理增强、多标签交集筛选、Wiki 文件树增强。

---

## 2026-04-17 09:27 — P18-2 Windows 验证通过：Provider 级真流式单轮（Gemini 实施）

- 目标：验证 Gemini 提交的 P18-2 真流式实现在 Windows 环境下编译并通过测试。
- 关键决策（Gemini 实施）：
  - `LlmProvider` trait 新增 `complete_stream(&self, prompt, on_chunk)` 接口，提供默认回退实现（调用 `complete` 后一次性回调）。
  - `OllamaProvider.complete_stream` 接入 `/api/generate stream=true`，按行 JSON 增量解析并转发 chunk。
  - `OpenAiProvider.complete_stream` 接入 `/chat/completions stream=true`，按 SSE `data:` 增量解析并转发 chunk。
  - `state.rs` `generate_query_answer_with_provider` 改为直接消费 `complete_stream`，前端事件语义（`answer_chunk`）与阶段1兼容。
  - `llm_provider` 字段类型从 `OnceLock<Option<Arc<OllamaProvider>>>` 升级为 `OnceLock<Arc<dyn LlmProvider>>`，支持 Mock 注入与多后端。
  - `LlmProvider` 新增 `base_url()` / `model()` 元数据接口方法。
  - `db.rs` 新增 2 个 `ask_history` 测试（去重 + 容量裁剪）。
- 变更文件（Gemini 提交 16dacc2）：
  - `src-tauri/src/llm/provider.rs`
  - `src-tauri/src/llm/ollama.rs`
  - `src-tauri/src/llm/openai.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/Cargo.toml`
  - `web/src/App.tsx`（minor fix）
  - `docs/实施过程记录.md`
- 自动化验证（Windows，Claude Code 2026-04-17）：
  - `cargo test`：**89 通过，0 失败**
  - `npx vitest run`：**112 通过，0 失败**
- 风险与后续：
  - P18-3（真流式多轮会话）或 P19 候选方向待用户确认后推进。
  - 三方协作（Claude Code / Codex / Gemini）模式已稳定运行，以 agents.md §18 + 本文件为交接来源。

---

## 2026-04-15 17:50 — P9-B 完成：OCR Provider 选择（Tesseract/Paddle）+ 双向回退
- 目标：在保持本地优先的前提下，支持 OCR Provider 选择，并在单一 Provider 失败时自动回退到另一 Provider。
- 关键决策：
  - 采用 subagents 并行：后端实现 `ocr_provider` 路由与回退执行序，前端新增通用文件摄入的 OCR Provider 下拉并透传参数。
  - 默认策略为 `tesseract`，图片 OCR 失败后自动尝试 `paddle`；若用户显式选 `paddle`，则按 `paddle -> tesseract` 回退。
  - 保持非图片格式（md/pdf/docx/pptx/txt）行为不变，仅图片路径使用 OCR provider 逻辑。
- 变更文件：
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/state.rs`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（95/95）
  - `Set-Location E:\llm-wiki\web; npm run build`：通过
  - `Set-Location E:\llm-wiki\src-tauri; cargo test`：通过（82/82）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo test`
  - `cargo tauri dev`
  - 在 Inbox 的“通用文件摄入”选择 OCR Provider 为 `tesseract` 或 `paddle`，对同一图片路径分别执行摄入，验证失败时回退链路可用。
- 给 Claude Code 的收口（本轮重点）：
  - 前端提交：`e4ee1a9`（OCR Provider 下拉 + 参数透传）
  - 后端提交：`dfd3aac`（`ingest_file` 支持 `ocr_provider` + 双向回退）
  - 仍未提交的样本文件：`test.pdf/test1.docx/test2.pptx/test3.png`（用户要求保留工作区样本）
- 风险与后续：
  - `paddleocr` 命令行在不同安装形态（pip/conda/venv）参数细节可能不同，后续可增加“命令探测与自定义命令路径”。
  - 当前仅做 OCR 结果文本直提取，后续可加版面结构保留（段落、表格、标题层级）。

## 2026-04-15 17:20 — P9 第一阶段完成：统一文件摄入（md/pdf/docx/pptx/txt/图片）
- 目标：建立统一“文件摄入”入口，减少按格式分别操作；先打通 Word/PPT/图片最小可用链路。
- 关键决策：
  - 前后端采用 subagents 并行：后端新增 `ingest_file` 扩展名路由，前端新增“通用文件摄入”入口并复用现有进度反馈。
  - `docx/pptx` 先走轻量文本提取（Zip + OpenXML 文本节点），优先交付可用链路，再迭代精度。
  - 图片 OCR 采用本地 `tesseract` 命令；未安装时给中文可读错误，不静默失败。
- 变更文件：
  - `src-tauri/Cargo.toml`
  - `src-tauri/Cargo.lock`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（95/95）
  - `Set-Location E:\llm-wiki\web; npm run build`：通过
  - `Set-Location E:\llm-wiki\src-tauri; cargo check`：通过（2 个既有 dead_code 警告）
  - `Set-Location E:\llm-wiki\src-tauri; cargo test`：通过（80/80）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - 在 Inbox 的“通用文件摄入”依次验证：`.md/.pdf/.docx/.pptx/.txt/.png(or .jpg)` 路径可触发处理（图片依赖 tesseract）。
- 风险与后续：
  - `docx/pptx` 当前为轻量提取策略，复杂版式文本顺序可能不稳定；后续需增强语义分段与表格支持。
  - 图片 OCR 依赖本机 Tesseract 与语言包；下一轮可补“安装检测 + 一键指引”。
  - 工作区存在 `test.pdf`（用户要求保留但不提交）。

## 2026-04-15 16:45 — P8 修复：PDF ToUnicode CMap 失败回退 + 前端错误友好化
- 目标：修复部分 PDF 在 `ToUnicode CMap` 解析失败时无法摄入的问题，并避免前端直接展示底层报错长串。
- 关键决策：
  - 后端 `extract_text_from_pdf` 采用“双通道提取”：优先标准提取，失败或空文本时回退到页面操作符解析（`Tj/TJ/'/"`）。
  - 仅在主路径与回退路径均失败时返回错误，并改为可读中文错误，不直出底层细节。
  - 前端新增错误映射函数，对 CMap/扫描件/非 PDF 等典型错误给出可操作提示，同时保留短片段原因用于定位。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（93/93）
  - `Set-Location E:\llm-wiki\src-tauri; cargo test`：通过（76/76）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run test -- --run`
  - `Set-Location ..\src-tauri`
  - `cargo test`
  - `cargo tauri dev`
  - 在 Inbox 输入同一 PDF 路径重试“PDF 摄入”，确认不再出现原始 `ToUnicode CMap` 长报错；若仍失败，应显示中文可读提示。
- 风险与后续：
  - 回退解析对复杂字体编码 PDF 仍可能提取不全；后续可补 OCR 路径作为最终兜底。
  - 工作区存在 `test.pdf`（用户指定保留但不提交）。

## 2026-04-15 16:15 — P8 完成：PDF 摄入入口 + 编辑器未保存保护 + PDF 回归测试
- 目标：补齐 PDF 摄入前端入口，完善 Wiki 编辑器离开保护，并增强 PDF 摄入错误可读性与回归覆盖。
- 关键决策：
  - 采用 subagents 并行：前端负责 UI/交互与 tauri-client，后端负责 PDF 错误处理与测试增强。
  - 前端新增 `PDF 摄入` 输入框与按钮，复用现有 ingest 进度订阅和刷新链路，避免新增独立状态机。
  - Wiki 编辑器在“关闭预览/切换页面”时增加未保存确认，降低误操作丢改动风险。
  - 后端将“伪 PDF”报错升级为可理解文案（提示内容可能不是有效 PDF），并补充 `.PDF` 扩展名兼容测试。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/app-utils.test.ts`
  - `src-tauri/src/state.rs`
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（90/90）
  - `Set-Location E:\llm-wiki\web; npm run build`：通过
  - `Set-Location E:\llm-wiki\src-tauri; cargo check`：通过（2 个既有 dead_code 警告）
  - `Set-Location E:\llm-wiki\src-tauri; cargo test`：通过（75/75）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - 在 Inbox 执行 PDF 摄入：输入本地 `.pdf` 路径后点击“PDF 摄入”；在 Wiki 预览执行“编辑内容”后尝试切换页面，确认出现未保存提示。
- 风险与后续：
  - 当前输入格式仍以 Markdown/URL/PDF 为主；Word/PPT/图片等多格式摄入尚未设计 Provider 与解析链路。
  - 未保存提示使用浏览器确认弹窗，后续可替换为应用内统一对话框组件。

## 2026-04-15 16:08 — P7 并行完成：Wiki 内联编辑器 + PDF 摄入后端
- 目标：按 P7 规划并行完成前端 Wiki 内联编辑与后端 PDF 摄入能力，形成可交接给 Claude Code 的稳定基线。
- 关键决策：
  - 本轮采用 subagents 并行实施：前端子任务与后端子任务分配独立文件所有权，避免冲突改写。
  - 后端 PDF 摄入采用 `lopdf` 提取文本后复用 `ingest_markdown` 主流程，保持 IngestResult/日志/索引行为一致。
  - 前端 Wiki 详情预览区新增编辑态（编辑/保存/取消），仅在 Tauri Runtime 启用保存；浏览器预览保持只读并给出提示。
- 变更文件：
  - `src-tauri/Cargo.toml`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/types.ts`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
- 自动化验证：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`：通过
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`：通过（87/87）
  - `Set-Location E:\llm-wiki\web; npm run build`：通过
  - `Set-Location E:\llm-wiki\src-tauri; cargo check`：通过（2 个既有 dead_code 警告）
  - `Set-Location E:\llm-wiki\src-tauri; cargo test`：通过（73/73）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - 在 Wiki 页面执行：`查看内容 -> 编辑内容 -> 修改文本 -> 保存`，确认页面内容更新且无报错。
- 风险与后续：
  - 当前 PDF 摄入命令已就绪，但前端尚未提供独立的“PDF 摄入”按钮入口；下一轮建议补齐 Inbox 侧显式入口与手动回归脚本。
  - `SaveWikiPageInput`/`IngestUrlInput` 在模型层仍为未实例化结构体警告（不影响运行），可在后续清理。

## 2026-04-13 15:00 — P4-B 收口：摘要按行折叠 + 调试面板独立化
- 目标：完成 P4-B 收尾；摘要折叠由字符数改为行数；调试信息面板从 frontmatter 头部解耦。
- 关键决策：
  - 摘要折叠阈值从 `140 字符` 改为 `3 行`（`wikiSummaryPreviewLines = 3`）。按行折叠更符合多语言文本的阅读习惯；字符截断在中英混排时边界不直觉。
  - 调试面板独立：移除 frontmatter 头部的"调试信息"按钮，改为 wiki 内容区下方的独立可折叠区块（`wiki-preview__debug-section` + `wiki-preview__debug-toggle`）。frontmatter 区块现在只有"展开/折叠"一个操作，职责更清晰。
  - 新增 CSS 类 `.wiki-preview__debug-section` / `.wiki-preview__debug-toggle`，视觉上与 frontmatter 分离。
- 变更文件：
  - `web/src/App.tsx`（摘要逻辑、debug 面板位置）
  - `web/src/styles.css`（新增 debug-section/toggle CSS）
  - `web/src/app-utils.test.ts`（更新测试适配行数逻辑）
  - `agents.md`（§18.1 标记 P4-B 完成，§18.2 更新 TODO，§18.4 更新基线日期）
- 验证：
  - `npm run test -- --run`：**77/77 通过**
  - `npm run typecheck`：**零错误**
- 风险与后续：
  - 若 LLM 生成摘要都是单行长文本（无换行），3行阈值永不触发 → 可后续在 ingest 时规范化摘要格式（每句换行）。
  - P5 功能深化（PDF/URL ingest、Query 结果跳转、Lint 聚合折叠）等待下一轮规划。

---

## 2026-04-15 15:00 — P5-A：Lint 分组折叠 + Query 保存后跳转 Wiki

- 目标：提升 Lint 大报告可读性；Query 保存结果后直接进入 Wiki 查看。
- 关键决策：
  - `groupLintIssuesByPath` 导出函数将过滤后问题按 path 分组，null/undefined path 归入"全局"。
  - 分组折叠状态用 `Set<string>` 管理，默认全部展开；每个分组 header 独立切换。
  - Query 保存成功后调用 `setActiveModule("wiki")` + `handleOpenWikiPage(result.wiki_path)`，无需用户额外点击。
  - 路径字段从 lint-issue 详情卡移除（分组 header 已显示），减少重复。
- 变更文件：
  - `web/src/App.tsx`（helper + state + 渲染）
  - `web/src/app-utils.test.ts`（4 个新测试）
  - `web/src/styles.css`（lint-group 系列样式）
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（81/81）
  - `npm run build`：通过
- 风险与后续：
  - P5-B 下一步：前端（Lint 补丁建议 UI 增强）与后端（Ingest PDF/URL 支持）并行。

## 2026-04-15 14:46 — 收口：Wiki 排序偏好记忆 + 搜索框回车触发
- 目标：完成本轮收口并为多 Agent 交接提供稳定基线。
- 关键决策：
  - Wiki 排序偏好接入本地存储：`updated_desc / updated_asc / title_asc`，重启后保留。
  - 搜索框新增回车触发搜索（处理中状态下忽略重复触发；兼容中文输入法组合态）。
  - 提供显式读写函数并补测试，保证后续接手可直接复用。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（77/77）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 验证：
    - 切换排序后重启应用，排序模式仍保持；
    - 在 Wiki 搜索框按 Enter 可触发搜索。
- 风险与后续：
  - 排序偏好目前只在前端持久化，未同步到后端配置；若未来需要跨端一致，可落到 `app-config.json`。

## 2026-04-15 14:41 — 下一轮：Wiki 列表排序切换（最新/最早/标题）
- 目标：增强 Wiki 列表浏览效率，支持按不同维度快速排序。
- 关键决策：
  - 前端新增排序模式：`更新时间（新到旧）`、`更新时间（旧到新）`、`标题（A-Z）`。
  - 排序逻辑在前端本地执行，不改后端接口和返回结构。
  - 列表顶部提示同步显示当前排序模式；排序后仍与详情就地展开逻辑兼容。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（72/72）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 模块切换排序，确认顺序即时变化且“查看内容”行为不受影响。
- 风险与后续：
  - 当前排序模式仅前端内存态，不持久化；如需下次启动记忆，可接入 `localStorage`。

## 2026-04-15 14:39 — 修复摘要展开后“收起摘要”按钮消失
- 目标：修复 Wiki 摘要展开后无法再次收起的问题。
- 关键决策：
  - 摘要按钮显示条件从“当前是否截断”改为“原始摘要是否超过阈值”。
  - 保证长摘要在展开态仍保留“收起摘要”按钮，形成可逆交互。
- 变更文件：
  - `web/src/App.tsx`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（69/69）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 列表对长摘要执行“展开摘要”后，确认仍可点击“收起摘要”。
- 风险与后续：
  - 截断阈值当前为固定字符数，若后续 UI 改为行高截断，需同步调整按钮判定逻辑。

## 2026-04-15 14:36 — 下一轮：Wiki 摘要折叠/展开 + 搜索关键词高亮
- 目标：提升 Wiki 列表长摘要可读性，降低滚动负担，同时提升搜索反馈可视化。
- 关键决策：
  - 每条 Wiki 卡片摘要默认折叠（按字符数截断），支持“展开摘要/收起摘要”切换。
  - 关键词搜索时，在摘要中高亮命中词（中英文均支持，去重并过滤 1 字母英文噪声）。
  - 展开状态按页面路径维护；当列表刷新后自动清理不存在的路径状态，避免脏状态残留。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（69/69）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 页输入关键词并搜索，确认摘要中关键词高亮；长摘要可展开/收起。
- 风险与后续：
  - 当前摘要截断按字符数处理，后续如需更稳定的跨语言观感，可改为按渲染行数折叠（line-clamp + 展开容器）。

## 2026-04-15 12:18 — 下一轮：状态栏成功提示自动收起（错误保留）
- 目标：减少顶部状态栏在成功操作后的视觉占用，避免持续遮挡。
- 关键决策：
  - 新增自动收起策略：成功/完成类提示 4.5 秒后自动清空。
  - 错误、失败、告警与进行中提示保持不自动收起，避免关键信息丢失。
  - 保留用户手动关闭能力（右侧关闭按钮不变）。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（65/65）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 操作一次“查看内容/复制”确认成功提示会自动消失；触发错误提示时不会自动消失。
- 风险与后续：
  - 目前按关键词分类消息类型；若后续状态文案体系扩展，建议改为结构化状态对象（success/error/progress）。

## 2026-04-15 12:16 — `imported_at` 下沉到调试信息（默认隐藏）
- 目标：将 `imported_at` 从主信息流移出，避免干扰日常使用；保留排障可见性。
- 关键决策：
  - 主 Frontmatter 区继续不展示 `imported_at`，仅保留 `title/source/raw/entities`。
  - 新增“调试信息”开关（默认关闭），展开后展示：
    - `imported_at（展示）`：按北京时间、分钟级格式化；
    - `imported_at（原始）`：保留原始值。
  - 调试值解析优先级：`frontmatter.imported_at` > 旧格式元数据 `- Imported at: ...`。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（62/62）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 详情中确认：默认不显示 `imported_at` 卡片；点击“调试信息”后可见 `imported_at` 的展示值与原始值。
- 风险与后续：
  - 若后续调试字段继续增多，建议单独做“开发者面板”容器，避免详情区膨胀。

## 2026-04-15 12:10 — 去除 `imported_at` 展示项 + 全部时间精度统一到分钟
- 目标：
  - 去除 Frontmatter 区 `imported_at` 卡片（保留数据存储，不在 UI 暴露）。
  - 前端所有时间展示统一到分钟级，不显示秒。
- 关键决策：
  - Frontmatter 展示仅保留 `title/source/raw/entities`，复制按钮也仅保留这些字段。
  - `formatLintCheckedAt` 升级为同时支持数值时间戳与 ISO 字符串输入，并统一输出 `YYYY-MM-DD HH:mm 北京时间`。
  - 日志时间展示也统一走 `formatLintCheckedAt`，保持全局一致。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/lint-utils.ts`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（59/59）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 列表与详情区确认所有时间仅显示到分钟，且 Frontmatter 中不再展示 `imported_at`。
- 风险与后续：
  - 当前“分钟精度”仅影响展示层，不影响后端存储精度；如后续需“悬浮显示秒级”，可追加 tooltip 展示原始值。

## 2026-04-15 12:02 — Frontmatter `imported_at` 可读化（北京时间）且复制保留原值
- 目标：提升详情区 `imported_at` 可读性，同时保证复制时不丢失原始时间戳。
- 关键决策：
  - 前端将 `imported_at` 的“显示值”和“复制值”分离：
    - 显示：若为秒/毫秒时间戳，渲染为北京时间；
    - 复制：仍复制原始值（避免精度与格式损失）。
  - 兼容旧格式元数据回退路径，`imported_at` 同样走可读化显示。
  - 不改后端接口，仅改前端展示与测试。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（59/59）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 详情观察 `imported_at` 显示为北京时间；点击复制后粘贴应仍是原始时间戳值。
- 风险与后续：
  - 若后续后端统一改为 ISO 时间格式，可考虑直接展示 ISO + 本地化双列，减少语义歧义。

## 2026-04-15 11:58 — 修复旧格式页面无复制按钮（兼容非 YAML 元数据）
- 目标：修复“预览已展开但没有复制按钮”的问题。
- 根因：
  - 当前复制按钮依赖结构化 frontmatter（YAML）渲染。
  - 部分历史页面使用旧格式元数据（正文中的 `- Source/- Raw/- Imported at` 列表），不含 YAML frontmatter，导致按钮区未显示。
- 关键决策：
  - 在前端新增旧格式元数据解析回退逻辑：当 `frontmatter` 为空时，从内容文本提取 `source/raw/imported_at`，仍渲染 Frontmatter 区与复制按钮。
  - 保持后端接口不变，仅增强前端兼容展示。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（58/58）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 打开一条旧格式页面（含 `- Source/- Raw/- Imported at`），确认详情区出现复制按钮。
- 风险与后续：
  - 旧格式解析目前针对常见字段命名；若未来存在更多变体写法，可继续补充正则与样例测试。

## 2026-04-15 11:53 — 修复 Wiki 详情不展开（Windows `\\?\` 路径前缀）
- 目标：修复“点击查看内容后无变化、看不到复制按钮”的回归问题。
- 根因：
  - 列表卡片路径与详情返回路径在 Windows 下可能出现 `\\?\` 前缀差异（如 `E:\...` vs `\\?\E:\...`）。
  - 就地展开逻辑依赖路径相等判断，导致误判为“不是同一页面”，所以不渲染详情块。
- 关键决策：
  - 在前端路径归一化函数中显式剥离 `\\?\` / `\\?\UNC\` 前缀，再进行统一分隔符与大小写比较。
  - 增加对应单元测试，覆盖规范路径前缀场景，防止回归。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（56/56）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 列表点击“查看内容”，确认当前条目下出现详情与 Frontmatter 复制按钮。
- 风险与后续：
  - 若未来后端返回更多路径形态（URL 编码、相对路径混用），建议继续扩展归一化策略并补测试样例。

## 2026-04-15 11:46 — Wiki 详情改为就地展开（同条下方）
- 目标：修复“点击查看内容后详情出现在列表底部”的交互问题，改为在当前条目下就地展开。
- 关键决策：
  - 详情渲染从列表外全局块改为卡片内联块，避免长列表下的来回滚动。
  - 仅在“当前激活页面不在列表中”时保留兜底详情区（例如从引用跳转到列表外页面）。
  - 新增路径归一化比较函数，统一处理 Windows 路径大小写与斜杠差异，保证“激活条目”判断稳定。
  - 复制按钮仍放在详情里的 Frontmatter 区，配合就地展开即可直接可见。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test`：通过（55/55）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `sl E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
  - `sl ../src-tauri`
  - `cargo tauri dev`
  - 在 Wiki 列表点击“查看内容”，确认详情在该条目下展开且可见“复制”按钮。
- 风险与后续：
  - 当前仍是单详情模式（同一时刻只展开一个条目）；如后续需要多条同时展开，可改为按路径集合管理展开状态。

## 2026-04-15 11:02 — P4-B Frontmatter 展示交互收尾（折叠/复制）
- 目标：提升 Wiki 详情页 Frontmatter 区块可读性与操作效率，补充折叠与复制交互。
- 关键决策：
  - 仅修改前端展示层与交互，不修改后端接口契约。
  - Frontmatter 区新增“折叠/展开”开关，减少详情页视觉负担。
  - `title/source/raw/imported_at` 与 `entities` 增加“复制”按钮，统一复制格式为 `field: value`。
  - 复制失败或环境不支持剪贴板时，通过顶部状态栏给出明确提示。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test -- --run`：通过（52/52）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `cd web; npm run typecheck; npm run test -- --run; npm run build`
  - `cd ../src-tauri; cargo tauri dev`
  - 打开 Wiki 页面详情，验证：
    - Frontmatter 可折叠/展开；
    - 点击复制后状态提示为“已复制 ...”；
    - entities 复制内容为逗号拼接文本。
- 风险与后续：
  - 当前复制逻辑依赖 `navigator.clipboard`；如运行环境禁用系统剪贴板，功能会降级为提示信息。

## 2026-04-15 10:56 — Wiki 列表时间显示改为北京时间
- 目标：修复 Wiki 页面列表与详情时间展示为 UTC 的可读性问题，统一改为北京时间（UTC+8）。
- 关键决策：
  - 仅改前端展示层，不改后端时间存储与接口字段。
  - `formatLintCheckedAt` 从 `toISOString + UTC` 改为 `Intl.DateTimeFormat(timeZone=Asia/Shanghai)`，输出形态为 `YYYY-MM-DD HH:mm:ss 北京时间`。
  - 保持容错：非法时间戳仍回退原始字符串。
- 变更文件：
  - `web/src/lint-utils.ts`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm run typecheck`：通过
  - `npm run test -- --run`：通过（51/51）
  - `npm run build`：通过
- 用户侧 PowerShell 验证命令：
  - `cd web; npm run typecheck; npm run test -- --run; npm run build`
  - `cd ../src-tauri; cargo tauri dev`（打开 Wiki 页面确认时间展示为“北京时间”）
- 风险与后续：
  - 当前仅处理数值时间戳格式化；若后端后续返回 ISO 字符串并要求同样本地化，可在同一函数补充 ISO 解析分支。

## 2026-04-15 10:50 — P4-A Frontmatter 读取与 UI 展示（Claude/Codex 交替协作轮）
- 目标：完成 `get_wiki_page_detail` 的 frontmatter 结构化返回，并在前端 Wiki 详情区展示元数据。
- 关键决策：
  - 后端不新增 YAML 依赖，采用最小解析器仅提取当前已约定字段：`title/source/raw/imported_at/entities`。
  - `WikiPageDetail` 新增可选字段 `frontmatter`，保持兼容：无 frontmatter 页面仍可正常读取。
  - 前端 Wiki 详情新增 `Frontmatter` 区块，展示标量字段与 `entities` 标签，不改变现有后端命令名称与调用方式。
  - 本轮未使用 subagents：当前执行环境此轮未获得用户显式并行子代理授权，按最接近可用方式采用单代理串行实现；影响范围为本轮交付速度，不影响接口与功能正确性。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `web`：
    - `npm run typecheck`：通过
    - `npm run test -- --run`：通过（51/51）
    - `npm run build`：通过
  - `src-tauri`：
    - `cargo check`：当前 WSL 环境缺少 Rust 工具链（`/bin/bash: cargo: command not found`），未能在本机完成后端编译验证。
- 用户侧 PowerShell 手动验证命令：
  - `cd src-tauri; cargo check; cargo test`
  - `cd ../web; npm run typecheck; npm run test -- --run`
  - `cd ..; npm run dev --workspace web`（仅前端预览）
  - `cd src-tauri; cargo tauri dev`（桌面端联调，进入 Wiki 模块打开含 frontmatter 页面）
- 风险与后续：
  - Rust 侧新增解析逻辑未在当前环境完成编译，需要你在 Windows PowerShell 运行 `cargo check/test` 最终确认。
  - 进入下一轮前，建议优先做 P4 收尾：细化 frontmatter 展示交互（如字段折叠/复制）并清理 `AGENTS.md` 中 P4-B 与历史记录的状态不一致。

## 2026-04-13 18:45 — P4-B 前端布局重构
- 目标：将单页滚动布局改造为侧边栏 + 主内容区布局，采用亮色 Indigo/Violet 主题。
- 关键决策：
  - `activeModule` 状态（`ModuleId`）驱动条件渲染，默认 `"inbox"`。
  - 侧边栏固定 220px，含品牌 Logo、5 个导航项、底部 LLM 状态点。
  - 状态反馈从各模块内 `runtime-status` 统一上移为顶部 `status-bar`，点叉号可关闭。
  - 模块划分：概览 (inbox) = 运行模式 + Vault 操作 + 日志；Wiki = 搜索/列表/详情；Ask = 问答；Lint = 检查/补丁；Settings = Provider 配置。
  - 主题令牌重写：`--accent: #4f46e5`（indigo-600）、`--accent-2: #7c3aed`（violet-600），完整光亮色板。
  - 中文排版引号 `"..."` 触发 TypeScript TS1005 错误（TypeScript 将 U+201C 识别为无效字符串分隔符），用 `\u201c` 转义修复。
- 变更文件：
  - `web/src/styles.css`（完整重写，约 750 行）
  - `web/src/App.tsx`（新增 `activeModule` 状态与侧边栏 + 模块条件渲染）
- 验证：
  - `npm run typecheck`：零错误
  - `npm run test -- --run`：49/49 通过
  - `npm run build`：零错误，产出 dist/
  - `cargo check`：零错误
- 风险与后续：
  - P4-A（frontmatter UI 展示）待开发
  - `agents.md §18` 当前状态待下一轮更新

---

## 2026-04-13 17:46
- 目标：推进主线 P3-C，将 ingest 实体持久化到 Wiki 页面 frontmatter。
- 关键决策：
  - 采用 subagents 并行：后端子任务负责流程与写入逻辑，测试子任务负责 frontmatter 回归测试。
  - `state::ingest_markdown` 调整顺序为：摘要 -> 实体提取 -> 写入 wiki（携带 entities）-> 双向链接注入。
  - `vault::ingest_markdown` 新增 `entities` 入参；`build_wiki_summary` 增加 YAML frontmatter 头，至少包含：
    - `title`
    - `source`
    - `raw`
    - `imported_at`
    - `entities`
  - entities 为空时输出 `entities: []`，保证 frontmatter 合法且稳定可解析。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：
  - 新增测试：
    - `ingest_generated_page_contains_frontmatter_with_entities_field`
    - `ingest_generated_page_keeps_valid_frontmatter_when_entities_empty`
  - WSL 无 Rust 工具链，需在 Windows PowerShell 侧执行 `cargo test` / `cargo check` 复核。
- 风险与后续：
  - 当前 frontmatter 仅写入，尚未在 UI 结构化展示；下一步可做 frontmatter 读取与展示（见 `agents.md` 新 TODO）。

---

## 2026-04-13 17:33
- 目标：按方案2重构运行模式交互，并完成云端/本地连通性验证。
- 关键决策：
  - 运行模式区移除 `Hybrid/Strict Local` 双卡片，改为单一“运行策略选择器”（select）。
  - 策略项保留两种：`Hybrid（自由模式）` 与 `Strict Local（仅本地）`，切换逻辑继续复用 `handleModeSelect -> setBackendMode`。
  - 仅调整前端展示方式，不改后端模式语义；切换时显示“切换中...”，避免用户误判无响应。
  - 连通性测试同时覆盖云端与本地：
    - 云端 DeepSeek：`https://api.deepseek.com/v1/models` 返回 `HTTP 200`。
    - 本地 Ollama：WSL 到 `172.25.64.1:11434/api/tags` 返回 `HTTP 200`（`127.0.0.1`/`localhost` 在 WSL 下不可达属于预期）。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck`：通过。
  - `npm --workspace web run test -- --run`：通过（49/49）。
  - `npm --workspace web run build`：通过。
  - 网络连通性探测：cloud/local 均可达（见上）。
- 风险与后续：
  - Windows 侧最终体验仍以 `cargo tauri dev` 实机为准，建议你继续在目标分辨率下快速回归一轮。

---

## 2026-04-13 16:36
- 目标：修复点击 Hybrid 后窗口假死（鼠标转圈、界面灰化）。
- 关键决策：
  - 根因定位为 `set_mode` 锁重入死锁：函数内持有 `inner` 锁时调用 `persist_config`，而后者会再次加锁。
  - 修复方案：`set_mode` 先读取快照并释放锁，再调用 `persist_config`；成功/失败分支再分别短时加锁写回状态与日志。
  - 同轮完成 Settings 布局回归修复，采用 `settings-panel` 局部布局类覆盖，避免通用 `dev-panel` 三列网格挤压。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 验证：
  - 前端：`npm --workspace web run typecheck`、`npm --workspace web run test -- --run`、`npm --workspace web run build` 均通过。
  - 需在 Windows `cargo tauri dev` 实机验证点击 Hybrid/StrictLocal 不再卡死。
- 风险与后续：
  - 建议后续为 `set_mode` 增加并发/锁行为回归测试（可通过线程+超时机制做防死锁护栏）。

---

## 2026-04-13 16:28
- 目标：修复 Settings 面板布局重叠（预设按钮、字段、保存按钮挤压/叠层）。
- 关键决策：
  - 采用 subagent 前端专修，限制改动范围在 `App.tsx + styles.css`，避免影响其他模块。
  - 为 Settings 引入局部布局类 `settings-panel`，不改通用 `dev-panel` 规则：
    - 状态提示区、预设按钮区、字段区、保存区拆成独立分区。
    - 字段区单独 grid（桌面双列、`<=980px` 单列）。
    - 预设按钮桌面保持同排并支持横向滚动；移动端可换行，避免压缩成竖排。
  - 仅做局部覆盖，开发操作/Lint/Ask 等面板继续使用原通用布局。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck`：通过。
  - `npm --workspace web run test -- --run`：通过（49/49）。
  - `npm --workspace web run build`：通过。
- 风险与后续：
  - 需在 Windows 下 `cargo tauri dev` 实机查看 UI，确认与你当前窗口尺寸下无重叠。

---

## 2026-04-13 16:20
- 目标：移除 Rust dead_code 警告，并实现 Hybrid 模式下云/本地 Provider 可手动选择（不再强制“有 key 就走云端”）。
- 关键决策：
  - 采用 subagents 并行：
    - 后端子任务：`active_provider` 路由生效、StrictLocal 强制本地保持不变、`openai.rs` 未使用接口清理并补测试。
    - 前端子任务：Settings 增加“活跃 Provider”选择（cloud/ollama），保存时按用户选择写入 `active_provider`。
  - 前端与后端同时加入安全回退：若选择 cloud 且 API Key 为空，自动回退为 ollama，避免无效配置。
  - 保持 OpenAI-compatible 能力与 DeepSeek/GLM/MiniMax 预设并存。
  - 额外执行了真实连通性探测：读取本地运行时配置后请求 `https://api.deepseek.com/v1/models`，返回 `HTTP 200`（未输出完整 key）。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/llm/openai.rs`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck`：通过。
  - `npm --workspace web run test -- --run`：通过（49/49）。
  - `npm --workspace web run build`：通过。
  - DeepSeek 连通性探测：`/models` 返回 `HTTP 200`。
  - WSL 无 Rust 工具链，需在 Windows PowerShell 侧执行 `cargo check` / `cargo test` 最终复核。
- 风险与后续：
  - `cargo check` 中是否仍有编译告警需以 Windows 侧结果为准。
  - 若后续增加更多 Provider，建议将预设与默认端点抽离为统一配置映射表。

---

## 2026-04-13 16:14
- 目标：修复 `AppConfig` 新增字段后的编译错误（`vault.rs` 初始化缺失 `active_provider`）。
- 关键决策：
  - 采用 subagent 独立修复 `vault.rs`，避免与其他并行改动冲突。
  - `initialize_vault` 的 `AppConfig` 初始化统一使用 `cloud_*` + `active_provider` 字段，均设为 `None`（保持默认本地配置语义）。
- 变更文件：
  - `src-tauri/src/vault.rs`
  - `docs/实施过程记录.md`
- 验证：
  - 需在 Windows PowerShell 侧执行 `cargo test` / `cargo check` 复核（WSL 无 Rust 工具链）。
- 风险与后续：
  - 若继续出现结构体初始化缺字段错误，优先检查其他 `AppConfig { ... }` 构造点是否同步更新。

---

## 2026-04-13 16:08
- 目标：修复 Windows 侧 `cargo test/cargo check` 的编译错误（云 Provider 重构后的残留字段与导出问题）。
- 关键决策：
  - 采用 subagents 并行修复两处独立问题，避免同文件冲突：
    - 子任务A：`llm/mod.rs` 补齐 `DEFAULT_OPENAI_BASE_URL` 的 re-export。
    - 子任务B：`vault.rs` 将 `AppConfig` 初始化中的旧字段 `openai_*` 全量替换为 `cloud_*`。
  - 主控复核 `src-tauri/src` 中 `openai_*` 仅保留在 `serde alias` 和兼容性测试中，不再作为结构体字段写入。
- 变更文件：
  - `src-tauri/src/llm/mod.rs`
  - `src-tauri/src/vault.rs`
  - `docs/实施过程记录.md`
- 验证：
  - 已完成静态扫查：`rg -n "openai_api_key|openai_model|openai_base_url|openai_provider_name|DEFAULT_OPENAI_BASE_URL" src-tauri/src -S`。
  - WSL 无 Rust 工具链，需在 Windows PowerShell 侧执行 `cargo test` / `cargo check` 复核。
- 风险与后续：
  - 若仍有编译错误，优先检查是否存在未拉取到的并行改动或本地缓存目标文件残留。

---

## 2026-04-13 15:52
- 目标：Settings 云端 Provider 扩展到 GLM / MiniMax（保持 OpenAI-compatible 自定义能力）。
- 关键决策：
  - 采用 subagents 并行：前端子任务负责预设按钮与测试，后端子任务负责 provider 别名与默认端点。
  - 后端 `normalize_cloud_provider_name` 扩展别名归一：`deepseek`、`glm/zhipu`、`minimax`。
  - 当 `cloud_base_url` 为空时，按 provider 自动补默认端点：
    - DeepSeek：`https://api.deepseek.com/v1`
    - GLM/Zhipu：`https://open.bigmodel.cn/api/paas/v4`
    - MiniMax：`https://api.minimax.chat/v1`
  - 前端 Settings 新增三家预设按钮，仅填充 `provider/base_url/model`，不覆盖 API Key；仍允许手动改成任意 OpenAI-compatible 模型与地址。
  - 主控整合时修复了 `get_llm_config` 仅识别 DeepSeek 的遗留逻辑，统一走通用默认端点推导。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/models.rs`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck`：通过。
  - `npm --workspace web run test -- --run`：通过（46/46）。
  - `npm --workspace web run build`：通过。
  - WSL 环境无 Rust 工具链，`cargo test` / `cargo tauri dev` 需在 Windows PowerShell 侧回归。
- 风险与后续：
  - 需你在 PowerShell 执行 Rust 测试确认后端编译与行为（尤其 `state.rs` 新增 provider 归一化逻辑）。
  - 若后续新增更多云 Provider，建议将预设配置抽离为独立配置表（JSON）减少硬编码。

---

## 2026-04-13 13:00
- 目标：P3-B Cloud Provider——OpenAI 集成，Hybrid 模式支持云端 LLM。
- 关键决策：
  - 新建 `src-tauri/src/llm/openai.rs`，实现 `OpenAiProvider`（Chat Completions API）。
  - `AppConfig` 增加 `openai_api_key` 和 `openai_model` 字段（`#[serde(skip_serializing_if = "Option::is_none")]`），保证向后兼容。
  - `persist_config` 内部从 guard 读取 openai 字段，避免所有现有调用点签名变化。
  - Provider 路由：StrictLocal 禁止云；Hybrid + 非空 API Key → OpenAI，否则回退 Ollama。
  - OpenAI Provider 不使用 OnceLock 缓存（因 API Key 可运行时更改），每次调用按需构造（`Arc<OpenAiProvider>` 轻量）。
  - `llm_status_future` 更新为检查当前活跃 Provider（Ollama 或 OpenAI）。
  - 新增 `get_llm_config` / `set_llm_config` 命令，前端 Settings 面板可读写 API Key 和模型名。
  - API Key 显示为 password input，存本地 `.runtime/app-config.json`，不入仓库。
- 变更文件：
  - `src-tauri/src/llm/openai.rs`（新建）
  - `src-tauri/src/llm/mod.rs`（导出 OpenAiProvider）
  - `src-tauri/src/models.rs`（AppConfig 新增 openai 字段，新增 LlmProviderConfig 类型）
  - `src-tauri/src/state.rs`（AppStateData 新增字段、load_config 更新、serialize_config_full 新增、get_llm_provider 路由、llm_status_future 双 provider、get_llm_config/set_llm_config 方法、测试修复）
  - `src-tauri/src/commands.rs`（新增 get_llm_config / set_llm_config 命令）
  - `src-tauri/src/main.rs`（注册新命令）
  - `src-tauri/src/vault.rs`（AppConfig 构造修补 openai 字段为 None）
  - `web/src/types.ts`（新增 LlmProviderConfig 接口）
  - `web/src/tauri-client.ts`（新增 fetchLlmConfig / saveLlmConfig）
  - `web/src/App.tsx`（Settings 面板：OpenAI API Key 输入 + 模型选择 + 保存按钮）
- 验证：
  - `cargo check`：通过（2 个 dead_code 警告，属已知 openai.rs 辅助方法，不影响功能）。
  - `cargo test`：61/61 通过（较上轮增加 3 个 openai 单元测试）。
  - TypeScript 类型检查：通过，零错误。
  - 前端单元测试：43/43 通过。
- 风险与后续：
  - OpenAI 实际调用需在 `cargo tauri dev` 下配置真实 API Key 手动验证。
  - P3-C（entities frontmatter）尚未开始。

---

## 2026-04-13 12:34
- 目标：P3-A 进度反馈——Ingest 和 Query 长时间 LLM 操作的实时步骤提示。
- 关键决策：
  - 在 `AppState` 中存储 `OnceLock<AppHandle>`，由 `main.rs` setup hook 注入，`emit_progress` 从任意方法中无锁调用。
  - `ProgressPayload { step, message }` 作为事件载荷，统一 `ingest_progress` 和 `query_progress` 两个事件名。
  - `query_ask` / `query_ask_with_options` 升级为 `async fn`，彻底移除 `generate_query_answer_sync`（block_in_place 路径）。
  - 测试：5 个 query 相关测试改为 `#[tokio::test]` + `.await`；`answer_strategy` 断言改为接受 "llm" 或 "rule"（测试机 Ollama 已运行，不能硬断言 "rule"）。
  - 前端：`listenProgress` 封装在 `tauri-client.ts`，`App.tsx` 在操作前订阅、`finally` 中取消订阅。
- 变更文件：
  - `src-tauri/src/models.rs`（新增 `ProgressPayload`）
  - `src-tauri/src/state.rs`（app_handle 字段、set_app_handle、emit_progress、ingest 4步 emit、query async 升级、测试修复）
  - `src-tauri/src/main.rs`（setup hook 注入 AppHandle）
  - `src-tauri/src/commands.rs`（query_ask、query_ask_with_options 改为 async）
  - `web/src/types.ts`（新增 ProgressPayload）
  - `web/src/tauri-client.ts`（新增 listenProgress）
  - `web/src/App.tsx`（ingest + query 加 listenProgress 订阅/取消）
  - `docs/实施过程记录.md`
- 验证：
  - `cargo check`：通过，零警告。
  - `cargo test`：58/58 通过。
  - TypeScript 类型检查：通过。
  - 前端单元测试：43/43 通过。
  - 实际 emit 事件需 `cargo tauri dev` 运行时验证。
- 风险与后续：
  - `useRef` 已导入但未用于 unlisten，当前用局部变量处理，若组件卸载时操作仍在进行需注意内存泄漏（P3 后续可按需用 useRef 做 cleanup）。
  - P3-B（Cloud Provider/OpenAI）和 P3-C（entities frontmatter）尚未开始。

---

## 2026-04-13 12:19
- 目标：P2 语义 Lint——LLM 驱动的矛盾 / 陈旧 / 覆盖度检测。
- 关键决策：
  - `lint_report_full_future`：采用与 `get_llm_status` 相同的 future 提取模式，避免 State 借用跨 await 边界。
  - `run_lint` 命令改为 async，返回 `Result<LintReport, String>`（Tauri 要求异步命令带引用参数时必须用 Result）。
  - LLM prompt 要求结构化管道格式（`CODE|severity|message|path|suggestion`），便于精确解析且对 LLM 输出噪声容忍度高。
  - 最多取 20 页摘要（每条截断 200 字），控制 token；最多返回 10 条语义问题。
  - LLM 不可用时静默返回空列表，规则 Lint 不受影响。
  - 顺序执行（后端→前端），本轮前端无需变更（Tauri invoke 自动解包 `Ok`）。
- 变更文件：
  - `src-tauri/src/state.rs`（新增 `collect_semantic_lint_input`、`run_semantic_lint`、`lint_report_full_future`、`parse_semantic_lint_response`、`merge_lint_with_semantic` + 5 个单元测试）
  - `src-tauri/src/commands.rs`（`run_lint` 改为 async + Result）
  - `docs/实施过程记录.md`
- 验证：
  - `cargo test`：58/58 通过（新增 5 个语义 Lint 解析测试）。
  - 前端单元测试：43/43 通过。
  - Ollama 实际调用需在 Windows 侧 `cargo tauri dev` 中回归。
- 风险与后续：
  - 当前语义分析基于页面摘要（非全文），LLM 可能漏报；后续可按需升级为摘要+关键段落。
  - P3（进度指示/流式反馈、Cloud Provider）尚未开始。

---

## 2026-04-13 12:13
- 目标：P1 Wiki 复利机制——实体提取 + 双向链接注入。
- 关键决策：
  - 采用顺序执行方式（后端→前端），文件所有权不重叠，符合 §14 无冲突原则。
    WSL 环境下子代理并行写同一 worktree 存在 git 合并复杂度，此轮由主控顺序实现并在记录中注明。
  - `extract_entities`：LLM 截取前 2000 字符生成实体列表，失败静默回退空列表，不阻塞主流程。
  - `update_related_pages_with_link`：FTS 搜索最多 5 个相关页，双向追加 See Also 节链接，更新 FTS 索引。
  - `append_see_also_link`（vault.rs）：幂等写入，检查 `link_target` 字符串是否已在内容中，避免重复注入。
  - 删除 3 个 dead_code 方法（`llm_status`、`generate_summary_sync`、`generate_summary_with_provider`）和 `OllamaProvider::with_defaults`，消除所有编译警告。
- 变更文件：
  - `src-tauri/src/models.rs`（IngestResult 新增 `entities`、`updated_pages`）
  - `src-tauri/src/vault.rs`（新增 `append_see_also_link`）
  - `src-tauri/src/state.rs`（新增 `extract_entities`、`update_related_pages_with_link`，更新 `ingest_markdown`，删除 dead_code）
  - `src-tauri/src/llm/ollama.rs`（删除 `with_defaults`）
  - `web/src/types.ts`（IngestResult 新增可选字段）
  - `web/src/App.tsx`（ingest 结果展示实体与更新页数）
  - `docs/实施过程记录.md`
- 验证：
  - `cargo check`：通过，零警告。
  - TypeScript 类型检查：通过。
  - 前端单元测试：43/43 通过。
  - Rust 单元测试和完整 `cargo tauri dev` 需在 Windows PowerShell 侧回归。
- 风险与后续：
  - Ollama 未启动时 `extract_entities` 静默返回空列表，`updated_pages` 为空，功能降级但不报错。
  - P1 当前只做了自动链接，未在新页面 frontmatter 中持久化 `entities` 字段，后续可按需补充。
  - P2 语义 Lint（LLM 矛盾检测）尚未开始。

---

## 2026-04-10 17:55
- 目标：修复 `cargo test` 阶段的 `get_llm_status` 编译错误（Tauri 异步命令签名与生命周期约束）。
- 关键决策：
  - 将 `get_llm_status` 命令返回改为 `Result<LlmStatus, String>`，满足 Tauri 对异步命令返回类型约束。
  - 在命令中先获取 `llm_status_future()` 再 `await`，避免在 `await` 期间持有 `State` 借用导致 `'static` 生命周期错误。
  - 在 `AppState` 内新增 `llm_status_input / llm_status_from_input / llm_status_future`，把异步执行依赖收敛为可安全移动的快照数据。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `docs/实施过程记录.md`
- 验证：
  - 当前 WSL 环境无 Rust 工具链，无法本地执行 `cargo test`。
  - 需在 Windows PowerShell 侧执行 `cargo test` 与 `cargo tauri dev --config .\tauri.conf.json` 复核。
- 风险与后续：
  - 本次修复聚焦命令签名与生命周期约束，不改变 LLM 状态业务语义；后续仍需你在 Windows 侧回归确认。

## 2026-04-10 17:40
- 目标：开工第一步，落地 LLM 状态可观测能力（后端命令 + 前端展示）。
- 关键决策：
  - 子代理A（后端）负责新增 `get_llm_status` 命令与 `LlmStatus` 模型，状态查询基于本地 Ollama health check。
  - 子代理B（前端）负责接入状态读取与 UI 展示，运行模式区新增 LLM 状态块。
  - 子代理C（前端最小修复）负责兼容映射：后端 `healthy/base_url` 对齐到前端 `available/address`，防止误判为不可用。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端测试：`npm --workspace web run test` 通过（13/13）。
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - 前端构建：`npm --workspace web run build` 通过。
  - WSL 当前无 Rust 工具链，Rust 测试需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前 Query 仍以规则检索为主，LLM 状态仅用于可观测与连通性提示；后续再推进 Query/ingest 的 Provider 深化接入。
  - `src-tauri/src/llm/` 仍在迭代中，后续需补齐 `cargo test` 与 `cargo tauri dev` 的 Windows 回归结果。

## 2026-04-10 17:16
- 目标：完成兼容性修正子任务 B，将 MCP/Skills 调研文档中的 Codex 专用安装结果改写为 Codex/Claude Code 双栈对照口径。
- 关键决策：
  - 保留原有安装事实与批次信息，但将“已执行安装结果”改写为双栈示例，避免后续流程被单一工具命令绑定。
  - 明确以当前执行器为准：本次记录采用 Codex 口径展示，同时补充 Claude Code 对照命令，作为兼容性参考而非强制执行命令。
  - 仅修改指定两份文档，不扩散到其他配置、代码或流程文件。
- 变更文件：
  - `docs/开工前MCP与Skills调研.md`
  - `docs/实施过程记录.md`
- 验证：
  - 已复核调研文档的安装结果段落，确认包含 Codex 与 Claude Code 双栈对照说明。
  - 已复核实施记录新增条目，确认字段包含目标、关键决策、变更文件、验证、风险与后续。
  - 本轮为文档修订，不涉及业务代码，未运行额外构建或测试命令。
- 风险与后续：
  - 后续若在 Claude Code 环境复用该调研结果，需沿用同一双栈口径并以实际执行器的 MCP 命令为准。
  - 若后续再补充安装步骤，应继续避免写成单一工具专属命令。

## 2026-04-10 14:35
- 目标：将友好路径字段从“详情页”扩展到 Query/列表/引用全链路，减少前端路径回退分支。
- 关键决策：
  - 子代理A（后端）负责扩展返回模型：`QueryCitation.display_path`、`WikiPageItem.display_path`、`WikiPageCitationItem.cited_page_display_path`，并补 Rust 测试。
  - 子代理B（前端）负责类型对齐与 `resolveDisplayPath` 顺序测试补强，不改变 UI 交互行为。
  - 主控负责合并验收与文档同步，确认路径安全边界逻辑未回归。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/types.ts`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - 前端定向测试：`npm --workspace web run test -- src/app-utils.test.ts` 通过（9/9）。
  - WSL 当前无 Rust 工具链，Rust 测试需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 子代理在 WSL 测试环境补装过 Linux Rollup 可选依赖，导致 `package-lock.json` 有变更；提交前可按实际需要决定是否纳入本轮提交。

## 2026-04-10 14:10
- 目标：修复页面预览路径可读性，避免 UI 显示 Windows `\\?\` verbatim 前缀。
- 关键决策：
  - 子代理A（后端）负责 `WikiPageDetail` 新增 `display_path`，并在后端做友好路径格式化，不改变现有安全边界与读取逻辑。
  - 子代理B（前端）负责统一“显示路径”策略：优先 `display_path`，缺失时回退 `path/page_path/cited_page_path`，保持跳转行为不变。
  - 主控负责接口契约与实施记录同步，确保字段命名与回退策略可回归。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `web/src/tauri-client.ts`
  - `web/src/types.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - 前端单元测试：`npm --workspace web run test` 通过（9/9）。
  - WSL 当前无 Rust 工具链，Rust 测试仍需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前仅 `WikiPageDetail` 后端强制返回 `display_path`；其他类型采用前端回退兼容，后续可按需在后端统一补齐 `display_path` 字段。

## 2026-04-10 13:35
- 目标：完成“页面引用点击跳转预览”增强，并按 subagents 模式执行。
- 关键决策：
  - 子代理A负责后端引用查询与安全校验，返回页面引用明细并标注目标是否存在。
  - 子代理B负责前端交互，在预览区内提供“可点击引用”跳转到被引页面预览。
  - 主控统一完成接口对齐、文档同步与验收，保持引用区和预览区联动但不引入编辑能力。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - Windows 侧需继续执行 `cargo test` 与 `cargo tauri dev --config .\tauri.conf.json` 复核交互。
- 风险与后续：
  - 当前引用跳转仍是预览内联动，后续可按需要增加“打开所在目录”或“复制引用路径”。

## 2026-04-10 13:10
- 目标：完成页面引用查询能力的前后端联调（subagents 并行）。
- 关键决策：
  - 子代理A负责后端：新增 `get_wiki_page_citations` 命令与路径安全校验，补齐 Rust 测试。
  - 子代理B负责前端：在页面预览区接入引用列表展示，显示 `score/excerpt/目标存在性`。
  - 主控补齐接口契约文档，确保命令与类型定义同步。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - 前端 Vitest 在当前 WSL 环境仍受 rollup 可选依赖缺失影响，需以 Windows 结果为准。
  - Rust 测试需在 Windows PowerShell 执行 `cargo test` 复核。
- 风险与后续：
  - 当前引用列表为只读展示，后续可增强“点击引用跳转到对应页面预览”交互。

## 2026-04-10 12:50
- 目标：修复 Windows 环境下 `cargo test` 的路径断言误报。
- 关键决策：
  - 采用 subagent 修复测试，仅调整断言方式，不改业务逻辑。
  - 将 `wiki_page_detail_reads_markdown_content` 从路径字符串字面比较改为语义等价比较（`canonicalize`）。
  - 新增测试辅助函数 `assert_paths_semantically_equal`，兼容 `\\?\` 前缀与 8.3 短路径差异。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `docs/实施过程记录.md`
- 验证：
  - 需在 Windows PowerShell 侧复跑 `cargo test`。
- 风险与后续：
  - 当前修复聚焦路径规范化差异；后续新增路径测试时统一沿用语义比较，避免同类误报。

## 2026-04-10 12:35
- 目标：完成 Wiki 页面正文预览（列表内直接查看）。
- 关键决策：
  - 按 subagents 并行分工执行：
    - 子代理A（后端）：`get_wiki_page_detail` 命令、路径安全限制、Rust 测试。
    - 子代理B（前端）：预览交互、参数映射、样式与 TypeScript 测试更新。
  - 后端仅允许读取当前 `vault/wiki` 下 Markdown，拒绝越界路径。
  - 预览交互保持轻量：查看内容/关闭预览，不在本轮加入编辑能力。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - Rust 单元测试需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前预览是纯文本显示，后续可扩展 Markdown 渲染和“打开文件位置”快捷操作。

## 2026-04-10 12:05
- 目标：将“整个项目实施过程必须使用 subagents”固化为硬性规则。
- 关键决策：
  - 在 `agents.md` 中明确：每轮编码前必须由主控完成任务拆分，并至少拆为前端/后端独立子任务。
  - 增加兜底说明：若环境不具备子代理执行能力，主控不得伪装并行，但不得删除该硬性约束。
- 变更文件：
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：人工校对条款与当前项目协作方式一致。
- 风险与后续：后续每轮继续按 subagents 约束推进，并在记录中注明实际分工情况。

## 2026-04-10 11:45
- 目标：增强 Wiki 页面可用性，支持关键字搜索。
- 关键决策：
  - 后端新增 `search_wiki_pages` 命令，按标题/摘要/路径匹配关键字，空关键字回退最近页面列表。
  - 前端 Wiki 面板新增搜索输入框与“搜索/恢复最近”按钮，结果直接刷新当前页面列表。
  - 搜索上限由后端控制，防止一次返回过多数据影响渲染。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - Rust 搜索相关测试已补充（db/state），需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前是简单字符串匹配，后续可升级为 FTS 权重检索并加入高亮片段。

## 2026-04-10 11:20
- 目标：增加 Wiki 页面列表能力，提升桌面端可观测性与回归效率。
- 关键决策：
  - 后端新增 `get_recent_wiki_pages` 命令，从 `wiki_pages` 表读取最近更新页面。
  - 前端新增“Wiki 页面”面板，展示标题、摘要、路径、更新时间。
  - 保持只读展示，不引入页面编辑行为，避免扩大本轮变更范围。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - Rust 单元测试需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前列表来源于数据库，不直接扫描文件系统；若出现手工新增未入库页面，可通过 lint 提示后再补同步策略。

## 2026-04-10 10:55
- 目标：补齐 Ingest 内容去重，避免重复页面与重复任务污染。
- 关键决策：
  - 以 `content_hash` 为去重主键：若已存在对应 wiki 页面且页面文件仍存在，则直接复用并返回。
  - 重复导入不再新增 tasks/wiki_pages，也不重复写 `index.md`，仅返回“复用既有页面”结果。
  - 保持兼容：若数据库有历史记录但页面文件已丢失，则回退到正常导入流程。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查：`npm --workspace web run typecheck` 通过。
  - Rust 去重测试已补充（db/vault），需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前去重粒度是“全文一致”；后续可扩展到“近似重复”与“标题/段落级去重”。

## 2026-04-10 10:20
- 目标：增强 Query 保存后的可追溯性，并将引用一致性纳入 lint。
- 关键决策：
  - SQLite 新增 `citations` 表，由 Query 保存流程写入引用关系（被保存页面 -> 被引用页面）。
  - Lint 增加 `BROKEN_CITATION` 与 `BROKEN_CITING_PAGE` 规则，用于发现引用断链和失效记录。
  - Lint 执行前调用 schema 校验，兼容旧库自动补齐新表结构。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `src-tauri/src/state.rs`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - 前端类型检查不受影响：`npm --workspace web run typecheck` 通过。
  - Rust 单元测试需在 Windows PowerShell 侧执行 `cargo test` 复核。
- 风险与后续：
  - 当前引用记录仅保存页面路径与摘录，后续可扩展 locator/quote_hash 以支持更细粒度证据追踪。

## 2026-04-09 18:05
- 目标：推进 Query 主线，支持“问答结果保存为 Wiki 页面”。
- 关键决策：
  - 后端新增 `save_query_answer` 命令，统一完成 wiki 页面写入、`index.md` 同步、`log.md` 追加、SQLite/FTS 更新。
  - 前端 Ask 面板新增“保存回答到 Wiki”按钮，复用当前 Query 结果直接保存。
  - 保持严格本地约束：保存流程仅写本地 Vault，不引入任何外部网络调用。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `src-tauri/src/db.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - WSL：`npm --workspace web run typecheck` 可通过。
  - WSL：`npm --workspace web run test` 仍受可选依赖安装差异影响（rollup linux optional package 缺失），需以 Windows 结果为准。
  - Rust 自动化测试需在 Windows PowerShell 执行 `cargo test` 复核。
- 风险与后续：
  - 当前保存策略默认按 `query-时间戳.md` 新建页面，后续可增加“覆盖保存到指定页面”的冲突合并策略。

## 2026-04-09 17:30
- 目标：收口 Query 参数持久化（TopK）并完成前后端联动。
- 关键决策：
  - 后端将 `query_top_k` 纳入 `.runtime/app-config.json`，提供 `get_query_settings/set_query_top_k` 命令。
  - 前端启动时读取 Query 设置，Ask 面板新增“保存参数”按钮，支持持久化默认 TopK。
  - 保持 `query_ask` 与 `query_ask_with_options` 兼容：显式参数优先，否则使用持久化默认值。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - WSL：`npm --workspace web run typecheck` 通过。
  - WSL：`npm --workspace web run test` 受可选依赖安装差异影响失败（`@rollup/rollup-linux-x64-gnu` 缺失），需以 Windows PowerShell 结果为准。
  - Rust 侧需在 Windows PowerShell 执行 `cargo test` 与 `cargo tauri dev --config .\tauri.conf.json` 复核。
- 风险与后续：
  - 当前工作区仍存在 Windows/WSL 混装 `node_modules` 差异；后续建议固定“仅 Windows 安装并运行 npm”的流程。

## 2026-04-08 11:10
- 目标：将 v1 技术设计与 agents 约束改为中文，并固化记录机制。
- 关键决策：保留原语义不变，仅做中文化；新增“中文注释”和“实施记录文件”硬约束。
- 变更文件：
  - `docs/v1-technical-design.md`
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：人工校对文档结构与约束项，确认与 A+C 路线一致。
- 风险与后续：后续代码实现前仍需先完成 MCP/skills 调研与安装确认。

## 2026-04-08 11:28
- 目标：完成开工前 MCP/Skills 联网调研，形成安装前清单。
- 关键决策：
  - v1 采用 MCP 最小集（Filesystem/Git/Time）优先。
  - Obsidian 第三方 MCP 暂缓，不作为 v1 依赖。
  - Skills 先使用当前已安装集合，不做额外批量安装。
- 变更文件：
  - `docs/开工前MCP与Skills调研.md`
  - `docs/实施过程记录.md`
- 验证：对官方文档/仓库进行来源核验并完成候选分级。
- 风险与后续：待用户确认“安装批次 A”后再执行实际安装与连通性验证。

## 2026-04-08 11:36
- 目标：完成 MCP 安装批次 A（filesystem/git/time）并做连通性验证。
- 关键决策：
  - `filesystem` 使用项目本地 Node 安装路径，避免每次 npx 远程拉取。
  - `git` 与 `time` 使用项目内 Python venv 安装，避免系统 Python PEP668 限制。
  - WSL 联网使用 Windows 代理地址 `172.25.64.1:7897`。
- 变更文件：
  - `docs/开工前MCP与Skills调研.md`
  - `docs/实施过程记录.md`
- 环境变更：
  - 新增目录：`/mnt/e/llm-wiki/.tools/mcp-node`
  - 新增目录：`/mnt/e/llm-wiki/.tools/mcp-venv`
  - 已写入 `~/.codex/config.toml` 的 MCP 条目：`filesystem`、`git`、`time`
- 验证：
  - `codex mcp list` 显示 3 个 server 均为 enabled。
  - `codex mcp get filesystem|git|time` 配置正确。
  - 服务进程 3 秒超时测试退出码均为 `124`（表示服务可正常常驻启动）。
- 风险与后续：
  - 官方 servers 多为 reference 实现，后续进入编码阶段仍需做最小权限与输入边界加固。

## 2026-04-08 11:58
- 目标：按 subagents 模式完成第 1 轮代码骨架（web + src-tauri）。
- 关键决策：
  - 采用并行子代理分工：`web` 与 `src-tauri` 分离写入，主控统一集成。
  - 前端先做可运行骨架与最小 Tauri 桥接，不提前接入复杂状态管理。
  - 后端先提供命令桩与模式策略枚举，后续迭代替换真实业务。
- 变更文件：
  - 新增：`package.json`、`.gitignore`、`README.md`
  - 新增：`web/*`（React + TS + Vite 骨架）
  - 新增：`src-tauri/*`（Tauri/Rust 命令骨架）
  - 新增：`docs/v1-接口契约草案.md`
  - 更新：`docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过，生成 `web/dist`。
  - `cargo --version` 与 `rustc --version` 在当前环境不可用，未完成 Rust 编译验证。
- 风险与后续：
  - 需要在具备 Rust 工具链的环境执行 `cargo check` 与 `tauri build`。
  - 命令桩尚未接入 SQLite/FTS/Vault/Provider 路由，属于下一轮工作。

## 2026-04-08 12:09
- 目标：将 subagents 并行实施模式固化为项目执行约束。
- 关键决策：
  - 在 `agents.md` 中新增固定章节，明确主控/子代理职责与并行冲突规则。
  - 约束“每轮需产出验证与未完成项，并在下一轮前经用户确认”。
- 变更文件：
  - `agents.md`
  - `docs/实施过程记录.md`
- 验证：人工校对新增条款与当前实施方式一致。
- 风险与后续：后续每轮按该规则执行并持续记录。

## 2026-04-08 12:26
- 目标：完成第 2 轮“前后端真实联动”最小闭环（模式切换 + 最近日志 + 运行态展示）。
- 关键决策：
  - 继续采用 subagents 并行：前端交互与 Rust 状态持久化分离实现，主控统一验收。
  - `set_mode` 改为“先持久化后更新内存态”，失败时保持原模式并写警告日志。
  - 浏览器预览模式保留优雅降级，避免调用 Tauri API 报错。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/models.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/types.ts`
  - `web/src/styles.css`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - 当前 WSL 环境无 Rust 工具链，未执行 `cargo check`；Windows 端可用 `cargo tauri dev` 验证联动。
- 风险与后续：
  - 配置文件当前落点依赖运行时工作目录（`.runtime/app-config.json`），下一轮可升级为固定应用数据目录。
  - 前端出现 `web/package-lock.json` 与 `web/node_modules`（子代理本地验证产生），后续可统一为根 workspace 安装策略。

## 2026-04-08 12:33
- 目标：新增“单元测试 + 用户手动验证（PowerShell）”的长期执行约束。
- 关键决策：
  - 将测试要求固化进 `agents.md` 与技术设计文档，作为后续轮次强制执行项。
  - 新增独立规范文档，统一自动化测试与手动验证输出格式。
- 变更文件：
  - `agents.md`
  - `docs/v1-technical-design.md`
  - `docs/测试与验证规范.md`
  - `docs/实施过程记录.md`
- 验证：人工校对文档条款，确认与用户新增要求一致。
- 风险与后续：从下一轮开始，对每次功能改动同时补充单元测试与 PowerShell 验证命令。

## 2026-04-08 12:40
- 目标：完成第 3 轮“Vault 初始化 + SQLite schema + Markdown 基础摄入”最小闭环。
- 关键决策：
  - 后端新增 `init_vault` 与 `ingest_markdown` 命令，先覆盖最小可用路径。
  - 前端新增“开发操作”区域，提供 Vault 初始化与示例摄入入口。
  - 继续采用 subagents 并行开发，由主控统一验收。
- 变更文件：
  - `src-tauri/Cargo.toml`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/types.ts`
  - `web/src/styles.css`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - Rust 编译与 Tauri 运行需在 Windows 环境执行用户侧 PowerShell 验证。
- 风险与后续：
  - 当前后端单元测试尚未补齐（已列为强制要求，下一步补齐）。
  - 根目录与 `web` 子目录 lockfile 并存，后续可统一依赖管理策略。

## 2026-04-08 16:30
- 目标：修正运行配置路径并补齐前后端单元测试。
- 关键决策：
  - 将 `app-config.json` 固定到项目根 `.runtime/app-config.json`，避免 `src-tauri/.runtime` 路径歧义。
  - 将 Vault 生成文件中的固定标签改为英文，降低 PowerShell 默认编码查看乱码概率。
  - 前端引入最小 Vitest 测试框架，后端补充 Rust 单元测试。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `src-tauri/src/vault.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/app-formatters.ts`
  - `web/src/app-utils.test.ts`
  - `web/package.json`
  - `web/package-lock.json`
  - `docs/实施过程记录.md`
- 验证：
  - `cd web && npm run test` 通过（5/5）。
  - `cd web && npm run typecheck` 通过。
  - `cd web && npm run build` 通过。
  - Rust 测试需在 Windows 侧执行 `cargo test` 复核。
- 风险与后续：
  - 仍需用户在 Windows 侧完成 Rust 测试与一次手动功能回归，作为本轮最终验收。

## 2026-04-08 16:45
- 目标：完成第 4 轮真实 lint 规则与前端 lint 面板。
- 关键决策：
  - 后端 lint 从示例逻辑升级为真实扫描规则（wiki/index/db/tasks）。
  - 前端新增 lint 面板与 `run_lint` 触发按钮，并保持浏览器预览降级。
  - 按新增规范补齐前端自动化测试；Rust 测试在 Windows 侧执行。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/lint-utils.ts`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `cd web && npm run test` 通过（8/8）。
  - `cd web && npm run typecheck` 通过。
  - `cd web && npm run build` 通过。
  - Rust 测试需在 Windows 侧执行 `cargo test --manifest-path src-tauri/Cargo.toml`。
- 风险与后续：
  - lint 规则当前聚焦结构一致性，后续可补内容级规则（例如重复摘要、坏链接分级修复建议）。

## 2026-04-08 18:08
- 目标：将“开发操作”输入框默认值升级为项目绝对路径，减少每次手动改路径。
- 关键决策：
  - 后端新增 `get_default_paths` 命令统一返回默认路径（`<项目根>/vault`、`<项目根>/README.md`）。
  - 前端首屏加载时并行获取总览与默认路径，在 Tauri 环境下自动填充输入框。
  - 保留浏览器预览模式降级行为，不依赖后端命令。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境无 `cargo`，未执行 Rust 侧 `cargo test` 与 `cargo fmt`，需在 Windows PowerShell 侧复核。
- 风险与后续：
  - 如用户希望“记住上次手改路径”，可在下一轮增加前端本地缓存或后端配置持久化策略。

## 2026-04-08 18:40
- 目标：完成 Query v1 最小闭环（本地规则检索 + Ask 面板联调）。
- 关键决策：
  - Query v1 不接云模型，先用本地规则检索（关键词命中计分）返回证据片段。
  - 后端返回 `query_ask` 结构化结果，前端展示答案、引用、命中页数量与检查时间。
  - 保持浏览器预览降级：非 Tauri 环境不调用后端命令并给出中文提示。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 目前 Query 仍是规则检索，后续可接 SQLite FTS5 与引用置信度评分。

## 2026-04-09 16:08
- 目标：推进 Query 到 FTS5 索引优先检索（保留文件扫描降级）。
- 关键决策：
  - `fts_pages` 作为增强索引能力，初始化失败不阻断主流程。
  - ingest 成功后尝试写入 FTS 索引，失败仅记录任务事件并允许后续降级。
  - `query_ask` 优先走 FTS 候选页，再复用现有规则评分；FTS 异常时自动回退文件扫描并写日志。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/vault.rs`
  - `src-tauri/src/state.rs`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 与运行验证需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 目前 FTS 查询词仍基于轻量 token，后续可增加中文分词策略与权重调优。

## 2026-04-09 16:20
- 目标：推进 Query 中文检索命中率与结果质量（中文分词 + 排序权重 + 去重）。
- 关键决策：
  - 分词改为中英混合：英文 token 保留；连续中文片段生成原片段与 2-gram token。
  - 排序分数改为“正文命中 + 标题命中加权 + 短语命中加权”。
  - FTS 与 scan 两条路径统一去重逻辑，保证结果页面唯一。
  - `PendingTaskRecord` 的 `kind/raw_path` 纳入 lint 信息，清理 dead_code warning 且不改变 lint 语义。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 中文 token 目前是轻量规则，后续可按需要接入更强中文分词词典与停用词策略。

## 2026-04-09 16:36
- 目标：推进 Query 参数化 v1（TopK 可配置 + 停用词过滤）。
- 关键决策：
  - 后端新增 `query_ask_with_options(question, options)`，保留 `query_ask` 向后兼容。
  - `top_k` 在后端统一归一化为 `1..=8`，默认值为 `3`，避免前端传参越界导致行为不稳定。
  - 分词阶段增加中英停用词过滤，降低中文问句与英文虚词对相关度的噪声影响。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（8/8）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - 当前 WSL 环境缺少 `cargo`，Rust `cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 目前停用词表是最小集合，后续可改为可配置词表并引入领域词典。

## 2026-04-10 18:20 - 修复 tokio runtime 问题，启用异步 Ingest
- 目标：解决 "没有可用的 tokio runtime" 导致 LLM 无法调用的问题
- 关键决策：
  - 问题根因：`ingest_markdown` 命令是同步函数，无法在其中调用异步 LLM
  - 解决方案：将 `ingest_markdown` 命令和 `AppState::ingest_markdown` 方法改为 `async fn`
  - 在异步上下文中直接调用 `generate_summary().await`，不再需要 `generate_summary_sync`
  - Tauri 原生支持异步命令，运行时会自动提供 tokio runtime
- 变更文件：
  - `src-tauri/src/commands.rs` (ingest_markdown 改为 async)
  - `src-tauri/src/state.rs` (ingest_markdown 改为 async，调用 generate_summary)
  - `src-tauri/src/llm/ollama.rs` (更新测试中的模型名称)
  - `docs/实施过程记录.md`
- 验证：
  - `cargo check` ✓ 通过
  - `cargo test` ✓ 39/39 全部通过
- 风险与后续：
  - 用户需要重新编译并测试 LLM 摄入功能
  - 预期日志应显示 "LLM 摘要生成成功" 而非 "没有可用的 tokio runtime"

## 2026-04-10 18:00 - 修改默认模型为 llama3:8b
- 目标：适配用户本地安装的模型名称
- 关键决策：
  - 用户安装的是 `llama3:8b`，而非 `llama3.2`
  - 修改 `OllamaConfig::default()` 中的默认模型名
- 变更文件：
  - `src-tauri/src/llm/ollama.rs`
  - `docs/实施过程记录.md`
- 验证：
  - `cargo check` ✓ 通过

## 2026-04-10 17:50 - 修复测试：recent_wiki_pages_returns_db_rows
- 目标：修复 Round 2 中遗留的 1 个测试失败
- 关键决策：
  - 问题根因：测试在数据库中插入 wiki 页面路径，但未创建实际文件
  - `assert_paths_semantically_equal` 使用 `fs::canonicalize` 要求文件必须存在
  - 解决方案：在测试中创建实际的 wiki 文件（a.md, b.md）
- 变更文件：
  - `src-tauri/src/state.rs` (测试代码)
  - `docs/实施过程记录.md`
- 验证：
  - `cargo test recent_wiki_pages_returns_db_rows` ✓ 通过
  - `cargo test` ✓ 全部 39/39 通过
- 风险与后续：
  - 无新增风险，测试更加健壮

## 2026-04-10 17:30 - Round 2: LLM Provider 抽象层与 Ollama 集成
- 目标：完成 P0 核心 LLM 集成（Provider trait + Ollama + Ingest LLM 摘要）
- 关键决策：
  - 采用 subagents 并行模式，分 3 个 phase 推进：
    - Phase 1: Provider trait 设计（完成 ✓）
    - Phase 2: Ollama HTTP 客户端实现（完成 ✓）
    - Phase 3: Ingest 流程集成 LLM（完成 ✓）
  - 新增 `src-tauri/src/llm/` 模块，包含 provider.rs 和 ollama.rs
  - state.rs 中使用 `OnceLock` 延迟初始化 LLM Provider
  - 实现 `generate_summary_sync()` 同步包装器，使用 `tokio::task::block_in_place` 调用异步 LLM
  - Ollama 配置默认值：localhost:11434, llama3:8b, 60s 超时
  - 错误处理：LLM 失败时自动回退到 `fallback_summarize()` 截断摘要
- 变更文件：
  - 新增：`src-tauri/src/llm/mod.rs`
  - 新增：`src-tauri/src/llm/provider.rs`
  - 新增：`src-tauri/src/llm/ollama.rs`
  - 修改：`src-tauri/src/main.rs` (添加 mod llm)
  - 修改：`src-tauri/src/state.rs` (LLM provider 管理、摘要生成)
  - 修改：`src-tauri/src/vault.rs` (函数重命名 summarize_text → fallback_summarize)
  - 修改：`src-tauri/Cargo.toml` (新增 async-trait, reqwest, tokio 依赖)
  - 更新：`docs/实施过程记录.md`
- 验证：
  - `cargo check` 通过（6 个 unused 警告，预期行为）
  - `cargo test` ✓ 39/39 全部通过（已修复路径测试）
- 风险与后续：
  - 当前 Ollama 集成未启用健康检查，首次调用时可能超时（60s）
  - 建议在 UI 中添加"测试 Ollama 连接"按钮
  - 后续 Phase 4: 前端进度指示（待用户确认后继续）

## 2026-04-10 继续推进
- 目标：新增页面引用查询命令 `get_wiki_page_citations(page_path)`，用于页面预览侧展示被引用页面列表。
- 关键决策：
  - 复用现有页面路径安全策略，只允许在当前 vault 的 `wiki` 目录内查询页面引用。
  - 返回结构统一补齐 `cited_page_path`、`score`、`excerpt`、`target_exists`，方便前端直接渲染预览区。
  - `target_exists` 仍按本地文件系统与 `vault/wiki` 范围校验，不依赖外部服务。
  - 单元测试覆盖未初始化、正常返回、越界路径拒绝三类场景，避免后续权限回归。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `docs/实施过程记录.md`
- 验证建议：
  - Windows PowerShell 下执行 `cargo test`。
  - 启动 `cargo tauri dev --config .\tauri.conf.json`，在页面预览侧检查引用查询调用是否正常。
- 风险与后续：
  - 前端尚未接入新命令，下一轮需要在预览面板补充引用列表展示与空状态处理。

## 2026-04-10 路径默认值与策略枚举兼容修正
- 目标：按用户要求将示例摄入默认路径固定为 `E:\llm-wiki\test-llm.md`，并修复 Query 结果策略字段前后端枚举不一致。
- 关键决策：
  - `answer_strategy` 后端主枚举保持 `llm | rule`，前端保留对历史值 `llm_synthesis | rule_fallback` 的兼容显示，避免旧数据回放异常。
  - 默认示例摄入路径在前端常量与后端 `default_paths` 同步改为 `E:\llm-wiki\test-llm.md`（Windows）。
  - 前端类型收口：`answer_strategy` 改为联合类型，避免继续使用无约束 `string`。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/types.ts`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - 建议执行：`npm --workspace web run test`
  - 建议执行：`npm --workspace web run typecheck`
  - 建议执行：`npm --workspace web run build`
- 风险与后续：
  - 若后续要彻底收敛历史字段，可在数据迁移阶段移除 `llm_synthesis | rule_fallback` 兼容分支。

## 2026-04-10 Query 检索策略可观测性（subagents 并行）
- 目标：在 Ask 结果中显式展示 `search_strategy`，让用户可直接判断本次 Query 是 FTS 召回还是回退扫描。
- 并行分工：
  - 子代理A（后端）：`QueryAnswerResult` 增加 `search_strategy` 字段并补 Rust 测试。
  - 子代理B（前端）：Ask 面板增加“检索策略”展示，补策略标签映射与前端测试。
  - 主控：契约文档与实施记录同步、统一验收。
- 关键决策：
  - `search_strategy` 使用闭集字符串：`empty | fts | scan`。
  - `answer_strategy` 与 `search_strategy` 继续分离：前者表示回答生成方式，后者表示检索召回路径。
  - 前端保持 `answer_strategy` 历史兼容逻辑不变。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（17/17）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - WSL 当前无 Rust 工具链，`cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 后续可在 Lint 面板增加 severity 数量分组（error/warning/info），便于快速判读报告质量。

## 2026-04-10 编译修复：search_strategy 类型对齐
- 目标：修复 `cargo test` 报错 `expected String, found &str`。
- 修复内容：
  - 在 `query_ask_with_options` 返回 `QueryAnswerResult` 时，将 `search_strategy` 从 `&str` 显式转换为 `String`。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `docs/实施过程记录.md`
- 验证：
  - 需在 Windows PowerShell 执行 `cargo test` 复核。

## 2026-04-10 Lint 严重级别分组统计（subagents 并行）
- 目标：在 Lint 面板提供 `error/warning/info` 数量分组，提升健康度判读效率。
- 并行分工：
  - 子代理A（后端）：`run_lint` 返回 `severity_stats` 并补 Rust 断言。
  - 子代理B（前端）：增加统计解析函数与 Lint 面板展示。
  - 子代理C（前端测试）：补充统计优先级与回退逻辑测试。
  - 主控：合并验收、接口契约同步、实施记录归档。
- 关键决策：
  - 后端结构新增 `severity_stats`（`error/warning/info`），并以 `serde(default)` 保持兼容。
  - 前端优先使用后端 `severity_stats`；字段缺失时回退按 `issues` 现场计算，保证旧数据兼容。
  - 未知 severity 按 `info` 归类，避免统计丢失。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/lint-utils.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/v1-接口契约草案.md`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（20/20）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - WSL 当前无 Rust 工具链，`cargo test` 需在 Windows PowerShell 侧执行。
- 风险与后续：
  - 后续可在 Lint 面板增加按严重级别筛选，提升问题定位效率。

## 2026-04-10 Lint 严重级别筛选（subagents 并行）
- 目标：在 Lint 面板支持按严重级别筛选（全部/错误/警告/信息），提升问题定位效率。
- 并行分工：
  - 子代理A：前端 UI（`App.tsx`）增加筛选状态与交互。
  - 子代理B：工具函数与测试（`lint-utils.ts`、`app-utils.test.ts`）补充筛选与断言。
  - 主控：回归验证与提交准备。
- 关键决策：
  - 筛选值使用闭集：`all | error | warning | info`。
  - 未知 severity 在筛选逻辑中归入 `info`，避免丢失。
  - 统计仍复用 `severity_stats` 优先、`issues` 回退计算策略。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/lint-utils.ts`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（24/24）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
- 风险与后续：
  - 后续可增加“按代码（code）搜索/筛选”能力，进一步缩短定位路径。

## 2026-04-13 Lint code 关键词筛选（subagents 并行）
- 目标：在 Lint 面板新增按 `code` 关键词筛选，并与严重级别筛选叠加，提升问题定位效率。
- 并行分工：
  - 子代理A：`lint-utils.ts` 与 `app-utils.test.ts`（新增 code 筛选函数与测试）。
  - 子代理B：`App.tsx`（新增关键词输入、叠加筛选与空状态文案）。
  - 主控：过滤逻辑收敛为工具函数复用、回归验证、记录归档。
- 关键决策：
  - code 筛选函数 `filterLintIssuesByCode` 支持大小写不敏感 `includes`。
  - 关键词为空时返回全部问题，`code` 缺失按空串处理。
  - 空状态文案区分四类场景：无 lint 问题、仅严重级别无命中、仅 code 无命中、组合条件无命中。
- 变更文件：
  - `web/src/lint-utils.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（27/27）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
- 风险与后续：
  - 后续可继续增加“按路径/建议文本关键词筛选”，形成完整的问题定位工具链。

## 2026-04-13 Lint path/suggestion 关键词筛选（subagents 并行）
- 目标：在 Lint 面板增加 `path` 与 `suggestion` 关键词筛选，并与 severity/code 筛选叠加。
- 并行分工：
  - 子代理A：`lint-utils.ts` + `app-utils.test.ts`（新增 path/suggestion 筛选函数与测试）。
  - 子代理B：`App.tsx`（新增输入框、叠加筛选、空状态文案）。
  - 主控：过滤逻辑收敛到工具函数复用、回归验证、记录归档。
- 关键决策：
  - 新增 `filterLintIssuesByPath`、`filterLintIssuesBySuggestion`，均采用大小写不敏感 `includes`。
  - 关键词为空返回全量；`path/suggestion` 缺失按空串处理。
  - 空状态文案覆盖：无问题、单维筛选无命中、多维组合无命中。
- 变更文件：
  - `web/src/lint-utils.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（33/33）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
- 风险与后续：
  - 后续可将筛选条件持久化到本地配置，支持重启后保持定位上下文。

## 2026-04-13 Lint 多维筛选持久化（subagents 并行）
- 目标：将 Lint 多维筛选扩展到 `path/suggestion`，并持久化筛选状态，减少回归时重复输入。
- 并行分工：
  - 子代理A：`lint-utils.ts` 与 `app-utils.test.ts`（新增 path/suggestion 过滤函数、筛选状态读写函数与测试）。
  - 子代理B：`App.tsx`（新增 path/suggestion 输入、清空筛选、状态恢复与写回）。
  - 主控：收敛为工具函数复用、回归验证、实施记录归档。
- 关键决策：
  - 筛选状态使用统一 key：`llm_wiki_lint_filters_v1`。
  - 读取异常（无存储/JSON 非法/字段缺失）统一回退默认值，避免 UI 崩溃。
  - 多维筛选顺序：severity -> code -> path -> suggestion；空状态文案区分单维与多维命中失败。
- 变更文件：
  - `web/src/lint-utils.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `docs/实施过程记录.md`
- 验证：
  - `npm --workspace web run test` 通过（37/37）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
- 风险与后续：
  - 后续可将筛选态与当前 vault 绑定（按 vault path 分桶），避免跨知识库误复用历史筛选。

## 2026-04-13 Lint 补丁建议预览 v1 + 多维筛选持久化回归（subagents 并行）
- 目标：
  - 接入 Lint 补丁建议预览 v1，仅展示不自动应用。
  - 前端 Lint 多维筛选继续完善为 `severity/code/path/suggestion`，并保持本地持久化。
  - 补充回归验证注意点，便于 Windows 侧复测。
- 关键决策：
  - 补丁建议仅做预览，不进入自动写盘或自动应用流程。
  - 筛选态继续沿用本地持久化，页面刷新后保持上次筛选上下文。
  - 多维筛选仍按 `severity -> code -> path -> suggestion` 叠加，避免结果集回退不一致。
- 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo test`
  - `cargo tauri dev --config .\tauri.conf.json`
  - `Set-Location E:\llm-wiki`
  - `npm --workspace web run test`
  - `npm --workspace web run typecheck`
  - `npm --workspace web run build`
- 注意点：
  - `cargo tauri dev` 如提示端口占用，优先检查是否已有 Vite/Tauri 进程残留。
  - 若提示可执行文件占用，先关闭旧的 `llm-wiki-tauri.exe` 再重启。
  - 端口与进程问题属于 Windows 常见回归项，复测前先清理旧进程。
- 变更文件：
  - `docs/实施过程记录.md`

## 2026-04-13 Lint 补丁审批流 v1（手动应用，subagents 并行）
- 目标：
  - 在“补丁建议预览”基础上，补齐最小可用审批闭环：用户点击后端命令手动应用补丁。
  - 保持“仅用户触发才写盘”，不引入自动应用。
- 并行分工：
  - 子代理A（后端）：`models/commands/main/state` 新增 `apply_lint_patch` 命令与最小可应用集合。
  - 子代理B（前端）：`types/tauri-client/App` 接入“应用建议”按钮、调用与回显。
  - 主控：接口契约收敛（请求字段最小化）、语义修正（`MISSING_INDEX_ENTRY` 只补 index 引用）、回归验证与记录。
- 关键决策：
  - `apply_lint_patch` 仅支持四类 code：
    - `MISSING_INDEX_ENTRY`
    - `ORPHAN_WIKI_PAGE`
    - `INDEX_MISSING`
    - `LOG_MISSING`
  - 其余 code 返回：`暂不支持自动应用，请手动处理`。
  - `MISSING_INDEX_ENTRY` 修正为“补齐 index 链接”，不再创建页面占位文件。
  - 前端应用按钮在运行中全局禁用同组按钮，避免并发重复写入。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `src-tauri/src/state.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 自动化验证：
  - 前端：
    - `npm --workspace web run test` 通过（41/41）。
    - `npm --workspace web run typecheck` 通过。
    - `npm --workspace web run build` 通过。
  - 后端：
    - 已补 state 单元测试（成功应用 + 不支持 code + `MISSING_INDEX_ENTRY` 补链路）。
    - 当前 WSL 环境缺少 `cargo`，未在本地执行 Rust 测试；需在 Windows PowerShell 复测。
- Windows PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo test`
  - `cargo tauri dev --config .\tauri.conf.json`
  - `Set-Location E:\llm-wiki`
  - `npm --workspace web run test`
  - `npm --workspace web run typecheck`

## 2026-04-13 补丁审批历史 v1（subagents 并行）
- 目标：
  - 将 `apply_lint_patch` 的执行结果持久化，支持查询“最近补丁应用记录”。
  - 前端在 Lint 面板展示补丁历史，并在每次应用后自动刷新。
- 并行分工：
  - 子代理A（后端）：`models/db/state/commands/main` 增加事件模型、落库与查询命令。
  - 子代理B（前端）：`types/tauri-client/App/styles` 增加历史读取、展示与刷新。
  - 主控：契约收敛与校验，修复命令名不一致（前端从 `fetch_recent_*` 更正为 `get_recent_*`）。
- 关键实现：
  - 新增事件表：`lint_patch_events`（`issue_code/path/applied/message/created_at`）。
  - `apply_lint_patch` 成功后调用事件写入；写失败只记录 warning，不中断主流程。
  - 新增命令：`get_recent_lint_patch_events`（固定最近 20 条）。
  - 前端新增“最近补丁应用记录”列表；应用补丁后刷新历史与 lint 结果。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm --workspace web run test` 通过（42/42）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - 当前 WSL 无 `cargo`，Rust 测试需在 Windows PowerShell 侧执行。
- Windows PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo test`
  - `cargo tauri dev --config .\tauri.conf.json`
  - 在应用中执行一次“运行 Lint -> 生成补丁建议 -> 应用建议”，确认“最近补丁应用记录”出现新条目。

## 2026-04-13 批量应用补丁建议 v1（subagents 并行）
- 目标：
  - 在“补丁建议”列表上提供一次性批量执行入口，减少逐条点击成本。
  - 批量执行后给出成功/失败/跳过汇总，并保持历史记录可追踪。
- 并行分工：
  - 子代理A（后端）：新增 `apply_lint_patches_batch` 命令与批量结果模型、状态层汇总逻辑和单元测试。
  - 子代理B（前端）：新增批量调用与按钮、汇总展示、最小测试。
  - 主控：修复前后端参数契约（`inputs`）、兼容后端统计字段（`total/success/failed/skipped`）并做回归校验。
- 关键实现：
  - 后端新增：
    - `apply_lint_patches_batch(inputs: Vec<LintPatchApplyInput>)`
    - 批量结果：`total/success/failed/skipped/items`
    - 单条失败不阻断后续项，最终输出汇总日志。
  - 前端新增：
    - “批量应用可应用项”按钮与 loading 状态
    - 汇总文案展示（成功/失败/跳过）
    - 批量后自动刷新 lint 与“最近补丁应用记录”
  - 契约修正：
    - 前端批量请求体改为 `inputs`（并兼容保留 `items`）。
    - 批量响应归一化新增对 `success/failed/skipped/total` 字段兼容。
- 变更文件：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/app-utils.test.ts`
  - `web/src/styles.css`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm --workspace web run test` 通过（43/43）。
  - `npm --workspace web run typecheck` 通过。
  - `npm --workspace web run build` 通过。
  - 当前 WSL 无 `cargo`，Rust 测试需在 Windows PowerShell 侧执行。
- Windows PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo test`
  - `cargo tauri dev --config .\tauri.conf.json`
  - 在应用中执行一次“运行 Lint -> 生成补丁建议 -> 批量应用可应用项”，确认汇总文案与历史记录刷新。

## 2026-04-13 摄入/查询按钮卡住修复（subagents 并行）
- 问题现象：
  - 点击“示例摄入”后按钮长期停留“摄入中...”，界面看起来假死。
- 根因分析：
  - `handleDemoIngest` 与 `handleQueryAsk` 在设置 loading 状态后，先在 `try` 外执行 `await listenProgress(...)`。
  - 一旦订阅阶段异常，`finally` 不会运行，导致 `setDevAction(null)` / `setQueryRunning(false)` 不触发。
- 修复策略：
  - 将进度订阅移入 `try` 内部，并增加独立保护分支：
    - 订阅失败仅 `console.warn`，不阻断主流程。
    - `finally` 中仅在 `unlisten` 存在时调用，确保状态总能复位。
- 变更文件：
  - `web/src/App.tsx`
  - `docs/实施过程记录.md`
- 自动化验证：
  - `npm --workspace web run test` 通过（49/49）。
  - `npm --workspace web run typecheck` 通过。
- Windows PowerShell 复验：
  - `Set-Location E:\llm-wiki`
  - `npm --workspace web run test`
  - `npm --workspace web run typecheck`
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo tauri dev --config .\tauri.conf.json`
  - 在应用中分别点击“示例摄入”“执行 Query”，确认按钮可从运行态恢复，不再长期卡住。

## 2026-04-16 P12：搜索关键词高亮 + Wiki 页面删除

### 功能说明
- **标题高亮**：Wiki 列表搜索命中时，标题中的关键词用 `<mark>` 高亮（复用 `buildWikiHighlightSegments`）。
- **删除按钮**：Wiki 详情页非编辑模式下新增"删除"按钮（danger 样式），二次 `confirm` 确认后删除。
  - 前端：删除后关闭预览、从列表移除对应项、显示状态消息。
- **`delete_wiki_page` 命令**：删除 `.md` 文件 + 清理 `fts_pages` / `citations` / `wiki_pages` 三表记录。

### 变更文件
- `src-tauri/src/models.rs`：新增 `DeleteWikiPageResult`
- `src-tauri/src/db.rs`：新增 `delete_wiki_page_from_db` + 回归测试
- `src-tauri/src/state.rs`：新增 `delete_wiki_page_impl`（DB 失败降级）
- `src-tauri/src/commands.rs`：新增 `delete_wiki_page` Tauri 命令
- `src-tauri/src/main.rs`：注册命令
- `web/src/types.ts`：新增 `DeleteWikiPageResult` interface
- `web/src/tauri-client.ts`：新增 `deleteWikiPage(path)`
- `web/src/App.tsx`：标题高亮 + 删除按钮 + `handleDeleteWikiPage` handler
- `web/src/styles.css`：新增 `.dev-panel__button--danger` 样式

### 验证基线
- `cargo test`：**86 通过，0 失败**
- `npx vitest run`：**105 通过，0 失败**
- TypeScript：**零错误**

## 2026-04-16 P13：Wiki 正文 Markdown 渲染


## 2026-04-16 P14：流式增强与测试性能重构

### 功能说明
- **Provider 抽象化**：完成 `LlmProvider` trait 提取，解耦 `Ollama/OpenAI` 具体实现，支持动态 Mock 注入。
- **测试性能重构**：优化数据库连接池（改为共享 Arc<Pool>），异步命令上下文注入优化，移除测试中冗余的同步 IO。
- **变更修复**：修正 `run_semantic_lint` 调用签名，修复异步上下文匹配错误。

### 变更文件
- `src-tauri/src/llm/mod.rs`
- `src-tauri/src/llm/provider.rs`
- `src-tauri/src/llm/ollama.rs`
- `src-tauri/src/llm/openai.rs`
- `src-tauri/src/state.rs`
- `src-tauri/src/db.rs`

### 验证基线
- `cargo check`：通过。
- `cargo test`：已复核异步测试链路。

- `web/src/styles.css`：新增 `.wiki-preview__content--rendered` 及子元素样式（h1-h4、p、ul/ol、code、pre、blockquote、table、a、hr、strong、em）

### 验证基线
- `npx vitest run`：**105 通过，0 失败**
- TypeScript：**零错误**

## 2026-04-16 P14：Wiki 页面重命名

### 功能说明
- Wiki 详情页非编辑模式新增"重命名"按钮，点击后出现 inline 输入栏。
- 输入新文件名（不含 .md 后缀），Enter 确认/Esc 取消。
- 后端校验：不能含路径分隔符、不能与已有文件同名。
- 成功后：文件系统重命名 + DB 三表 path 同步（wiki_pages / citations / fts_pages）。
- 前端：关闭重命名栏，刷新页面列表，以新路径重新打开详情。

### 变更文件
- `src-tauri/src/models.rs`：新增 `RenameWikiPageResult`
- `src-tauri/src/db.rs`：新增 `rename_wiki_page_in_db` + 回归测试
- `src-tauri/src/state.rs`：新增 `rename_wiki_page_impl`（输入验证 + 文件操作 + DB 同步）
- `src-tauri/src/commands.rs`：新增 `rename_wiki_page` Tauri 命令
- `src-tauri/src/main.rs`：注册命令
- `web/src/types.ts`：新增 `RenameWikiPageResult` interface
- `web/src/tauri-client.ts`：新增 `renameWikiPage(oldPath, newName)`
- `web/src/App.tsx`：重命名按钮 + inline 输入栏 + handler
- `web/src/styles.css`：新增 `.wiki-rename-bar` 样式

### 验证基线
- `cargo test`：**87 通过，0 失败**
- `npx vitest run`：**105 通过，0 失败**
- TypeScript：**零错误**

## 2026-04-16 P15：文件路径输入改为 picker + 多选

### 功能说明
- 所有文件/文件夹路径输入框新增"选择"按钮，调用原生 Tauri 文件对话框。
- **Vault 路径**：文件夹选择（单选），📁 按钮，更新 vaultPath。
- **文件摄入**：文件多选（md/txt/pdf/docx/pptx/png/jpg/bmp/webp/tif），📄 按钮；
  - 选中多个文件后显示"已选 N 个文件"占位 + 文件名列表 + 清除按钮；
  - 摄入时逐个处理，显示 (1/N) 进度，完成后清除选择。
- 手动输入仍然有效；picker 选择后手动输入自动切换回单路径模式。
- 示例摄入路径保持手动输入（dev 工具，不需要 picker）。
- 非 Tauri 环境（浏览器）下 picker 按钮 disabled，行为不变。

### 变更文件
- `src-tauri/Cargo.toml`：新增 `tauri-plugin-dialog = "2"`
- `src-tauri/src/main.rs`：注册 `.plugin(tauri_plugin_dialog::init())`
- `src-tauri/capabilities/default.json`：新建，权限 `core:default` + `dialog:allow-open`
- `web/package.json`：新增 `@tauri-apps/plugin-dialog`
- `web/src/tauri-client.ts`：新增 `pickFiles()` / `pickFolder()` helper
- `web/src/App.tsx`：Vault 路径 + 文件摄入 input 改造（picker 按钮 + 多选文件列表）；`handleFileIngest` 支持多文件顺序摄入
- `web/src/styles.css`：新增 `.path-input-row` / `.path-pick-btn` / `.picked-files` 系列样式

### 验证基线
- `npx vitest run`：**105 通过，0 失败**
- TypeScript：**零错误**
- Rust：capabilities + Cargo.toml 配置就绪，`cargo test` 待下次启动验证

## 2026-04-16 16:20 — P16 收口：Ask 历史 DB 持久化 + 回退策略对齐（并行为主）

- 目标：
  - 接替 Claude Code 后快速收口 `P16`，把 Ask 历史从“前端 localStorage 为主”升级为“后端 DB 优先 + 前端回退”。
  - 对齐代码与文档，明确下一次 Claude Code 接手入口。
- 编码前调研闸门（本轮）：
  - 已检查当前可用 MCP/tools 与 skills/workflow，结论是本轮实现无需新增安装。
  - 取舍：直接复用现有 Tauri + SQLite + React 流程，避免引入新依赖造成交接噪音。
- 关键决策：
  - 按 `agents.md §14` 执行 subagents 并行拆分：后端子任务与前端子任务分开改造，避免同文件冲突。
  - 后端 Ask 历史采用 `question_norm` 去重（裁剪空白 + 压缩空白 + 小写），并加唯一索引保障幂等。
  - Ask 历史落库后执行容量裁剪，上限 `200` 条，避免无限增长。
  - 前端初始化策略改为：优先 `get_ask_history`；后端不可用时回退 `localStorage`，并保持 chips 交互不变。
  - 查询成功后同时更新内存历史与 localStorage，并异步调用 `save_ask_history`，不阻塞主流程。
- 变更文件：
  - `src-tauri/src/db.rs`
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/tauri-client.ts`
  - `web/src/app-utils.test.ts`
  - `AGENTS.md`
  - `docs/实施过程记录.md`
- 自动化验证（WSL）：
  - `npm --prefix web run typecheck`：通过
  - `npm --prefix web run test -- --run`：通过（108/108）
  - `npm --prefix web run build`：通过
  - `cargo check` / `cargo test`：当前 WSL 环境无 `cargo`，无法执行（待 Windows 复核）
- 用户侧 PowerShell 验证命令（Windows）：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - 手动验证：
    - 连续提问同义问题（大小写/空白不同），确认 Ask 历史不重复并刷新到最新。
    - 连续提问 220+ 个不同问题，确认历史保留上限为 200。
    - 模拟后端不可用后重启，确认 Ask chips 可从 localStorage 回退显示。
- 给 Claude Code 的收口与入手点（下一轮）：
  - 当前收口点：`P16` 功能代码已就位，缺口主要在 Windows Rust 基线复核。
  - 下一轮先做：
    1. 在 Windows 先跑 `cargo check` + `cargo test` 确认 `ask_history` 相关迁移与测试无回归。
    2. 再进入 `P17`（优先“Wiki 标签/分类筛选”），避免与文件树功能并行改同一前端文件。
- 风险与后续：
  - `ask_history` 迁移在极大历史库下首次启动可能有额外耗时，后续可补迁移耗时日志。
  - 根工作区在 WSL 下执行 `npm install` 会受 `@tauri-apps/cli-win32-x64-msvc` 平台限制；本轮验证使用 `npm --prefix web` 绕开。

## 2026-04-16 16:35 — P17-B：Vault 文件树浏览 + 计划对齐

- 目标：
  - 按开发计划继续推进 Wiki 浏览体验，在 Wiki 模块增加左侧 Vault 文件树，支持层级浏览与点击打开页面。
  - 同步对齐计划文档，明确 P17/P18 收口与下一轮入口。
- 关键决策：
  - 文件树基于现有 `WikiPageItem.path/display_path` 前端构建，不新增后端接口，降低改动面与回归风险。
  - 目录节点支持折叠/展开，文件节点点击后复用既有 `handleOpenWikiPage` 流程，避免新状态机。
  - 与现有筛选策略对齐：文件树基于当前 `displayedWikiPages` 渲染，自动跟随关键词与标签筛选结果变化。
  - 本轮受当前会话执行规则限制（未获用户显式授权开启 subagents），采用主控串行实施；已在记录中注明。
- 变更文件：
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `agents.md`
  - `docs/实施过程记录.md`
- 自动化验证（WSL）：
  - `npm --prefix web run typecheck`：通过
  - `npm --prefix web run test -- --run`：通过（110/110）
  - `npm --prefix web run build`：通过
- 用户侧 PowerShell 验证命令（Windows）：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - 手动验证：
    - 进入 Wiki 模块，确认出现左侧“Vault 文件树”。
    - 点击目录可折叠/展开；点击文件可打开对应页面详情。
    - 在关键词搜索与标签筛选后，文件树与右侧列表同步收敛到筛选结果。
- 给 Claude Code 的收口与入手点（下一轮）：
  - 当前收口点：`P17-A/P17-B` 已落地，`P18` 待做。
  - 下一轮优先：Ask 历史管理增强（清空入口 + 关键词过滤），然后再做文件树增强（自动定位当前页）。
- 风险与后续：
  - 当前树节点名称直接来自路径片段，后续可增加“仅显示相对 vault 路径”策略统一展示口径。
  - 目录较深时树会变长，后续可补“仅展开当前路径”与“折叠全部”快捷操作。

## 2026-04-16 16:50 — Ask 流式对话实施计划（1→2→3）固化

- 目标：
  - 根据用户确认，将 Ask 对话升级路线按 `1（伪流式）→2（真流式单轮）→3（真流式多轮）` 固化为可执行计划。
  - 为 Claude Code 提供明确接手入口、阶段边界与收口标准。
- 关键决策：
  - 采用分阶段收口，不跨阶段并行开发，确保每阶段都可独立验证与回滚。
  - 阶段 1 优先做最小可用体验改进（事件分片渲染），在不改 Provider 核心抽象前提下先交付可感知提升。
  - 阶段 2 再升级为真流式（Provider stream 抽象 + 统一事件协议），避免一次性引入过多变量。
  - 阶段 3 最后引入会话态（session/cancel/retry），保持与任务状态机、日志契约一致。
  - 考虑当前周额度较低，本轮只做计划与文档收口，不展开新代码实现。
- 分阶段 DoD（新增）：
  - 阶段 1 DoD：
    - Ask 面板可实时增量显示回答文本（伪流式）。
    - 现有 `query_ask`、citations、保存到 Wiki 不回归。
    - 前端测试与手动脚本覆盖增量渲染路径。
  - 阶段 2 DoD：
    - 本地 Ollama 与云端 OpenAI-compatible 均支持真流式单轮。
    - 事件协议稳定：`start/chunk/done/error`（或等价语义）。
    - Strict Local / Hybrid 策略保持一致，无越权调用。
  - 阶段 3 DoD：
    - 支持多轮 session、取消生成、重试，且任务日志可追踪。
    - 会话状态可恢复，不破坏引用证据与保存回答链路。
- 本轮变更文件：
  - `agents.md`
  - `docs/实施过程记录.md`
- 自动化验证：
  - 本轮仅文档更新，无代码逻辑改动；不新增自动化测试。
- 用户侧 PowerShell 验证命令（下轮编码前基线）：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
- 给 Claude Code 的接手入口：
  - 从 `agents.md §18.2` 的 `P18 阶段 1` 开始实现。
  - 阶段 1 完成后先写实施记录并等待用户确认，再进入阶段 2。
  - 若额度紧张，优先完成“阶段 1 + 验证 + 文档收口”，禁止提前并入阶段 2/3 代码。
- 风险与后续：
  - 阶段 2 的 SSE/流式解析在不同云 Provider 兼容性有差异，需单独做 Provider 级回归。
  - 阶段 3 会话态引入后，前端状态复杂度显著上升，建议先定义最小会话状态模型再实现。

## 2026-04-16 17:05 — P18-1 完成：Ask 伪流式对话（后端分片 + 前端聊天流）

- 目标：
  - 按 `1→2→3` 路线推进第一阶段，在不改 Provider 抽象的前提下交付可感知的“流式聊天”体验。
- 关键决策：
  - 后端保留现有 `query_ask_with_options` 主流程，回答生成后按短片段通过 `query_progress` 事件推送。
  - 前端对 `query_progress` 按 `step` 解析：`answer_chunk` 作为回答增量，其余仍作为状态提示。
  - Ask UI 从“单次结果文本”升级为“消息流样式”（user/assistant），并保留原有 citations 与“保存回答到 Wiki”链路。
  - 本轮采用串行实施并快速收口，控制额度消耗；阶段 2/3 暂不展开。
- 变更文件：
  - `src-tauri/src/state.rs`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
  - `agents.md`
  - `docs/实施过程记录.md`
- 自动化验证（WSL）：
  - `npm --prefix web run typecheck`：通过
  - `npm --prefix web run test -- --run`：通过（112/112）
  - `npm --prefix web run build`：通过
  - `cargo check` / `cargo test`：当前 WSL 环境无 `cargo`，无法执行（待 Windows 复核）
- 用户侧 PowerShell 验证命令（Windows）：
  - `Set-Location E:\llm-wiki\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - `Set-Location ..\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - 手动验证：
    - 在 Ask 连续提问 2~3 次，确认对话区域按“你/助手”消息流渲染。
    - 观察助手回答是否逐段增长显示（而非一次性出现）。
    - 生成结束后 citations 正常展示，“保存回答到 Wiki”可正常执行。
- 给 Claude Code 的接手点（下一阶段）：
  - 从 `P18 阶段2（真流式单轮）` 开始：先改 Provider stream 抽象，再接入 Tauri 命令与统一事件协议。
  - 本阶段新增伪流式事件步骤：`answer_stream_start` / `answer_chunk` / `answer_stream_done`，阶段2可复用语义但应升级为真实 token/chunk 来源。
- 风险与后续：
  - 阶段1分片来自完整回答，首 token 延迟仍受后端生成耗时影响；需阶段2解决。
  - 事件分片频率目前按字符/标点切片，后续可按 Provider 原生 chunk 节奏替换。

## 2026-04-16 17:15 — P18-2 第一轮实现：真流式单轮（待 Windows Rust 验证）

- 目标：
  - 启动阶段2，实现 Provider 级真实流式单轮问答（不再依赖完整回答后二次切片）。
- 关键决策：
  - `LlmProvider` 新增 `complete_stream` 接口，并提供默认回退实现（调用 `complete` 一次性回调）。
  - `OllamaProvider` 接入 `/api/generate` `stream=true`，按行 JSON 增量解析并转发 chunk。
  - `OpenAiProvider` 接入 `/chat/completions` `stream=true`，按 SSE `data:` 增量解析并转发 chunk。
  - `query_ask_with_options` 改为直接消费 `complete_stream` 的增量，沿用 `query_progress` 的 `answer_chunk` 事件语义。
  - 保持前端事件解析逻辑兼容阶段1，不改用户交互路径。
- 变更文件：
  - `src-tauri/src/llm/provider.rs`
  - `src-tauri/src/llm/ollama.rs`
  - `src-tauri/src/llm/openai.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/Cargo.toml`
- 自动化验证：
  - `npm --prefix web run typecheck`：通过
  - `npm --prefix web run test -- --run`：通过（112/112）
  - `npm --prefix web run build`：通过
  - `cargo check` / `cargo test`：当前 WSL 环境无 `cargo`，未执行（必须在 Windows 复核）
- 用户侧 PowerShell 验证命令（Windows）：
  - `Set-Location E:\llm-wiki\src-tauri`
  - `cargo check`
  - `cargo test`
  - `cargo tauri dev`
  - `Set-Location ..\web`
  - `npm run typecheck`
  - `npm run test -- --run`
  - `npm run build`
  - 手动验证：
    - 切换 Ollama / Cloud Provider 后分别执行 Ask，确认回答按真实增量持续输出。
    - 对比阶段1行为，观察首段输出延迟应降低。
    - 确认最终 citations、保存到 Wiki、Strict Local 策略未回归。
- 当前状态：
  - 阶段2代码已进入“待验证”状态，未在用户回传 Windows 验证结果前标记为最终完成。

## 2026-04-17 17:55 �� P21-C2 ʵʩ��ɣ������������Զ��۽���������ǿ

- Ŀ�꣺����֪ʶͼ�׵���Ƚ������飬ʵ�ֽڵ���ٶ�λ��ƽ��̽����
- �ؼ����ߣ�
  - **��������ϵͳ**��ǰ������ `graphSearchHits` ״̬��֧�ֶԱ����·����ʵʱģ��ƥ�䡣ͼ��ͨ�� `nodeCanvasObject` �����нڵ��·����ƻ�ɫ���� (halo) �Ӿ�������
  - **ƽ������۽�**������ڵ㡢�������������ھ�ʱ��ͨ�� `graphRef.current.centerAt` ʵ��ƽ��λ�ƣ����Զ����ŵ� 2.0x ��ͼ�������ͼ�¡���·�����⡣
  - **��̬�����**������ʱ������Զ�������ʾ�����������������������̬�ָ����ڵ����顱��
  - **ͳһ�����·**���ع� `handleGraphNodeClick`����ѡ���߼����Ӿ��۽�����ϲ���
- ����ļ���
  - `web/src/App.tsx`������״̬�������߼�������۽�ʵ�֡�
  - `web/src/styles.css`�������������ʽ��������ָ��ߡ��������̬��ʽ��
- �Զ�����֤��
  - `npm run test`��130 ͨ����������ͼ���߼���֤����
  - `npm run typecheck`�������
  - `cargo check`��ͨ����
- �û��� PowerShell �ֶ���֤���
  - `Set-Location E:\llm-wiki\web; npm run tauri:dev`
  - ��֤�㣺
    - ��ͼ�׶�������������ؼ��ʣ�ͼ�ж�Ӧ�ڵ�Ӧ���ֻ�ɫ���Ρ�
    - �����������б���ͼ�еĽڵ㣬ͼ�����Ӧƽ���ƶ������ġ�
    - ѡ�з�������/���ڣ�����֤��ͼ�ü��Ƿ����Ԥ�ڡ�
- �����������
  - Ŀǰ����Ϊ�� includes ƥ�䣬�����ɽ����� FTS5 ʵ�ָ����ܵ�������
  - �ڵ�� x/y ��������ڲ�����δ�ȶ�ʱ���ȶ����Ѽ��� number ���ͼ�鱣����

## 2026-04-17 18:05 �� Э���淶ǿ����ǿ�Ʋ��п���ģʽ (Subagents)

- Ŀ�꣺ȷ����Ŀ����Ч����������������ͳһ���� Agent (Gemini/Claude/Codex) ��ִ����Ϊ��
- �ؼ����ߣ�
  - **ȫԱ����Լ��**����ȷ Gemini ���ڵ����� AI Agent ��ִ�б�������ʱ����������ʹ�� subagents ģʽ�����������벢��ʵʩ��
  - **����ְ��̻�**������ Agent ������ǰ��������񣬽�ֹ�ڴ��ģ����н��е��̴߳��С�����������������ȷʵ�޷���֣����ڼ�¼��ע������
  - **����͸����**��ÿһ�ֲ��п����Ľ����������Ӵ����Ĳ�����֤�����ڽ���ʱ�ṩ���������������״̬��
- Ӱ�췶Χ���������� P19/P20/P21 ���Ժ�׶εı��빤����

## 2026-04-17 18:10 �� P19-2 ʵʩ��ɣ�Wiki �ļ�����ǿ

- Ŀ�꣺�������ģĿ¼�ṹ�µ� Wiki ���Ч���붨λ�ٶȡ�
- �ؼ����ߣ�
  - **ͼ�껯�㼶**���ļ���չ��/������ò�ͬͼ�� (??/??)��Markdown ҳ���� ?? ��ʶ�������ļ��� ?? ��ʶ��
  - **�Զ���λ�߼� (Auto-Reveal)**��ʵ�� `autoRevealWikiPage`����ҳ��ʱ��ϵͳ�Զ��ݹ��������Ŀ¼��չ�������ƽ�������ýڵ����ӿ����ġ�
  - **����Ŀ¼����**�����ļ���ͷ��������չ��ȫ�����롰����ȫ�������������û���� `wikiTreeFolderKeys` ���ٸ���״̬��
  - **��ʽ��ĥ**������ҳ������ 3px ��߸����߿���Ŀ¼�б����ӵ��붯����
- ����ļ���
  - `web/src/App.tsx`���㼶�ݹ��߼����¡���������������Auto-Reveal ʵ�֡�
  - `web/src/styles.css`���ļ���������ͼ����������̬�Ӿ���ǿ��
- �Զ�����֤��
  - `npm run test`��130 ͨ����
  - `npm run typecheck`�������
  - `cargo check`��ͨ����
- �û��� PowerShell �ֶ���֤���
  - `Set-Location E:\llm-wiki\web; npm run tauri:dev`
  - ��֤�㣺
    - ����ļ���ͼ�꣬ȷ��Ŀ¼������չ��/����
    - �� Wiki ������ͼ���д�һ�����ҳ�棬ȷ�ϲ���ļ������Զ���λ����λ�á�
    - ���ԡ�ȫ��չ��/ȫ�����𡱰�ť��

## 2026-04-17 18:15 �� P19-3 ʵʩ��ɣ���ǩά����ǿ

- Ŀ�꣺֧�ֻ��ڱ�ǩ�ĸ߼�ɸѡ�߼�����ǿ֪ʶ��ķ������������
- �ؼ����ߣ�
  - **��ѡ���� (AND) ����**���� `wikiActiveTag` ����Ϊ `Set<string>`��ֻ��ͬʱ��������ѡ�б�ǩ��ҳ��Żᱻչʾ��
  - **��ǩ������ʾ**���� `allWikiTags` �����߼������������UI ���� `(n)` ��ʽչʾ���ṩֱ�۵�֪ʶ�ֲ�������
  - **�Զ����ò���**���ڴ���ҳ��ʱ�Զ���ձ�ǩɸѡ��ȷ���û��ܹ�����������ҳ�������Ķ����ܵ�ǰ���������ĸ��š�
  - **UI ��ǿ**��Ϊ����̬��ǩ������Ӱ�ͱ߿�ǿ������������
- ����ļ���
  - `web/src/App.tsx`�������߼���״̬�ع����������㡣
  - `web/src/styles.css`����ǩ Chip ����̬��ʽ�Ż���
- �Զ�����֤��
  - `npm run test`��130 ͨ����
  - `npm run typecheck`�������
  - `cargo check`��ͨ����
- �û��� PowerShell �ֶ���֤���
  - `Set-Location E:\llm-wiki\web; npm run tauri:dev`
  - ��֤�㣺
    - ����������ϵı�ǩ��ȷ���б���չʾͬʱ������Щ��ǩ��ҳ�档
    - ȷ��ÿ����ǩ Chip �ϵ�������ʵ�ʹ�����ҳ����һ�¡�
    - �����ȫ������ť��ȷ��ɸѡ״̬����ա�
