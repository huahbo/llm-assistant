# 实施过程记录

> 当前活跃记录。已完成阶段（H4-H7）的详细记录见 `docs/archive/实施过程记录.md`。

---

## 已完成阶段一览（详见归档）

| 阶段 | 完成时间 | 核心内容 |
|------|----------|----------|
| H4 | 2026-04-27 | Research Mode 检索增强（draft 生成时注入 wiki 上下文） |
| H5 | 2026-04-27 | Auto-Memory + Draft Rewrite + Ask 联动 + Skill 模板变量 |
| H6-S1/S1.5 | 2026-04-27 | Shell 安全策略（5维可配置）+ PathGuard |
| H6-S2 | 2026-04-27~28 | Agent 多轮工具循环（run_shell/search_wiki/read_wiki/write_wiki 审批） |
| H6-S3 | 2026-04-29~30 | 历史 Runs 软删除 + 工具时间线 + 运行模块收敛 |
| H7 | 2026-05-04~07 | App.tsx 架构拆分（5个Context + 9个Module），消除9500行单组件 |

---

## H8 — ReAct 流式对话 Agent（2026-05-07 ~ 2026-05-09）

### C1：基础层（LLM + DB）

**C1.1** — `LlmProvider` trait 加入 `chat_stream` 方法 + `ChatMessage/ChatStreamEvent` 类型定义。

**C1.2** — OpenAI/DeepSeek `chat_stream` 实现（SSE deltas 累积器）。

**C1.3** — Ollama `chat_stream` 实现 + version 探测能力降级（低版本 Ollama 自动回退 `/api/chat`）。

**C1.4** — `agent_chat` 模块：DB schema（conversations/messages/tools 三张表）+ CRUD + 内置工具种子（search_wiki/read_wiki/write_wiki/run_shell/web_search/fetch_url）。

### C2：后端 ReAct 主循环

**C2.1** — `agent_chat/tools.rs`：工具执行分发器（按 tool_name 路由到各 exec_* 函数）。

**C2.2** — `agent_chat/stream_parser.rs` + `commands.rs`：ReAct 主循环（多轮 LLM→工具→观察），流式 Tauri 事件 emit（`chat_text_delta/chat_tool_call/chat_tool_result/chat_message_done`）。`reasoning_content` 静默累积并存 DB（不 emit）。

**C2.3** — Tauri 命令注册：`send_chat_message/cancel_chat_message/get_conversations/get_messages`。

### C3：前端流式 UI

**C3.1** — `web/src/types.ts` 对话相关类型 + `tauri-client.ts` 命令封装。

**C3.2+C3.3** — `MessageBubble.tsx`（工具卡/流式 cursor/Markdown渲染）+ `useChatStream.ts`（reducer 驱动的流式状态机）+ 路由集成到主导航。

### C4：对话管理功能

**C4.1** — 自动生成对话标题（首条用户消息发出后，后台调 LLM 生成标题）。

**C4.2** — Skill 模板 + 记忆注入（与 Agent Studio 共享 ShellPolicyContext 数据源）。

**C4.3** — 对话列表搜索/归档/改名。

### C5：写操作审批 + 稳健性

**C5.1** — `write_wiki` 工具集成 ReAct 循环（审批前置，`chat_awaiting_approval` 事件，前端显示审批按钮）。

**C5.2** — 取消流（`cancel_chat_message` + `chat_cancelled`）+ 错误处理（`chat_error`）+ 类型/工具单元测试（`types.rs/tools.rs`）。

- 验证：`cargo test` ✅ `npm run typecheck` ✅

---

## H9-A — Chat UI 改进（2026-05-09~10）

- **Windows 系统提示**：`build_system_prompt()` 注入当前日期 + PowerShell 环境说明。
- **reasoning_content 修复**：DeepSeek 的 `thinking` 字段静默累积，存 DB，不在 UI 显示。
- **`<think>` 标签剥离**：`MarkdownRenderer` 渲染前用 regex 去除 `<think>...</think>` 块。
- **对话列表时间**：修复 `NaN` 显示，改为具体日期（`toLocaleDateString`）。
- **标题栏双击卡死**：移除 `startDragging()` 调用（Tauri v2 已弃用）。
- 提交：`5c310af` `18f6de2` `8d656cf`

---

## H9-B — Web 搜索工具（2026-05-10）

### 工具实现

- **`web_search`**：四级联搜（SearXNG → Tavily → Brave Search API → DuckDuckGo Lite via scraper）。
  - `state.rs`：`search_brave()`、`search_powershell()`（DuckDuckGo scraping）、`search_web_cascade()` 公开方法。
  - `models.rs`：`SearchConfig` 增加 `brave_api_key` 字段。
- **`fetch_url`**：`scraper` 0.20 抓取并提取正文文本（去脚本/样式）。
- DB 种子：`web_search` / `fetch_url` 注册为内置工具（tools 表）。
- 系统提示增加 web_search 使用指引与今日日期注入（`chrono::Local::now()`）。

### 前端配置

- `SearchConfigPanel.tsx`：增加 Brave Search 选项 + 条件显示 API Key 输入框。
- `tauri-client.ts`：`defaultSearchConfig` 增加 `brave_api_key: ""`。

### Bug 修复（同批）

- 代码块主题：改为 `atom-one-light`（浅色背景 `#f6f8fa`）。
- 提交：`fb23f29` `57b72ef`

---

## H9-C — 工具卡片折叠 + 重复消息修复（2026-05-11）

### ToolGroup 折叠组件

- `MessageBubble.tsx`：所有工具调用卡片合并为单个 `ToolGroup` 折叠组。
  - 默认收起；工具运行中或等待审批时自动展开；完成后显示 `N/M 完成 · Xms`。
- `styles.css`：`.chat-tool-group` 系列样式。

### 重复消息根因修复

- **根因**：ReAct 多轮迭代中，每轮创建新的 DB assistant 占位消息（不同 `message_id`）。原 `useChatStream.ts` reducer 的 `message_id` 固定在首个事件的 ID，末轮消息过滤失效，导致流式气泡与 DB 消息同时显示。
- **修复**：`TEXT_FLUSH` 和 `TOOL_CALL_START` 两个 reducer 分支均实时更新 `message_id: action.messageId`，始终跟踪最新一轮的 ID。
- **空行消失**：DB 中工具调用迭代产生的 `content=null` 助手消息，在 `MessageBubble` 渲染时提前 `return null`（非取消状态下）。
- 提交：`1c42f33`

---

## 当前状态（2026-05-11）

- **版本**：`v0.2.0`（`Cargo.toml` + `tauri.conf.json` + `package.json`）
- **测试基线**：`cargo test` 全绿（含 `agent_chat` 模块）；`npm run typecheck` ✅
- **Remote**：`main` @ `1c42f33`，已与 origin 同步
- **下一步**：H9 剩余小 bug 处理，或开启 H10 新阶段

---

> 历史实施记录（H4-H7，2026-04-27 ~ 2026-05-07）详见 `docs/archive/实施过程记录.md`

---

## H10 — MCP 动态装载（2026-05-11 ~ 2026-05-12）

- JSON-RPC 2.0 stdio 传输层；支持 `upsert_mcp_server / delete_mcp_server / reload_mcp_server` 三条 Tauri 命令。
- 关键文件：`src-tauri/src/mcp/`（新模块），`src-tauri/src/main.rs` 注册，`web/src/modules/agent/AgentStudio.tsx` Settings 页签，`web/src/tauri-client/mcp.ts`。
- 工具执行分发：`agent_chat/tools.rs` 新增 MCP 工具路由分支，动态读取已装载服务端能力。

---

## H11 — Swarm 子代理（2026-05-12 ~ 2026-05-13）

- `spawn_subagent` 工具：层级追踪（depth ≤ 2），父子对话 ID 关联，防止无限嵌套。
- Wiki 辅助工具增强：`list_wiki_pages` 分页、`read_wiki` 分块读取、`search_wiki` 混合召回（FTS + 向量）。
- 关键文件：`src-tauri/src/agent_chat/tools.rs`，`commands.rs`，前端工具卡 `MessageBubble.tsx`。

---

## H12 — 本地嵌入增强（2026-05-13 ~ 2026-05-14）

- Ollama `embed_page` 接口接入 RRF 四路召回（FTS5 + BM25 + 向量余弦 + 实体匹配）。
- Embed Provider 不可用时自动降级为纯 FTS，不阻断摄入流程。
- 关键文件：`src-tauri/src/search.rs`，`db.rs`（向量索引表），`state.rs`（RRF 融合逻辑）。

---

## H13 — 图谱双向联动（2026-05-14）

- `GraphBridgeContext` 在图谱节点上注入右键菜单："问这个"（带页面内容发起对话）、"检索相关"（触发混合搜索）。
- AI 回复中引用的 Wiki 路径自动高亮对应图谱节点（反向联动）。
- 关键文件：`web/src/modules/graph/`，`web/src/App.tsx`（Context 注入），`web/src/modules/agent/AgentChat.tsx`。

---

## H14/misc — 对话增强杂项（2026-05-14 ~ 2026-05-15）

- 跨会话消息全文搜索（DB FTS，`search_chat_messages` 命令）。
- 对话导出 Markdown 及"存入 Wiki"一键操作。
- `spawn_subagent` 前端工具卡专属渲染（子对话链接，深度徽章）。
- 历史工具调用在已完成对话中可展开查看（`get_messages` 补全 tool_calls 字段）。
- 关键文件：`src-tauri/src/agent_chat/commands.rs`，`web/src/modules/agent/`。

---

## H16 — state.rs 12 阶段拆分（2026-05-15 ~ 2026-05-17）

- 将 4000+ 行 `state.rs` 按功能边界拆为 12 个 service 子模块（`ingest_service`, `search_service`, `wiki_service` 等），测试全量迁移到各子模块，编译警告清零。
- 提交标签：`v0.2.3`；`cargo test` 138 全绿，`npm run typecheck` 0 errors。
- 关键文件：`src-tauri/src/state/`（新目录），`src-tauri/src/agent_chat/mod.rs`，`src-tauri/src/main.rs`。

---

## H17 — MCP 市场 + Skill UX 重设计（2026-05-17）

- `DiscoveryModule`：对接 Smithery Registry API，展示可用 MCP 服务列表，一键安装（写入配置并热装载），Env Key 输入对话框收集必要环境变量。
- Skill UX 重设计：预设 Skill 选择面板、剪贴板快速导入、`activeSkillKey` 删除后自动回退修复。
- 质量修复（v0.2.4）：`registry.rs` 中 `USER_AGENT` 改 `env!` 宏，`encode_byte` 提取为共享函数；`DiscoveryModule` installing 状态锁修复，`isInstalled` 精确路径匹配；`AgentSkillsPanel` presetRef click-outside 事件绑定。
- 关键文件：`web/src/modules/discovery/`（新模块），`src-tauri/src/agent_chat/registry.rs`（新文件），`web/src/modules/agent/AgentSkillsPanel.tsx`，`web/src/modules/agent/AgentStudio.tsx`。


---

## H21 — 全局命令面板 Ctrl+K（2026-05-17）

- CommandPalette.tsx：模糊搜索 Wiki 页面 + 操作命令 + 最近访问（localStorage），键盘导航（↑↓ Enter Esc），Portal 渲染避免 z-index 问题。
- 关键文件：web/src/modules/CommandPalette.tsx，web/src/contexts/CommandPaletteContext.tsx。

---

## H22 — Wiki 知识导出（2026-05-17）

- xport_wiki_markdown_zip：所有 Wiki 页面打包为 Markdown ZIP。
- xport_wiki_html_zip：pulldown-cmark 渲染 Markdown→HTML，[[link]] 转内部链接，可本地浏览。
- Operations Tab 新增"导出"区块，两个下载按钮。
- 关键文件：src-tauri/src/state/wiki_service.rs，web/src/modules/settings/OperationsModule.tsx。

---

## H23 — Wiki 内联 AI 辅助编辑（2026-05-17）

- 选中文字 → SelectionToolbar（续写/改写/扩写）→ AiAssistPreview 流式预览 → 接受/拒绝。
- i_assist_wiki_edit Tauri 命令，流式 LLM 响应通过 i_assist_chunk 事件推送。
- 关键文件：web/src/modules/wiki/SelectionToolbar.tsx，web/src/modules/wiki/AiAssistPreview.tsx，src-tauri/src/state/wiki_service.rs。

---

## H24 — 无头浏览器服务（2026-05-20）

- src-tauri/src/browser/mod.rs：Chrome→Edge→静态HTTP 三级兜底，headless_chrome CDP + spawn_blocking。
- etch_url_context Tauri 命令 + 前端 UrlContextCard + ChatInputBar URL paste 检测。
- 顺手修复旧 html_to_text 反向引用 bug（\1 Rust regex 不支持）。
- 测试：284 通过 0 失败（新增 3 个 browser 单元测试）。
- 关键文件：src-tauri/src/browser/mod.rs（新文件），src-tauri/src/state/ingest_service.rs，web/src/modules/agent/UrlContextCard.tsx，web/src/modules/agent/ChatInputBar.tsx。

---

## H25 — Deep Research 品质升级第一轮（2026-05-21）

- P1 报告格式：[N] 行内引用 + 学术/网页 References 双格式。
- P2 多搜索提供商并行：Tavily + SearXNG 同时搜，URL 去重，按质量分排序。
- P3 研究调研：参考 gpt-researcher、STORM、local-deep-research 等项目。
- P4 独立 HTML 单文件导出：xport_research_html 命令，CSS 内嵌，ResearchPanel 下载按钮。
- ResearchPanel UI 全面美化（terminal 风格日志、状态色徽章、语义按钮色）。
- commit d34465b；详细计划写入 docs/h26-deep-research-quality-plan.md。

---

## H26 — Deep Research 质量升级第二轮（2026-05-21）

### H26-B 学术 API

- search_arxiv()：arXiv Atom XML 解析，无需新依赖，返回标准 WebSearchResult。
- search_semantic_scholar()：S2 Graph API JSON 解析，可选 API key（SearchConfig.semantic_scholar_api_key）。
- SearchConfigPanel 新增 arXiv / Semantic Scholar 复选框 + 条件显示 API key 输入。
- commit 49babcb。

### H26-C 来源质量评分

- score_by_domain(url) 按域名评 0.4–0.95 分（arxiv/scholar/nature/science 等高分）。
- do_search_multi 结果按 quality_score 降序排列 + 	runcate(limit)。
- WebSearchResult 新增 quality_score: f32 + source_type: String 字段。
- commit 49babcb（同 H26-B）。

### H26-A Outline-First 报告架构

- generate_research_outline()：LLM → JSON 大纲（N 节，N=breadth.clamp(3,5)），fallback 回退 H25 子查询路径。
- start_research_task 大纲优先路径：emit esearch_outline_ready → 用户确认大纲 → 各节独立搜索+综合 → intro/conclusion → 拼装报告。
- AppState.pending_outline_approvals + pprove_research_outline Tauri 命令。
- ResearchDialog 大纲审批 UI（可编辑节标题/问题，确认按钮）。
- ResearchPanel 新增 4 个状态标签：规划大纲/等待确认/章节写作/组装报告。
- commit 49babcb。

### H26-E 分章节进度推送

- mit_section_progress() 闭包携带 section_index/section_title/	otal_sections。
- ResearchProgressPayload TS 接口新增三个可选字段。
- ResearchDialog writing_section 阶段显示渐进式进度条（当前节 / 总节数）。
- stageIcon 补充 writing_section / assembling / planning_outline 图标。
- commit  03b234；测试：291 通过 0 失败；typecheck 零错误。
