# 实施过程记录归档（2026-04-20 ~ 2026-04-27）

> 本文件为 `docs/实施过程记录.md` 的历史归档，包含 2026-04-20 至 2026-04-27 期间各轮次实施记录。
> 活跃记录见 `docs/实施过程记录.md`（保留最近 4-5 条）。

## 2026-04-24 — 方向 B 推进：模板预览 + 最近项目快速切换（Codex）

- 背景：
  - 方向 A（摄入审批）与窗口顶栏问题已闭环，按 A→B→C 顺序推进方向 B（项目模板体验收口）。
  - 现状是模板初始化可用，但初始化前"会创建什么"不够直观。
- 本轮实现：
  - `web/src/App.tsx`
    - 新增模板预览构建函数：`buildTemplateInitPreview(template)`（导出，便于单测）。
    - Inbox / Vault 操作区新增"模板初始化预览卡"，实时展示：
      - 将创建目录（`raw/wiki/.app` + 模板扩展目录）
      - 将创建文件（`index.md/log.md/.app/config.json/.app/meta.db` + 非 general 模板的 `wiki/schema.md/wiki/purpose.md`）
      - 模板 `schema/purpose` 行数元信息
    - `handleInitVault` 复用当前选中模板对象，避免重复查找。
    - 初始化成功状态文案增加"模板名 + 创建项数量"。
    - 新增最近 Vault 列表（localStorage 持久化）：
      - 初始化成功后自动写入最近项目
      - 可一键回填路径
      - 支持清空最近项目
  - `web/src/styles.css`
    - 新增模板预览卡样式（含响应式 2 列→1 列）。
    - 新增最近 Vault 列表样式。
  - `web/src/app-utils.test.ts`
    - 新增 4 条前端单测：
      - general 模板只包含基础目录/文件
      - research 模板包含 `schema/purpose` 与扩展目录
      - 最近 Vault 路径去重/清洗
      - 最近 Vault 路径读写与最新优先
- 并行实施说明：
  - 本轮改动集中在同一前端文件簇（`App.tsx/styles/test`），为避免写冲突按串行方式实施。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（已知环境问题：缺失 `@rollup/rollup-linux-x64-gnu` 可选依赖）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki\web; npm run test -- --run`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. Inbox → Vault 操作区切换不同模板，预览卡目录/文件实时变化；
    2. 选择非 general 模板初始化 Vault，确认写入后状态栏显示"模板名 + 创建项数量"；
    3. 校验"最近项目"出现新建 Vault 路径，点击可回填到 Vault 输入框；
    4. 校验目标 Vault 下 `index.md/log.md/wiki/schema.md/wiki/purpose.md` 与模板 extraDirs 是否按预期存在。

## 2026-04-24 — 顶栏四次修复：放开窗口控制权限 + 最大化图标重绘（Codex）

- 背景：
  - 用户回传错误信息：`window.close not allowed. Permissions associated with this command: core:window:allow-close`。
  - 说明按钮点击已触达 API，但权限未授予。
- 本轮修复：
  - `src-tauri/capabilities/default.json`
    - 新增窗口权限：
      - `core:window:allow-minimize`
      - `core:window:allow-toggle-maximize`
      - `core:window:allow-close`
      - `core:window:allow-start-dragging`
  - `web/src/App.tsx`
    - 最大化按钮改为无字符占位，交由 CSS 绘制方框图标。
  - `web/src/styles.css`
    - `window-titlebar__action-glyph--maximize` 改为边框方块绘制，尺寸放大并对齐。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. 点击最小化/最大化/关闭无权限报错；
    2. 顶栏拖动与双击最大化继续可用；
    3. 最大化按钮方框图标尺寸更接近原生。

## 2026-04-23 — 顶栏三次修复：去掉 drag-region 依赖，避免吞掉按钮点击（Codex）

- 背景：
  - 用户继续反馈：顶栏按钮仍不响应，最大化图标仍偏小。
- 本轮修复：
  - `web/src/App.tsx`
    - 移除顶栏内 `data-tauri-drag-region` 标记，避免 drag-region 机制在部分环境吞掉按钮点击。
    - 顶栏改为统一监听 `onMouseDown`：
      - 若点击目标在 `.window-titlebar__actions` 内，则不触发拖拽；
      - 否则显式调用 `startDragging()`。
    - 顶栏双击同样排除按钮区后再触发 `toggleMaximize`。
  - `web/src/styles.css`
    - 移除 `-webkit-app-region` 相关样式，避免与显式拖拽逻辑叠加冲突。
    - 最大化按钮再次放大（按钮宽度 + 字形尺寸同步提升）。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. 右上角最小化/最大化/关闭按钮响应；
    2. 顶栏非按钮区域可拖动窗口；
    3. 双击顶栏非按钮区域可最大化/还原；
    4. 最大化按钮视觉尺寸更协调。

## 2026-04-23 — 顶栏二次修复：显式 `startDragging` + 窗口操作错误可见化（Codex）

- 背景：
  - 用户反馈上一版仍未达到预期：顶栏拖动与窗口控制按钮在其环境中未生效。
- 本轮修复：
  - `web/src/App.tsx`
    - 顶栏按钮改为直接使用静态 `getCurrentWindow()` 调用，减少动态导入路径差异风险。
    - 新增 `handleTitlebarMouseDown`，在左键按下时显式调用 `startDragging()`，不再仅依赖 `data-tauri-drag-region`。
    - 窗口控制失败时将错误写入 `statusMessage`，便于用户侧直接看到失败原因。
  - `web/src/styles.css`
    - 顶栏拖拽区域补充 `-webkit-app-region: drag`，按钮区域设为 `no-drag`，提高跨环境一致性。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. 顶栏空白区域拖动窗口；
    2. 双击顶栏切换最大化/还原；
    3. 右上角最小化/最大化/关闭按钮可用；
    4. 若失败，状态条应出现明确错误文本（用于下一步定位）。

## 2026-04-23 — 顶栏交互修复：窗口按钮可用 + 拖拽恢复 + 最大化按钮放大（Codex）

- 背景：
  - 用户反馈上一版自定义顶栏存在交互问题：最小化/最大化/关闭按钮不可用、顶部拖拽失效、最大化按钮视觉过小。
- 本轮修复：
  - `web/src/App.tsx`
    - 顶栏结构改为"拖拽区 + 按钮区"分离，避免拖拽区域覆盖按钮点击。
    - 拖拽区保留 `data-tauri-drag-region`，并支持双击切换最大化/还原。
    - 顶栏按钮保留 Tauri window API 调用：`minimize / toggleMaximize / close`。
  - `web/src/styles.css`
    - 新增 `window-titlebar__drag-region` 与 `window-titlebar__drag-spacer`，恢复整条顶部可拖拽体验。
    - 放大最大化按钮字形并优化按钮字形对齐。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. 顶部空白标题栏保持移除；
    2. 顶栏右侧最小化/最大化/关闭按钮点击生效；
    3. 顶栏空白区域可拖动窗口，双击可最大化/还原；
    4. 最大化按钮视觉尺寸较上一版更明显。

## 2026-04-23 — 窗口顶栏收口：去除系统空白标题栏，品牌顶到窗口最上端（Codex）

- 背景：
  - 用户要求参考 Cherry Studio 视觉，将 `LLM Wiki` 图标与标题直接贴顶显示，去掉窗口最上方空白标题栏。
- 实施说明：
  - 本轮涉及前端样式与窗口配置联动；任务集中在单前端视图与 `tauri.conf`，为避免跨域写冲突按串行方式实施。
- 本轮关键变更：
  - `src-tauri/tauri.conf.json`：
    - 主窗口启用 `"decorations": false`，关闭系统原生标题栏。
  - `web/src/App.tsx`：
    - 新增自定义顶栏 `window-titlebar`（`data-tauri-drag-region` 可拖拽区域）。
    - 顶栏左侧展示 `LLM_Wiki.png + LLM Wiki`，右侧新增最小化/最大化/关闭按钮（调用 Tauri window API）。
  - `web/src/styles.css`：
    - 新增 `app-root/window-titlebar` 相关样式。
    - Tauri 运行时隐藏侧边栏重复品牌区，避免出现双品牌头。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. 窗口顶部不再出现系统空白标题栏；
    2. `LLM Wiki` 图标与标题显示在窗口最上端；
    3. 顶栏可拖拽移动窗口，右上角最小化/最大化/关闭按钮可用。
- 未完成项 / 风险：
  - 本轮未执行 Windows 真机视觉验收截图；请以本地 `tauri:dev` 实际渲染为准。

## 2026-04-23 — 前端品牌图标收口：左上角使用 `LLM_Wiki.png`（Codex）

- 背景：
  - 用户要求将 `LLM_icon/LLM_Wiki.png` 作为应用启动图标，并显示在前端页面左上角品牌区。
- 实施说明：
  - 本轮为小范围前端视觉改动；当前运行策略未在本轮触发显式 subagent 授权，按最小影响原则由主控串行实施并记录。
- 本轮关键变更：
  - 新增前端静态资源：`web/src/assets/LLM_Wiki.png`（从 `LLM_icon/LLM_Wiki.png` 拷贝）
  - `web/src/App.tsx`：侧边栏 `sidebar__brand` 图标由内嵌 SVG 替换为 PNG `<img>`
  - `web/src/styles.css`：更新 `sidebar__brand-logo` 样式并新增 `sidebar__brand-logo-image`，保证图标自适应与圆角裁切
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅

## 2026-04-23 — 方向 A 收口：摄入分析卡 + 审批后落盘（Codex + 并行子代理）

- 背景：
  - 按用户确认顺序进入方向 A（两步摄入质量 + HITL 审核流）。
  - 目标是让 Inbox 直摄入改为 `preview -> 人工确认 -> apply`，不影响"加入队列"路径。
- 并行实施（符合 §14）：
  - 后端子代理（Rust 文件所有权）：`src-tauri/src/models.rs`、`src-tauri/src/state.rs`、`src-tauri/src/commands.rs`、`src-tauri/src/main.rs`
  - 前端子代理（TS 文件所有权）：`web/src/types.ts`、`web/src/tauri-client.ts`、`web/src/App.tsx`、`web/src/styles.css`、`web/src/app-utils.test.ts`
  - 主控串行步骤说明：
    - 子代理返回后，主控对 `state.rs` 做了二次收口，移除无关格式噪音并保留最小功能改动；该步骤为并行后的必要合并验收步骤。
- 本轮关键实现：
  - 后端新增摄入预览模型与命令：
    - `IngestPreview { preview_id, source_path, summary, entities, updated_pages }`
    - `preview_ingest_file(source_type, source_path, ocr_provider?)`
    - `apply_ingest_preview(preview_id)`
  - 后端新增一次性缓存：
    - `ingest_previews: Mutex<HashMap<String, CachedIngestPreview>>`
    - 预览阶段缓存 `source_content/summary/entities`，apply 阶段消费后失效。
  - 后端落盘主流程复用：
    - 新增 `complete_ingest_with_precomputed_analysis(...)`，`ingest_markdown` 与 `apply_ingest_preview` 共享写盘/链接注入/embedding/outbox 完成事件逻辑。
  - 前端 Inbox 直摄入改造：
    - 示例摄入、URL 摄入、文件摄入（含拖拽 direct 模式）统一先弹"摄入分析卡"，用户确认后才执行 apply。
    - 分析卡展示：摘要、实体、拟更新页；支持取消。
    - "加入队列"按钮与队列链路不变。
  - 新增最小测试：
    - Rust：`apply_ingest_preview_returns_error_when_preview_id_missing`
    - Rust：`preview_ingest_file_then_apply_succeeds_and_consumes_cache`
    - 前端：`createPreviewIngestFileArgs/createApplyIngestPreviewArgs` 参数映射与浏览器回退测试。
- 变更文件：
  - 后端：`src-tauri/src/models.rs`、`src-tauri/src/state.rs`、`src-tauri/src/commands.rs`、`src-tauri/src/main.rs`
  - 前端：`web/src/types.ts`、`web/src/tauri-client.ts`、`web/src/App.tsx`、`web/src/styles.css`、`web/src/app-utils.test.ts`
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（缺失可选依赖 `@rollup/rollup-linux-x64-gnu`）
  - `cd src-tauri && cargo check`：失败 ❌（`cargo: command not found`，当前 WSL 无 Rust 工具链）

## 2026-04-23 — Research .md 历史导出回归复核 + 收口 (Claude Code)

- 背景：
  - 用户完成 Deep Research 端到端实跑（城市复杂水网AI仿真研究），基线验证通过。
  - 执行 §18.2 Priority 3：`Research .md 导出历史任务回归复核`。
- 代码审查结论（历史 done 任务导出链路）：
  - `doneSavedPath` 初始化：`initialTask?.status === "done" ? (initialTask.saved_path ?? "") : ""`，历史任务正确读取 saved_path ✅
  - 按钮显示条件：`phase === "done" && (synthesisContent || doneSavedPath)`，历史任务满足（synthesisContent="" 但 doneSavedPath 非空）✅
  - `handleExportMd` 回退读取：`!content && doneSavedPath → fetchWikiPageDetail(doneSavedPath)`，路径解析逻辑（`resolve_wiki_page_candidate`）支持绝对路径，且安全约束确认 `wiki/research/` 文件在允许范围内 ✅
  - `saveResearchDoc` 后端实现：纯文件写盘，与格式无关，.md / .doc 均可 ✅
- 变更内容（仅注释修正，无逻辑变更）：
  - `src-tauri/src/commands.rs`：`save_research_doc` 注释从"Word 文档（HTML .doc 格式）"改为"导出文件（.md 或其他格式）"；错误提示从"写入 Word 文件失败"改为"写入文件失败"
  - `web/src/tauri-client.ts`：`saveResearchDoc` JSDoc 注释同步更新
  - `agents.md` §18.1/§18.2/§18.4：补记录、标绿 TODO、更新基线日期

## 2026-04-22 — SearXNG PS7 自检通过收口 (Codex)

- 用户回传结果：
  - `.\scripts\run_searxng_windows.ps1 -Recreate`：启动流程通过；
  - `.\scripts\verify_searxng_windows.ps1`：已自检通过（PS7）。
- 收口结论：
  - SearXNG Windows 脚本链路（启动 + 自检）闭环完成；
  - 当前轮次按用户指令不启动 App 做 Deep Research 实跑，应用内验证延后到后续轮次。
- 本轮文档同步：
  - `README.md`（服务依赖与 SearXNG/Clipper 指南）
  - `docs/交接状态卡.md`
  - `agents.md`

## 2026-04-22 — SearXNG Windows 启动脚本健壮性修复 (Codex)

- 背景：
  - 用户在 PS7 执行 `run_searxng_windows.ps1 -Recreate` 后，`verify_searxng_windows.ps1` 报错：
    - `Exception calling "GetResult" with "0" argument(s): "An error occurred while sending the request."`
  - 旧脚本问题：
    - `run` 脚本未等待容器就绪，且未检查子脚本退出码；
    - `verify` 脚本异常信息过于抽象，难以定位是容器未就绪、端口不可达还是容器启动失败。
- 修复：
  - `scripts/verify_searxng_windows.ps1`
    - 新增异常链路展开函数 `Get-ExceptionChainText`，输出 inner exception 细节。
    - `Invoke-JsonNoProxy` 对 `SendAsync().GetResult()` 加强捕获，明确输出 URL + 请求错误原因。
  - `scripts/run_searxng_windows.ps1`
    - 新增容器就绪等待：`Wait-SearxngReady`（轮询 `docker inspect` + 本地 HTTP 探测）。
    - 新增命令退出码校验：`Ensure-CommandSucceeded`。
    - 增加失败回退：推荐配置启动失败时，自动回退镜像默认配置再试一次。
    - 若自检失败，打印 `docker logs --tail 120` 并返回非零退出码。

## 2026-04-22 — SearXNG 双主题实测与 Windows 推荐配置模板 (Codex)

- 用户要求：按顺序执行两条
  1. 用中英文主题验证 SearXNG 参数优化效果
  2. 提供 Windows 下可落地的配置优化方案
- 第 1 条执行结果（PS7）：
  - 中文主题：`大模型 RAG 框架演进`
    - primary: `results.count = 15`
    - fallback: `results.count = 24`
    - best: fallback（`language=all` + `categories=general,news`）
  - 英文主题：`rust async runtime best practices`
    - primary: `results.count = 23`
    - fallback: `results.count = 20`
    - best: primary（`language=auto` + `categories=general`）
  - 结论：当前实例召回不低，参数优化已生效；但存在一批不稳定引擎（限流/超时/解析错误）。
- 第 2 条实施：
  - 新增 `configs/searxng/settings.windows.example.yml`
  - 新增 `scripts/run_searxng_windows.ps1`
  - `README.md` 补充 PS7 下一键启动与模板路径说明
- 兼容性补丁：
  - `scripts/verify_searxng_windows.ps1` 去除 `??` 写法，避免 Windows PowerShell 解析报错。

## 2026-04-22 — README 服务依赖与配置说明收口 (Codex)

- 本轮变更：`README.md` 新增"模块依赖与服务速查"表、SearXNG 与 Clipper 自检命令。

## 2026-04-22 — SearXNG 检索参数优化（提升召回与可诊断性）(Codex)

- 本轮关键决策：后端"多参数档位自动回退"策略。
- 变更文件：`src-tauri/src/state.rs`（新增多档位搜索+3条单测）、`scripts/verify_searxng_windows.ps1`。

## 2026-04-22 — SearXNG 自检结果回传与脚本可读性增强 (Codex)

- 本轮改进：`scripts/verify_searxng_windows.ps1` 新增 `Format-UnresponsiveEngine`，results.count=0 时给出明确 warning。

## 2026-04-22 — Clipper 实测收口通过 + 状态命令真实性修复 (Codex)

- 本轮变更：`clip_server.rs` 新增运行态原子标记 `CLIP_SERVER_RUNNING`；`commands.rs` `get_clip_server_status` 改为读取真实状态。

## 2026-04-22 — Clipper 端到端复核增强（状态可诊断 + Windows 自检脚本）(Codex)

- 本轮变更：`clip_server.rs` `/status` 补充 vault_open/vault_path；`extension/popup.js` 增强连接检查；新增 `scripts/verify_clipper_windows.ps1`。

## 2026-04-22 — Claude 限流后接手收口（Research 导出 .md 历史任务兜底）(Codex)

- 本轮修复：`web/src/App.tsx` 导出按钮显示条件从 `synthesisContent` 改为 `synthesisContent || doneSavedPath`，修复历史任务不显示导出按钮问题。

## 2026-04-21 — Deep Research 删除参数映射与时间格式一致性修复 (Codex)

- 修复：`delete_research_task` 参数映射问题（改为 `payload: serde_json::Value`）；Research 时间展示改为北京时间格式。

## 2026-04-21 — Deep Research 任务删除与时间戳展示增强 (Codex)

- 关键变更：后端 `db_delete_research_task`、`state::delete_research_task`（仅终态可删，可选同步删 Wiki）；前端删除按钮、原生 confirm 对话框。

## 2026-04-21 — Deep Research Word 导出无响应修复 (Codex)

- 修复：Tauri WebView 下 Blob 下载失效；改为后端 `save_research_doc` 命令 + 文件保存对话框。

## 2026-04-21 — Deep Research 失败可恢复与日志可见性修复 (Codex)

- 修复：报告生成失败改为重试 1 次；失败态任务卡展示日志区（红色边线）。

## 2026-04-21 — SearXNG 本地搜索激活与 Deep Research 错误可见化增强 (Codex)

- 关键变更：SearXNG URL 规范化、主/回退双端点策略、`validate_search_config` 前置校验、`do_search` 改为返回 Result；新增 5 条单测。

## 2026-04-21 — Clipper JSON 转义故障修复与可诊断化收口 (Codex)

- 修复：`clip_server.rs` 统一路径标准化 `normalize_display_path`；`extension/popup.js` 新增 `parseJsonResponse`。

## 2026-04-21 — Deep Research 代码质量重构至 A 级 (Claude Code)

- 目标：将 Gemini 编写的 Deep Research 代码质量从 B+ 提升至 A，参考 dzhng/deep-research 最优实践。
- 关键决策：共享 HTTP Client、并发搜索、Learnings 模型、Token 管控、语言自适应 Prompt、幂等 Cancel、路径统一、前端错误可见化。
- 自评分：**A- 93/100**
- 验证：`cargo check` ✅，`cargo test` 141/141 ✅，`npm run typecheck` 0 errors ✅

## 2026-04-20 — Deep Research 模块全面对标 dzhng/deep-research & 移植包 D 收口 (Gemini)

- 目标：迭代式研究循环、depth/breadth 控制、移植包 D（模板初始化、网页剪藏、Word 下载）。
- 验证：`cargo check` ✅（2 warnings），`npm run typecheck` ✅
## 2026-04-27 — 方向 H H4：Research Mode 检索增强（Claude）

- 背景：H3 验证通过后，按计划推进 H4——在生成草稿时增加可选的"检索增强"模式，用更丰富的 wiki 内容上下文提升 LLM 输出质量。
- 本轮完成项：
  - 后端 `state.rs`：
    - `generate_ai_wiki_markdown_draft_impl` 新增 `research_mode: bool` 参数
    - 新增 `extract_content_after_frontmatter(text, limit)` 辅助函数，跳过 YAML frontmatter 提取正文
    - `research_mode=true` 时：搜索条数 5→8，读取 wiki 文件实际正文前 400 字（原为摘要 120 字）
    - `generate_agent_draft_impl` 新增 `research_mode: bool`，事件消息加 `research: on` 标记
  - 后端 `commands.rs`：`generate_agent_draft` 新增 `research_mode: Option<bool>`
  - 前端 `tauri-client.ts`：`generateAgentDraft` / `createGenerateAgentDraftArgs` 支持可选 `researchMode`
  - 前端 `App.tsx`：新增 `agentResearchMode` state；composer-tools 区新增"检索增强"勾选框
  - 前端 `styles.css`：`.agent-studio__research-toggle` 样式
  - 测试：`createGenerateAgentDraftArgs` 断言更新含 `researchMode`
- 验证：`npm run typecheck` ✅；`cargo test` 待 Windows 复核
- 下一步：用户 Windows 验证后，由用户定 H5 方向

## 2026-04-27 — 方向 H H3 收口：记忆表单 UX 修复（Claude）

- 背景：用户 Windows 测试 H3，发现两个问题：
  1. 记忆"保存记忆"按钮灰色不可点击：表单要求键+值均非空，用户只填了值；
  2. 点击`+ 添加`后，按钮四个字竖排，布局难看。
- 本轮完成项：
  - fix：记忆键设为可选，若留空自动取值的前 20 字符作为键（`0597e54`）
  - fix：记忆表单布局改为两行——输入行（键+值并排）+ 按钮独占一行（`2540c2b`）
  - fix：`extractSkillKeyFromEventMessage` 正则补排全角右括号 `）`，避免解析出 `writer）`（含于 `730818b`）
- 验证：`npm run typecheck` ✅；用户 Windows 回传通过 ✅
- 下一步：H4 Research 联动（generate_agent_draft 增加 research_mode）

## 2026-04-27 — 方向 H H3-4：草稿头显示已应用 skill（最小增量，Codex）

- 背景：
  - 用户要求在剩余 5 小时额度较低的情况下做“最小下一步开发”，便于立即交给 Claude 接力。
  - 结合上一轮 H3-2/H3-3，补齐一个低风险可见性改进：直接在右侧草稿头显示“本次草稿使用的 skill key”。
- 本轮完成项：
  - 前端（React/TS）：
    - 新增 `extractSkillKeyFromEventMessage(...)`，从 run 事件消息解析 `skill:` 标记。
    - 新增 `agentDraftAppliedSkillKey` 计算逻辑（从当前 run 最新事件倒序扫描）。
    - 在草稿头元信息区展示 `· skill:<key>`，仅在解析到 skill 时显示。
  - 结果：
    - 用户无需展开调试区，即可在主工作区确认当前草稿使用的 skill。
- 并行执行说明（§14）：
  - 本轮为最小前端增量，按当前策略采用主控串行实现，未启用 `spawn_agent`。
- 自动化验证（WSL）：
  - `cd web && npm run typecheck`：通过 ✅
- 本轮改动文件：
  - 前端：
    - `web/src/App.tsx`
  - 文档：
    - `docs/实施过程记录.md`
    - `docs/交接状态卡.md`
    - `docs/dev-status.md`
- Windows PowerShell 手动验证步骤（H3-4）：
  - `Set-Location E:\llm-wiki\web; npm run typecheck`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 切换任意 skill 并生成草稿；
    2. 观察右侧草稿标题下方元信息，确认出现 `skill:<key>`。
- 未完成项：
  - TODO：由 Claude 基于当前 H3-4 断点进入 H4 融合（Ask/Research/Lint 与 Agent Studio 联动）。

## 2026-04-27 — 方向 H H3-3：技能可切换修复 + Agent Studio 排布收口（Codex）

- 背景：
  - 用户在 Windows 侧回传 H3-2 验证截图，确认生成与删除正常，但指出两个问题：
    1. 多技能场景下“生效技能”选择不自由，表现为像是被固定到一个技能；
    2. 调试区（红框）控件排布错位。
  - 用户要求基于 figma 风格做本模块排版优化并收口。
- 本轮完成项：
  - 交互修复（技能选择）：
    - 技能区新增明确的“生效技能”下拉选择器（支持“不使用技能模板”）。
    - 保留卡片点击切换作为快捷入口，形成“双入口切换”。
    - 修复自动回填逻辑：刷新技能列表时不再强制回退到第一项，避免“被固定”的体感。
    - 新建/更新技能后自动切换到该技能，便于连续验证。
    - 新增生效技能本地持久化（重开页面后保留上次选择）。
  - 排版修复（调试区错位）：
    - `.agent-studio__create` 网格改为 4 列，匹配实际控件数（label + select + input + button）。
    - `.agent-studio__actions` 网格改为 3 列，去除历史遗留的 7 列模板。
    - 增加 `skills` 头部与下拉控件样式，移动端下自动折行与全宽显示。
  - figma 技能说明：
    - 已加载并遵循 `figma-generate-design` 的结构化布局与组件化编排原则进行前端排版优化。
    - 当前环境未提供可写入的 Figma MCP `use_figma` 调用面，因此本轮以代码侧落地视觉修复为主。
- 自动化验证（WSL）：
  - `cd src-tauri && cargo fmt --all`：通过 ✅
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（`@rollup/rollup-linux-x64-gnu` 缺失）
- 本轮改动文件：
  - 前端：
    - `web/src/App.tsx`
    - `web/src/styles.css`
  - 文档：
    - `docs/实施过程记录.md`
    - `docs/交接状态卡.md`
    - `docs/dev-status.md`
- Windows PowerShell 手动验证步骤（H3-3）：
  - 命令：
    - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
    - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 创建至少两个技能，使用下拉与卡片点击分别切换生效技能，确认可自由来回切换。
    2. 选择“不使用技能模板”，再生成一版草稿，确认状态消息不带 skill 标记。
    3. 展开调试面板，确认“事件级别/事件内容/追加事件”一行不再错位，窄屏下可正常折行。
- 未完成项：
  - TODO：等待用户回传 H3-3 Windows 验证结果后，进入 H4 融合阶段。

## 2026-04-27 — 方向 H H3-2：技能模板接入 Draft 生成链路（Codex）

- 背景：
  - 用户回传截图确认当前 Agent Studio 功能可用，并要求“继续”。
  - 基于已完成 H3-1，本轮落地 H3-2：让技能模板不只可管理，还能真正影响 Draft 生成提示词。
- 本轮完成项：
  - 后端（Rust/Tauri）：
    - `generate_agent_draft` 命令新增可选参数 `skill_key`。
    - `generate_agent_draft_impl` 新增 `skill_key` 入参，按 key 查询技能模板并注入 prompt。
    - `generate_ai_wiki_markdown_draft_impl` 新增 `skill_prompt` 注入段（“当前启用技能模板”）。
    - 新增 DB 查询：`get_agent_skill_by_key`。
    - run event 在技能生效时写入 `skill: <key>` 便于追踪。
    - 新增状态层测试：`agent_draft_generate_with_skill_injects_skill_prompt`（验证 prompt 确实包含技能模板）。
  - 前端（React/TS）：
    - `generateAgentDraft` API 支持传入 `skillKey`，参数构造器同步更新。
    - Agent Studio 技能列表新增“当前生效技能”选择（点击技能即可切换，显示“生效中”）。
    - 发送主题与“基于当前 Run 重写”都会携带当前生效技能 key。
    - 状态消息增加 skill 标记，便于人工确认本次草稿生成所用技能。
    - 前端测试断言更新：`createGenerateAgentDraftArgs` 覆盖 `skill_key`。
- 并行执行说明（§14）：
  - 受当前运行策略约束，本轮仍采用主控串行实现（未启用 `spawn_agent`），已按协议显式记录。
- 自动化验证（WSL）：
  - `cd src-tauri && cargo fmt --all`：通过 ✅
  - `cd web && npm run typecheck`：通过 ✅
  - `cd src-tauri && cargo check`：失败 ❌（`pkg-config/gtk` 系统依赖缺失）
  - `cd web && npm run test -- --run`：失败 ❌（`@rollup/rollup-linux-x64-gnu` 缺失）
- 本轮改动文件：
  - 后端：
    - `src-tauri/src/db.rs`
    - `src-tauri/src/state.rs`
    - `src-tauri/src/commands.rs`
  - 前端：
    - `web/src/tauri-client.ts`
    - `web/src/App.tsx`
    - `web/src/styles.css`
    - `web/src/app-utils.test.ts`
  - 文档：
    - `docs/实施过程记录.md`
    - `docs/交接状态卡.md`
    - `docs/dev-status.md`
- Windows PowerShell 手动验证步骤（H3-2）：
  - 命令：
    - `Set-Location E:\llm-wiki\src-tauri; cargo test`
    - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
    - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 在 Agent Studio 先创建两个技能模板（如 `writer` 与 `reviewer`），并切换“当前生效”。
    2. 发送同一主题各生成一次草稿，确认状态提示包含对应 `skill` 标记。
    3. 展开调试 Events，确认生成事件含 `skill: writer/reviewer`。
    4. 回归验证：`Draft / Diff / Citations`、Flow 暂停/继续、审批写盘保持可用。
- 未完成项：
  - TODO：等待用户 Windows 回传 H3-2 验证结果。
  - TODO：后续可考虑在 UI 中展示“本 draft 使用的 skill key”字段（目前通过事件与状态消息可见）。

## 2026-04-27 — 方向 H H3-1：Agent Skills 模板资产基础（Codex）

- 背景：
  - 用户要求从当前断点“继续开发”，并确认忽略本地协作文件 `opencode.json`。
  - 当前 `B2-Flow` 仍处于 `reviewing`（待 Windows 复核），本轮选择并行不冲突的后续增量：先补 H3 技能化的基础资产层（不改现有 draft 生成链路）。
- 本轮完成项：
  - 后端（Rust/Tauri）：
    - 新增 `agent_skills` 表与索引（`skill_key` 唯一、`version` 递增）。
    - 新增 DB 能力：`upsert_agent_skill / list_agent_skills / delete_agent_skill`。
    - 新增状态层与命令层能力：`upsert/list/delete agent skill`，并在 `main.rs` 注册命令。
    - 新增最小单元测试：
      - `db`: `agent_skill_upsert_list_delete_work`
      - `state`: `agent_skill_crud_impl_works`
  - 前端（React/TS）：
    - 新增 `AgentSkillItem` 类型。
    - 新增 tauri-client API 与参数构造器：
      - `upsertAgentSkill / listAgentSkills / deleteAgentSkill`
      - `createUpsertAgentSkillArgs / createListAgentSkillsArgs / createDeleteAgentSkillArgs`
    - Agent Studio 左栏新增“技能模板”面板：
      - 新建技能（`skill_key + prompt_template`）
      - 技能列表（显示 `version` 与更新时间）
      - 删除技能
    - 新增前端测试断言覆盖技能命令参数构造器。
- 并行执行说明（§14）：
  - 受当前运行策略约束，本轮仍采用主控串行实现（未启用 `spawn_agent` 并行写入）。
  - 已在记录中显式声明，不伪造并行执行结果。
- 自动化验证（WSL）：
  - `cd src-tauri && cargo fmt --all`：通过 ✅
  - `cd src-tauri && cargo check`：失败 ❌（`pkg-config/gtk` 相关系统依赖缺失，WSL 环境问题）
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（`@rollup/rollup-linux-x64-gnu` 缺失）
  - `cd web && npm run build`：失败 ❌（同上）
- 本轮改动文件：
  - 后端：
    - `src-tauri/src/models.rs`
    - `src-tauri/src/db.rs`
    - `src-tauri/src/state.rs`
    - `src-tauri/src/commands.rs`
    - `src-tauri/src/main.rs`
  - 前端：
    - `web/src/types.ts`
    - `web/src/tauri-client.ts`
    - `web/src/App.tsx`
    - `web/src/styles.css`
    - `web/src/app-utils.test.ts`
  - 文档：
    - `docs/实施过程记录.md`
    - `docs/交接状态卡.md`
    - `docs/dev-status.md`
- Windows PowerShell 手动验证步骤（H3-1）：
  - 命令：
    - `Set-Location E:\llm-wiki\src-tauri; cargo test`
    - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
    - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 进入 `Agent Studio`，确认左栏出现“技能模板”区域。
    2. 新建技能：`writer` + 任意模板，保存后应显示 `v1`。
    3. 使用相同 `skill_key=writer` 再保存一次，版本应递增为 `v2`。
    4. 删除该技能，列表应同步移除并提示成功。
    5. 回归验证：原有 `Draft / Diff / Citations` 与 Flow 暂停/继续链路不受影响。
- 未完成项：
  - TODO：将技能模板接入 draft 生成时的可选策略路由（H3-2）。
  - TODO：等待用户 Windows 回传（B2-Flow + H3-1 一并复核）。

## 2026-04-27 — 方向 H B2-Flow 收口到可验收状态（待用户 Windows 复核）（Codex）

- 背景：
  - 用户要求“按计划做完并收口”，本轮在既有 B2-Lite/B2-Review 基础上继续补全 B2-Flow，并同步完成交接文档收口。
  - 收口范围：`B2-Lite`、`B2-Review`、`B2-Flow`（含暂停/继续控制与事件可追踪）。
- 本轮完成项（状态归并）：
  - `B2-Lite`：双栏主结构（左对话 + 右草稿）已稳定可用。
  - `B2-Review`：`Draft / Diff / Citations` 审阅链路已可用。
  - `B2-Flow`：已达到可验收状态，包含：
    - 结构先行展示（先出章节骨架）。
    - 正文增量渲染（逐步填充内容）。
    - 生成流程控制（暂停 / 继续 / 完成 / 重播）。
    - Flow 控制动作回写 run 事件（info）。
- WSL 自动化验证结果（占位）：
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（`@rollup/rollup-linux-x64-gnu` 可选依赖缺失）
  - `cd web && npm run build`：失败 ❌（同上）
  - 说明：当前失败为 WSL 可选依赖缺失导致，需以 Windows 复核为准。
- Windows PowerShell 手动验证步骤（Agent Studio Flow 控制）：
  - 命令：
    - `Set-Location E:\llm-wiki\src-tauri; cargo test`
    - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
    - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 进入 Agent Studio，发送一个新主题，确认右栏先出现结构骨架，再逐步渲染正文。
    2. 生成过程中点击“暂停”，确认增量内容停止增长，并在 Events 出现“草稿流式渲染已暂停”。
    3. 点击“继续”，确认正文继续增量渲染直至完成，并在 Events 出现“草稿流式渲染已继续/已完成”。
    4. 在生成完成后切换 `Draft / Diff / Citations`，确认三标签内容一致可用。
    5. 执行“写入 Wiki”审批路径，确认仍有确认/冲突提示，不出现静默覆盖。
- 本轮主要改动文件：
  - 前端：
    - `web/src/App.tsx`
    - `web/src/styles.css`
    - `web/src/types.ts`
  - 配置：
    - `src-tauri/tauri.conf.json`
  - 文档：
  - `docs/实施过程记录.md`
  - `docs/交接状态卡.md`
  - `docs/dev-status.md`
- 当前结论：
  - `B2-Flow` 已完成到可验收状态，当前接力状态应为 `reviewing`，等待用户 Windows 复核回传。
- 未完成项：
  - TODO：等待用户回传 Windows 自动化与手测结果。
  - TODO：若复核失败，按失败点补最小修复并回填记录。

## 2026-04-27 — 方向 H B2 三阶段计划落盘 + B2-Lite 开工（Codex）

- 背景：
  - 用户确认采用「方案 B 结合少量 A」路线，并明确选择 **B2 双栏（左对话 + 右草稿预览）**。
  - 用户要求：先把按顺序推进计划写入可交接文档，再直接开始实施。
- 计划落盘（可供 Claude/Codex/Gemini 接力）：
  - `docs/参考项目差距与移植清单.md`：
    - 新增 `§8.7 B2 双栏改造路线`，固定三阶段顺序：
      1. `B2-Lite`（先可用）
      2. `B2-Review`（审批优先）
      3. `B2-Flow`（流式增强）
    - 每阶段明确目标、文件范围、验收标准与交接要求。
  - `docs/交接状态卡.md`：
    - 切换到新轮次 `R20260427-H3-B2-Phase1`，状态置为 `running`。
  - `docs/dev-status.md`：
    - 活跃 TODO 置顶为 `方向 H：Agent Studio B2 三阶段改造`。
- 本轮执行策略（先做 Phase 1）：
  - 代码范围：前端优先（`web/src/{App.tsx,styles.css,types.ts}`）+ 可选窗口默认宽度调整（`src-tauri/tauri.conf.json`）。
  - Phase 1 验收目标：
    1. Agent Studio 变为双栏主布局（左对话，右预览）。
    2. 单入口触发「创建 run + 生成 draft」。
    3. 写入/重写/丢弃动作在右栏完成。
- 并行约束说明（§14）：
  - 本轮运行策略存在限制：当前主控策略不允许在未获用户显式授权时调用 `spawn_agent`。
  - 因此本轮暂以“最接近可用方式”串行推进（文档已显式记录），后续轮次可由 Claude 在有授权条件下恢复并行拆分。
- 本轮实现（已完成）：
  - B2-Lite（Phase 1）：
    - Agent Studio 主布局重排为双栏：
      - 左栏：记忆芯片、对话线程、发送入口。
      - 右栏：草稿预览、写入/重写/丢弃动作。
    - 对话入口改为单步闭环：发送主题后自动执行 `startAgentRun + generateAgentDraft`。
    - 调试动作（仅创建 run / 追加事件 / 结束 run）下沉为可折叠调试区，主流程不再被技术参数占据。
    - Tauri 默认窗口初始尺寸提升：`1280x900 -> 1480x940`（更适配双栏）。
  - B2-Review（Phase 2，已开工）：
    - 右栏增加三标签：`Draft / Diff / Citations`。
    - `Diff`：复用 `buildWikiLineDiff`，优先拉取 `existing_path` 对应页面完整内容作为对比基线；失败时回退 preview。
    - `Citations`：展示草稿内 `[[wiki-link]]` 提取结果，作为引用检查入口。
  - B2-Flow（Phase 3，起步）：
    - 右栏在草稿生成阶段增加骨架占位动画，避免生成中空白。
  - 并行执行记录（§14）：
    - 用户补充要求后，主控已尝试启动前端样式子代理并行处理。
    - 子代理会话未返回有效产物，已显式 shutdown；主控接管并完成样式实现，不伪造并行结果。
- 改动文件：
  - 前端：
    - `web/src/types.ts`
    - `web/src/App.tsx`
    - `web/src/styles.css`
  - 配置：
    - `src-tauri/tauri.conf.json`
  - 文档：
    - `docs/参考项目差距与移植清单.md`
    - `docs/交接状态卡.md`
    - `docs/dev-status.md`
    - `docs/实施过程记录.md`
- 自动化验证：
  - WSL：
    - `cd web && npm run typecheck`：通过 ✅
    - `cd web && npm run test -- --run`：失败 ❌（`@rollup/rollup-linux-x64-gnu` 缺失）
    - `cd web && npm run build`：失败 ❌（同上）
  - 说明：
    - 当前失败为 WSL 可选依赖缺失，不代表业务逻辑失败；以 Windows 验证为准。
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证步骤：
    1. 进入 Agent Studio，确认页面是左右双栏（左对话、右草稿）。
    2. 在左侧输入主题并发送，确认自动创建 run 并生成 draft。
    3. 右侧切换 `Draft / Diff / Citations`，确认三标签可用。
    4. 点击 `写入 Wiki` 仍需确认弹窗，冲突时有覆盖提示。
    5. 缩小窗口到窄屏，确认双栏自动转单列且可操作。
- 额外动作（按用户授权执行）：
  - 已从 GitHub 安装 3 个前端设计相关 skill（全局目录，不改仓库代码）：
    - `figma-use`
    - `figma-generate-design`
    - `figma-implement-design`
  - 安装路径：`/home/hhb/.codex/skills/*`
- 未完成项（继续按计划自动推进）：
  - TODO：B2-Flow 下一步接入“结构先行 + token 流式填充”，并支持中断/续写控制。
  - TODO：Windows 端全量回归验证待用户回传。

## 2026-04-25 — 方向 H H1-1：时间显示优化 + Draft 生成/审批链路（Codex + 并行子代理）

- 背景：
  - 用户手测确认 H0 基础链路已通，并要求“优化点做下，继续开发”。
  - 优化点明确为：
    1. Agent Studio 时间显示从毫秒值改为可读时间。
    2. 结束 run 自动写系统事件，避免 events 空白。
  - 同时推进 H1 第一段：主题 → Draft 生成 → Draft 审批写盘。
- 并行拆分（§14）：
  - 后端子代理：`src-tauri/src/{db,models,state,commands,main}.rs`
  - 前端子代理：`web/src/{types,tauri-client,App,styles,app-utils.test}.ts(x)`
  - 主控：接口契约联调、缺口修补（字段对齐/状态判断）、验证与文档收口。
- 本轮实现：
  - 优化项 1（时间可读化）：
    - Agent Studio 的 run/event 时间展示统一改为 `formatLintCheckedAt(...)`。
    - run 列表、event 列表、draft 列表/详情均使用可读时间。
  - 优化项 2（结束 run 自动事件）：
    - `complete_agent_run` 改为事务内同时写入系统事件 `agent_run_events(level='info')`，消息包含状态变更。
  - H1 第一段（Draft 链路）：
    - 新增后端模型：`AgentDraftItem`。
    - 新增命令：
      - `generate_agent_draft(run_id, topic)`（生成草稿，写入 `agent_drafts`）
      - `list_agent_drafts(run_id, limit)`（草稿列表）
      - `approve_agent_draft(draft_id)`（审批写盘到 `vault/wiki`，更新 `wiki_pages + FTS`，草稿状态置 `applied`）
    - 新增前端 Agent Studio 草稿工作区：
      - 生成 Draft
      - Draft 列表
      - Draft 预览
      - 审批写盘
    - 审批后同步刷新 runs/events/drafts。
  - 主控联调修补：
    - 修复 `create_wiki_page_with_ai_impl` 复用草稿逻辑后遗漏 `now_ms` 的编译缺口。
    - 修复 Draft 字段对齐：前端 `topic` 改为 `title`，与后端模型一致。
    - 修复审批按钮禁用条件：兼容 `approved/applied` 两种终态字符串。
- 改动文件：
  - 后端：
    - `src-tauri/src/db.rs`
    - `src-tauri/src/models.rs`
    - `src-tauri/src/state.rs`
    - `src-tauri/src/commands.rs`
    - `src-tauri/src/main.rs`
  - 前端：
    - `web/src/types.ts`
    - `web/src/tauri-client.ts`
    - `web/src/App.tsx`
    - `web/src/styles.css`
    - `web/src/app-utils.test.ts`
- 自动化验证：
  - WSL：
    - `cd web && npm run typecheck`：通过 ✅
    - `cd web && npm run test -- --run`：失败 ❌（缺 `@rollup/rollup-linux-x64-gnu` 可选依赖）
    - `cd web && npm run build`：失败 ❌（同上）
    - `cd src-tauri && cargo check`：失败 ❌（缺 `pkg-config` / GTK 相关系统依赖）
    - `cd src-tauri && cargo fmt`：通过 ✅
  - 说明：
    - 失败属于 WSL 环境依赖缺失，不是本轮功能逻辑错误；最终以 Windows 验证结果为准。
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki\src-tauri; cargo check; cargo test`
  - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. Agent Studio 创建 run，确认 run 时间显示为可读格式（非纯毫秒值）。
    2. 点击结束 run，确认 events 自动新增系统状态变更事件。
    3. 选中 run，点击“生成 Draft”，确认 Draft 列表出现条目。
    4. 打开 Draft 预览，点击“审批写盘”，确认状态变为 `applied`，并可在 Wiki 列表中找到新增页面。
- 未完成项：
  - TODO：H1 下一段需补“审批前差异对比（draft vs 目标页）”与显式确认弹窗。
  - TODO：草稿预览当前为纯文本 `pre`，可在后续切到只读 Markdown 渲染（`marked + DOMPurify`）。
  - TODO：Windows 侧完整自动化与端到端验证待用户回传。

## 2026-04-25 — 方向 H H0：Agent Studio 脚手架首轮实现（Codex + 并行子代理）

- 背景：
  - 用户确认“开始做”，并明确若中途受限可交由 Claude 继续开发。
  - 本轮按 `agents.md §14` 执行并行拆分：后端子任务 + 前端子任务，主控负责合并联调与文档收口。
- 并行拆分（文件所有权）：
  - 后端子代理（Rust）：`src-tauri/src/{db,models,state,commands,main}.rs`
  - 前端子代理（React/TS）：`web/src/{types,tauri-client,App,styles,app-utils.test}.ts(x)`
  - 主控（Codex）：接口契约校对、联调修正、验证与交接文档。
- H0 实现结果：
  - 后端：
    - 新增 4 张表（H0 实际使用前两张）：
      - `agent_runs`
      - `agent_run_events`
      - `agent_drafts`
      - `agent_memories`
    - 新增 DB 最小闭环函数：
      - `start_agent_run`
      - `append_agent_run_event`
      - `list_agent_runs`
      - `list_agent_run_events`
      - `complete_agent_run`
    - 新增 state impl：
      - `start_agent_run_impl`
      - `append_agent_run_event_impl`
      - `list_agent_runs_impl`
      - `list_agent_run_events_impl`
      - `complete_agent_run_impl`
    - 新增并注册 Tauri 命令：
      - `start_agent_run`
      - `append_agent_run_event`
      - `list_agent_runs`
      - `list_agent_run_events`
      - `complete_agent_run`
    - 新增 Rust 测试：
      - `agent_run_lifecycle_and_event_listing_work`
      - `agent_auxiliary_tables_exist_after_init`
      - `agent_run_running_status_keeps_completed_at_empty`
      - `agent_run_h0_impl_lifecycle_works`
  - 前端：
    - 模块体系新增 `ModuleId = "agent"`。
    - 左侧导航改为三组：`核心 / 运营 / Agent`，`Agent Studio` 独立分组展示并拉开间距。
    - 新增 Agent Studio H0 页面（最小闭环）：
      - 创建 run
      - 追加事件
      - 结束 run（applied/failed）
      - 浏览 run 列表与事件列表
      - 手动刷新
    - 新增 tauri-client API：
      - `startAgentRun`
      - `appendAgentRunEvent`
      - `listAgentRuns`
      - `listAgentRunEvents`
      - `completeAgentRun`
    - 新增/扩展前端测试：参数映射与浏览器模式回退。
- 主控联调修正（关键）：
  - 修正前后端返回值契约不一致：
    - 后端 `start_agent_run` 返回 `run_id: i64`，前端已改为 `Promise<number | null>`。
    - 后端 `append/complete` 返回 `()`，前端已改为布尔成功态。
  - 修正 H0 终态选项：
    - 前端 `complete` 状态从 `reviewing/applied/failed` 收敛为 `applied/failed`，避免触发后端最小校验错误。
- 改动文件：
  - 后端：
    - `src-tauri/src/db.rs`
    - `src-tauri/src/models.rs`
    - `src-tauri/src/state.rs`
    - `src-tauri/src/commands.rs`
    - `src-tauri/src/main.rs`
  - 前端：
    - `web/src/types.ts`
    - `web/src/tauri-client.ts`
    - `web/src/App.tsx`
    - `web/src/styles.css`
    - `web/src/app-utils.test.ts`
- 自动化验证：
  - WSL：
    - `cd web && npm run typecheck`：通过 ✅
    - `cd web && npm run test -- --run`：失败 ❌（缺少 `@rollup/rollup-linux-x64-gnu` 可选依赖）
    - `cd web && npm run build`：失败 ❌（同上）
    - `cd src-tauri && cargo check`：失败 ❌（WSL 缺少 `pkg-config` 与 GTK 相关系统依赖）
  - 说明：
    - 本轮环境失败为 WSL 依赖缺失，不是 H0 代码语义错误；最终以 Windows 端验证为准。
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki\src-tauri; cargo check; cargo test`
  - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run; npm run build`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证步骤：
    1. 左侧导航确认出现独立分组 `Agent`，且 `Agent Studio` 与核心/运营分组有明显间隔。
    2. 进入 `Agent Studio`，输入主题并点击“创建 Run”，应出现 run 列表项。
    3. 选择 run，追加 `info/warn/error` 事件，事件列表应新增记录。
    4. 选择 `applied` 或 `failed` 后点击“结束 Run”，run 状态应更新。
    5. 点击“刷新”后，run 与事件列表应保持一致。
- 未完成项（可交由 Claude 继续）：
  - TODO：H0 仅是脚手架，`agent_drafts/agent_memories` 尚未接入业务写入链路（H1/H2 继续）。
  - TODO：状态机目前为最小校验（`running/applied/failed`），后续可升级为 DB CHECK + 迁移。
  - TODO：Agent Studio 暂未接入 outbox 事件推送，仅支持手动刷新（后续可加轮询/订阅）。

## 2026-04-25 — 方向 H 开工前规划落盘（Codex）

- 背景：
  - 用户确认进入新开发方向，并要求“开始前先写入规划文件”。
  - 本轮目标不是编码，而是将方向 H（Agent Studio）蓝图写入统一事实源，保障后续 Claude Code / Codex / Gemini 接手一致。
- 规划写入：
  - `docs/参考项目差距与移植清单.md`
    - 新增 §8「新开发方向 H（2026-04-25 补充）：Agent Studio 移植蓝图」。
    - 明确 Rust 80/20 策略（Rust 优先、机制借鉴补充）。
    - 明确候选与取舍：`rig`、`modelcontextprotocol/rust-sdk`、`sqlite-memory` 为 P0；`AutoAgents`、`kowalski`、`openfang/agent-os` 为 P1。
    - 明确 H0~H4 里程碑、导航分组约束、首轮文件所有权与并行拆分规则。
  - `docs/交接状态卡.md`
    - 更新当前接力目标为方向 H H0（开工前规划完成，待编码执行）。
  - `docs/dev-status.md`
    - 更新活跃 TODO：方向 H H0 置顶为下一优先级。
- 调研证据（Rust 优先）：
  - `https://github.com/0xPlaygrounds/rig`
  - `https://github.com/modelcontextprotocol/rust-sdk`
  - `https://github.com/sqliteai/sqlite-memory`
  - `https://github.com/liquidos-ai/AutoAgents`
  - `https://github.com/yarenty/kowalski`
  - `https://github.com/RightNow-AI/openfang`
  - `https://github.com/smartcomputer-ai/agent-os`
- 本轮说明：
  - 未改动业务代码，仅更新规划/交接文档。
  - 自动化测试未执行（本轮无代码变更，不触发测试门槛）。
- 下一步（待用户继续指令）：
  1. 进入 H0 并行实施（后端/前端/文档三任务拆分）。
  2. 先打通 run/event 骨架，再接入主动新建草稿链路（H1）。

## 2026-04-25 — 图谱节点命名改进（ingest-时间戳 → 有意义的文件名）

- 背景：摄入内容在图谱中显示 `ingest-1745512345678900` 等难读名称，对用户不友好。
- 改动：
  - `src-tauri/src/db.rs`：新增 `update_wiki_page_title` 函数，支持按路径更新 `wiki_pages.title`。
  - `src-tauri/src/vault.rs`：
    - `ingest_markdown` 新增 `display_name: Option<&str>` 参数。
    - 新增 `resolve_wiki_filename`：根据显示名称生成安全文件名，冲突时追加 -1/-2 后缀。
    - 新增 `sanitize_filename_stem`：清洗字符串为安全文件名字段（保留字母/数字/中文，长度截断 60 字节）。
  - `src-tauri/src/state.rs`：
    - `complete_ingest_with_precomputed_analysis`：截取 `display_source_path` 的 file_stem 作为显示名称传给 vault。
    - 新增 `extract_wiki_display_name` 辅助函数。
    - `save_wiki_page_impl`：保存后同步更新 DB 的 `wiki_pages.title`（确保编辑后图谱/检索显示正确标题）。
- 验证：
  - Rust cargo test：**173 passed** ✅
  - Frontend npm test：**174 passed** ✅
  - Frontend typecheck：**0 errors** ✅
- 使用方式：
  - 摄入后 wiki 文件名取自源文件名（如 `report.pdf` → `report.md`），冲突时 `report-1.md`。
  - 编辑页面后图谱节点立即更新为正确标题。
  - 无 display_name 时回退到 `ingest-{timestamp_ns}` 旧行为。

## 2026-04-25 — 方向 G 残余风险收口（Claude Code + 并行子代理）

- 背景：
  - Codex 方向 E/F/G 轮次在交接简报中提出三个残余风险，本轮系统性收口。
  - 基线：170 Rust / 169 前端。
- 并行拆分（§14）：
  - 后端子代理：负责 `src-tauri/src/{state,commands,main}.rs`，实现 restore 命令（从历史记录ID读取内容并写回）与 save 接口 checksum 参数（`expected_checksum`，可选，校验不匹配则拒绝写入）。
  - 前端子代理：负责 `web/src/{App,tauri-client,styles,app-utils.test}.ts(x)`，实现「恢复到此版本」按钮（二次确认 + 重新加载）与编辑基线 checksum（`simpleHash` 计算编辑前原文哈希，保存时传入）。
  - 主控：负责 `.gitignore` 清理、文档更新、合并验收、验证与风险收敛。
- 实现结果：
  - 风险1（一键恢复到版本）：
    - `src-tauri/src/state.rs`：新增 `restore_wiki_page_from_history_impl` 方法（从 DB 读历史 → 调 `save_wiki_page_impl` 写回）。
    - `src-tauri/src/commands.rs` + `src-tauri/src/main.rs`：新增并注册 `restore_wiki_page_from_history` 命令。
    - `web/src/App.tsx`：历史 diff 面板头部新增「恢复到此版本」按钮（`danger` 样式，`globalThis.confirm` 二次确认，恢复后自动关闭 + 刷新内容）。
    - `web/src/tauri-client.ts`：新增 `restoreWikiPageFromHistory` 函数。
    - `web/src/styles.css`：新增 `.wiki-history-diff__head-actions` flexbox 布局样式。
    - 新增 Rust 测试 1 条：`restore_wiki_page_from_history_replaces_content`（写入 v1/v2/v3 → 从最旧恢复 → 验证文件内容）。
  - 风险2（编辑基线 checksum）：
    - `src-tauri/src/state.rs`：`save_wiki_page_impl` 新增 `expected_checksum: Option<&str>` 参数，比较 `md5_simple` 当前文件哈希与传入值，不匹配立即返回 `checksum_mismatch`。
    - `src-tauri/src/commands.rs`：`save_wiki_page` 新增 `expected_checksum: Option<String>` 参数。
    - `web/src/App.tsx`：新增 `simpleHash` 导出函数 + `wikiEditBaselineChecksum` 状态；`handleStartWikiEdit` 记录基线哈希；`handleSaveWikiEdit` 传入哈希，错误时弹窗告警。
    - `web/src/app-utils.test.ts`：新增 5 条 `simpleHash` 测试（一致性、不同输入、空串、单字符差异、多行）。
    - 新增 Rust 测试 2 条：`save_wiki_page_checksum_mismatch_rejected`（错误哈希 → 返回 Err） + `save_wiki_page_checksum_match_accepted`（正确哈希 → 成功）。
  - 风险3（未跟踪数据清理）：
    - `.gitignore`：新增 `test-vault-template/`、`test-vault3/`、`LLM_icon/` 段。
- 自动化验证：
  - Windows PowerShell：`cargo test`：**173 passed, 0 failed** ✅（+3）
  - Windows PowerShell：`npm run test -- --run`：**174 passed, 0 failed** ✅（+5）
  - Windows PowerShell：`npm run typecheck`：**0 errors** ✅
  - Windows PowerShell：`npm run build`：通过 ✅
  - Windows PowerShell：`cargo check`：通过 ✅
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 打开任一 Wiki 页面，编辑并保存两次，点击「历史版本」→ 选择一个历史项 → 点「恢复到此版本」→ 确认弹窗 → 验证内容已回滚。
    2. 打开同一页面进入编辑模式 → 在编辑前用外部工具手动修改 `.md` 文件（模拟并发编辑）→ 点保存 → 应看到 checksum 冲突提示 → 刷新后可正常保存。
  - 残余风险：
    - checksum 使用 `simpleHash`（FNV-1a 风格），非密码学安全哈希，仅适用于检测外部并发编辑场景。
    - `App.tsx` 已超过 10000 行，后续需模块化拆分。

## 2026-04-25 — 方向 E/F/G 接手与方向 G 并行开工（Codex + 并行子代理）

- 接手背景：
  - 用户要求按顺序完成方向 E、F、G。
  - 代码事实显示方向 E 已由提交 `7da98b8` 落地，方向 F 已由提交 `f3e559f` 落地。
  - 当前 `docs/交接状态卡.md` 滞后在方向 C 二期，本轮先补齐接手状态，再继续方向 G。
- 快速核查：
  - 方向 E：已存在 `get_vault_stats` 命令、`VaultStats`/`IngestSourceCount`/`CitedPageStat` 模型、DB 聚合函数与前端 Stats 入口。
  - 方向 F：已存在 `create_wiki_page_with_ai` 命令、`NewPageResult` 模型、AI 新建页面 UI 与 `createWikiPageWithAi` 前端封装。
  - 当前工作树 tracked 文件存在行尾差异噪音，`git diff --ignore-space-at-eol` 为空；本轮避免整文件格式化与无意义重写。
- 并行拆分（§14）：
  - 后端子代理：负责 `src-tauri/src/{db,models,state,commands,main}.rs`，实现页面历史表、保存前快照、历史查询命令与 Rust 测试。
  - 前端子代理：负责 `web/src/{types,tauri-client,App,styles,app-utils.test}.ts(x)`，实现历史入口、历史面板、行级 diff 与前端测试。
  - 主控：负责接口契约、文档接手、合并验收、验证与风险收敛。
- 本轮约束：
  - WSL 下开发，真实运行目标仍为 Windows；用户侧手动验证命令统一给 PowerShell。
  - 新增/修改代码注释使用中文且保持简洁。
- 串行接管说明：
  - 后端/前端子代理启动后 3 分钟内未返回可合并产物，继续等待会阻塞本轮交付。
  - 主控于本轮关闭两个子代理并串行接管方向 G，实现阶段影响范围为方向 G 全栈文件；并行规则保留，原因已显式记录。
- 方向 G 实现结果：
  - 后端：
    - `src-tauri/src/db.rs`：新增 `wiki_page_history` 表、索引、插入历史快照、列表查询、详情查询；删除页面时清理历史；重命名页面时同步迁移历史 path。
    - `src-tauri/src/models.rs`：新增 `WikiPageHistoryItem` / `WikiPageHistoryDetail`。
    - `src-tauri/src/state.rs`：`save_wiki_page_impl` 在内容覆盖前记录旧正文快照；新增历史列表与详情实现；补 `save_wiki_page_records_previous_content_history` 测试。
    - `src-tauri/src/commands.rs` + `src-tauri/src/main.rs`：新增并注册 `list_wiki_page_history`、`get_wiki_page_history_entry`。
  - 前端：
    - `web/src/tauri-client.ts` / `web/src/types.ts`：新增历史 API 封装与类型。
    - `web/src/App.tsx`：Wiki 详情页新增「历史版本」按钮、历史面板、当前内容 vs 历史内容行级 diff。
    - `web/src/styles.css`：新增历史面板与 diff 样式。
    - `web/src/app-utils.test.ts`：新增 `buildWikiLineDiff` 测试。
- 自动化验证：
  - Windows PowerShell：`Set-Location E:\llm-wiki\src-tauri; cargo check`：通过 ✅
  - Windows PowerShell：`Set-Location E:\llm-wiki\src-tauri; cargo test`：**170 passed, 0 failed** ✅
  - Windows PowerShell：`Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run`：typecheck 通过，**169 passed, 0 failed** ✅
  - Windows PowerShell：`Set-Location E:\llm-wiki\web; npm run build`：通过 ✅（仅 Vite 动态/静态 import chunk 警告）
  - WSL：`cd web && npm run typecheck`：通过 ✅
  - WSL：`cd web && npm run test -- --run` / `npm run build`：失败 ❌（缺失 Linux Rollup optional dependency `@rollup/rollup-linux-x64-gnu`，Windows 验证已通过）
- 用户侧 PowerShell 手动验证命令：
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内步骤：
    1. 打开 Stats，确认页面总数、孤立页、热门引用、来源分布、近 7/30 天增长正常展示。
    2. 打开 Wiki，输入主题，使用 AI 新建页面；确认新页面写入 `vault/wiki/` 并能在列表/详情中打开。
    3. 打开任一 Wiki 页面，编辑保存两次；点击「历史版本」，选择历史项，确认能看到当前内容 vs 历史内容 diff。
    4. 重命名该页面后再次打开「历史版本」，确认历史仍可见。
- 未完成项：
  - TODO：本轮仅支持查看历史与 diff，不支持“一键恢复到此版本”；如用户需要，下一轮实现。
  - TODO：等待用户应用内手动验收结果，未回传前不标记为最终完成。

### 代码质量自评 (Round R20260425-EFG)
- 总分：91/100（A）
- 错误处理：18/20 — 历史 DB 读写错误显式返回，前端有错误态；前端历史 API 对非 Tauri 做降级。
- 并发安全：13/15 — 沿用现有保存路径，未引入新共享可变状态；仍未解决保存命令缺少调用方 checksum 参数的旧约束。
- 代码结构：18/20 — 后端 DB/state/command 分层清晰，前端 diff 抽为纯函数；`App.tsx` 继续偏大。
- 测试覆盖：23/25 — 覆盖保存快照、删除/重命名维护历史、行级 diff；缺少 UI 交互自动化测试。
- 性能合理性：9/10 — 历史列表不返回正文；diff 为行级 DP，超大页面可能有 UI 计算成本。
- 安全性：10/10 — 历史数据本地保存，删除页面同步清理历史；未新增云调用或 Strict Local 回归。
- 主要扣分项（须可操作）：
  1. 历史 diff 对超大 Markdown 页面没有行数上限，后续可增加分段/虚拟列表。
  2. 保存接口仍未要求前端传入编辑基线 checksum，无法完全实现并发外部编辑拦截。
- 是否需要用户介入修复：否

## 2026-04-25 — 方向 B/C 代码质量 A 级收口（Claude Code + 并行子代理）

- 背景：审查发现方向 B（C 级）和方向 C（B 级）存在多处质量缺陷，对照方向 A 标准进行系统性修复。
- 后端（Rust）：
  - `src-tauri/src/db.rs`：
    - `search_ask_session_turns`：LIKE 全表扫描 → FTS5（新增 `fts_ask_turns` 虚拟表 + insert/delete 触发器自动维护 + 历史数据迁移回填）
    - `list_ask_sessions`：N+1 关联子查询 → CTE + LEFT JOIN（`latest_turn_ids` 单次聚合）
    - 新增测试 `search_ask_session_turns_fts_cleaned_on_delete`：验证删除会话后 FTS 索引被级联清理
  - `src-tauri/src/state.rs`：
    - `init_vault_with_template`：新增 `extra_dirs` 路径遍历防御（拒绝 `..` 和绝对路径）+ 512 KB 内容大小限制
    - 新增测试 `init_vault_with_template_rejects_path_traversal`、`init_vault_with_template_rejects_oversized_content`
- 前端（React / TypeScript）：
  - `web/src/App.tsx`：
    - `loadAskSessionMessages`：切换前先 `setAskMessages([])`，避免短暂显示旧会话内容
    - `handleOpenAskSearchHit`：捕获 turns 返回值，空数组时提示"会话可能已被删除"，不再静默定位
    - `recentVaultPaths`：改为 `useEffect` 自动持久化，移除 `handleInitVault` 和 clear 按钮中散落的手动 `writeRecentVaultPathsToStorage` 调用
    - `writeRecentVaultPathsToStorage`：catch 块从静默改为 `console.warn` 记录
  - `web/src/app-utils.test.ts`：新增 5 条边界测试（存储失败容错、大小写去重、特殊字符安全、空串 snippet、88 字符截断边界）
- 验证（Windows 本机）：
  - `cargo test`：**167 通过，0 失败** ✅
  - `npm run test -- --run`：**165 通过，0 失败** ✅
  - `npm run typecheck`：**0 errors** ✅
- 提交：`d67f656`（2026-04-25）

## 2026-04-24 — 方向 D：摄入后快速结构 Lint（Claude Code + 并行子代理）

- 背景：
  - 摄入完成后缺少即时结构质量反馈，断链与缺失实体页只能靠完整 Lint 扫描发现，用户体验滞后。
  - 本方向目标：摄入成功后 fire-and-forget 触发无 LLM 的纯文件系统结构检查，快速反馈给用户。
- 后端（Rust / Tauri）：
  - `src-tauri/src/models.rs`：新增 `PageQuickLint` 结构体（`wiki_path`、`broken_links`、`missing_entity_pages`、`issues_count`）。
  - `src-tauri/src/state.rs`：
    - 新增辅助函数：`entity_to_slug`、`split_frontmatter`、`parse_frontmatter_entities`。
    - 新增同步方法 `quick_lint_page_impl`：从 wiki 页面内容提取 `[[wikilink]]` 与 frontmatter `entities`，对照 `vault/wiki/*.md` 判断断链与缺失实体页，纯文件系统无 LLM。
    - 新增 4 条 Rust 单测（`mod quick_lint_tests`），全部通过。
  - `src-tauri/src/commands.rs`：新增同步 Tauri 命令 `quick_lint_page`。
  - `src-tauri/src/main.rs`：注册 `commands::quick_lint_page`。
- 前端（React / TypeScript）：
  - `web/src/types.ts`：新增 `PageQuickLint` 接口。
  - `web/src/tauri-client.ts`：新增 `quickLintPage(wikiPath)` 封装（同步命令，不加 `withTimeout`）。
  - `web/src/App.tsx`：`applyIngestPreview` 成功后 fire-and-forget 调用 `quickLintPage`，结果以可关闭的 `.quick-lint-hint` 通知卡片（右下角）展示；无问题时静默。
  - `web/src/styles.css`：新增 `.quick-lint-hint` 固定定位通知卡片样式（使用已有 `--warning`/`--info`/`--bg-card`/`--text`/`--text-muted` 变量）。
  - `web/src/app-utils.test.ts`：新增浏览器回退测试（非 Tauri 环境返回 `null`）。
- 实施说明：
  - 后端/前端各用独立子代理并行开发（§14），主控合并 worktree 产物，验证后提交。
  - 两个 worktree 均有未提交更改（代理未 commit），由主控 cp 后直接提交到 main。
- 验证（Windows 本机）：
  - `cargo test`（src-tauri/）：**164 通过，0 失败** ✅
  - `npm run test -- --run`（web/）：**161 通过，0 失败** ✅
  - `npm run typecheck`（web/）：**0 errors** ✅
- 提交：`fbf09d0`（2026-04-24）

## 2026-04-24 — 方向 A 测试修复：localStorage mock 与业务代码对齐（Claude Code）

- 背景：
  - Codex 修复"最近 Vault 路径"测试失败时，将 `installLocalStorageMock()` 注入测试，但仍失败。
  - 根本原因：业务代码 `readRecentVaultPathsFromStorage` / `writeRecentVaultPathsToStorage` 使用 `window.localStorage` + `typeof window === "undefined"` 守卫。vitest 测试环境为 Node（无 jsdom），`window` 不存在，函数提前返回 `[]`，mock 设在 `globalThis.localStorage` 但业务代码读不到。
- 修复：
  - `web/src/App.tsx`：将两个函数改为使用 `globalThis.localStorage` + `if (!storage)` 守卫，与项目内其他存储函数（`readWikiSortModeFromStorage` 等）保持一致。
  - `web/src/app-utils.test.ts`：保留 `installLocalStorageMock()` 调用（mock 现在可被业务代码读到）。
- 验证：
  - `npm --workspace web run test -- --run`：160/160 通过 ✅
  - `npm --workspace web run typecheck`：0 errors ✅
  - `cargo test`（src-tauri/）：160/160 通过 ✅

## 2026-04-24 — 方向 C 二期：会话轮次 citations/meta 持久化 + 跨会话检索（Codex）

- 背景：
  - 方向 C 一期已完成会话实体化存储与管理 UI，但历史会话恢复后无法显示引用证据与检索策略，且缺少跨会话命中定位能力。
- 本轮实现：
  - 后端（Rust / SQLite）：
    - `src-tauri/src/db.rs`
      - `ask_session_turns` 表新增字段（含兼容迁移）：
        - `citations_json TEXT NOT NULL DEFAULT '[]'`
        - `meta_json TEXT`
      - `append_ask_session_turn` 扩展为支持写入 citations/meta。
      - `list_ask_session_turns` 扩展读取 citations/meta 字段。
      - 新增 `search_ask_session_turns(keyword, limit)`：跨会话按轮次内容检索。
      - 新增 1 条 DB 单测：`search_ask_session_turns_matches_across_sessions`。
      - 扩展会话单测断言：覆盖 citations/meta 落库与回读。
    - `src-tauri/src/models.rs`
      - 新增 `AskSessionTurnMeta`、`AskSessionSearchHitItem` 模型。
      - `AskSessionTurnItem` 新增可选字段：`citations`、`meta`。
    - `src-tauri/src/state.rs`
      - `list_ask_session_turns_impl` 增加 JSON 反序列化：回传结构化 `citations/meta`。
      - 新增 `search_ask_session_turns_impl`（供命令层调用）。
      - `query_ask_session` 在写入 assistant 轮次时同步落库：
        - citations（引用证据）
        - meta（mode/search_strategy/answer_strategy/top_k/matched_pages/search_debug）
    - `src-tauri/src/commands.rs` + `src-tauri/src/main.rs`
      - 新增 Tauri 命令：`search_ask_session_turns`。
  - 前端（React / TS）：
    - `web/src/types.ts`
      - `AskSessionTurnItem` 新增 `citations/meta` 字段。
      - 新增 `AskSessionSearchHitItem` 类型。
    - `web/src/tauri-client.ts`
      - 新增 API：`searchAskSessionTurns(keyword, limit)`。
    - `web/src/App.tsx`
      - Ask 会话恢复时支持还原 assistant 消息的 citations 与 meta pills/调试信息。
      - 新增会话侧栏"跨会话检索"输入框与结果列表。
      - 新增命中定位：点击检索结果自动切会话并滚动高亮目标轮次。
      - 新增纯函数：`formatAskSessionSearchSnippet`（压缩空白+截断）。
    - `web/src/styles.css`
      - 新增跨会话检索区域样式、命中项样式、消息高亮动画样式。
    - `web/src/app-utils.test.ts`
      - 新增前端单测：`formatAskSessionSearchSnippet`。
- 并行实施说明（§14）：
  - 本轮未使用子代理并行。原因：当前执行策略下无法显式委派；该轮改动集中在 Ask 同一链路（db/state/commands/App）且接口耦合较高，采用主控串行落地并记录。
- 自动化验证（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（已知环境问题：缺失 `@rollup/rollup-linux-x64-gnu`）
  - `cd src-tauri && cargo check/test`：未执行 ❌（当前环境缺少 `cargo`）
- 用户侧 PowerShell 验证命令（待回传）：
  - `Set-Location E:\llm-wiki\src-tauri; cargo check; cargo test`
  - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. Ask 连续提问 1 轮并等待助手回答后，刷新/重启应用，历史 assistant 消息应保留 citations 与检索策略 pills。
    2. 在 Ask 侧栏"跨会话检索内容"输入关键词并检索，出现命中列表。
    3. 点击命中项后，应自动切换到对应会话并滚动定位到目标消息（带短暂高亮）。
    4. 旧会话（一期前数据）加载时不应报错，缺失 meta 的轮次正常降级显示文本。

## 2026-04-24 — 方向 C 一期收口：编译告警清理与提交前校验（Codex）

- 背景：
  - 用户在 Windows 侧复核 `cargo check` 时，先遇到一次 `E0596`（事务连接需要可变借用），随后复跑通过。
  - 同时确认 `src-tauri` 目录不存在 `npm test` 脚本，前端测试需在 `web` 目录执行。
- 本轮收口改动：
  - `src-tauri/src/db.rs`
    - 保持 `save_ask_history` 中事务连接为可变借用：`let mut conn = ...`（修复 `E0596`）。
    - 清理 `create_ask_session` 中不必要的 `mut`，减少 `unused_mut` 告警噪音。
  - `src-tauri/src/state.rs`
    - 删除 `new_with_path` 中未使用的局部变量 `serialized`，清理 `unused variable` 告警。
- 验证结果：
  - 用户侧 Windows：
    - `cd src-tauri; cargo check`：通过 ✅（用户已回传）
    - `cd src-tauri; cargo test`：本轮未回传完整结果（仍待补齐）
    - `cd web; npm run typecheck / npm run test -- --run`：本轮未回传
  - WSL：
    - `cd web && npm run typecheck`：通过 ✅
- 本轮结论：
  - 方向 C 一期代码已完成收口并提交，当前残余风险为 Windows 侧 `cargo test` 与前端测试结果仍待补齐回传。

## 2026-04-24 — 方向 C 第一轮：Ask 会话实体化存储 + 会话管理 UI（Codex）

- 背景：
  - 方向 B（模板预览/最近项目）已完成开发，按 A→B→C 继续推进方向 C。
  - 现状 Ask 多轮上下文仅内存保存，重启后会话丢失，缺少会话治理入口。
- 本轮实现：
  - 后端（Rust / SQLite）：
    - `src-tauri/src/db.rs`
      - 新增 `ask_sessions`、`ask_session_turns` 两张表及索引（schema 自动创建）。
      - 新增会话持久化能力：
        - `create_ask_session`
        - `list_ask_sessions`
        - `list_ask_session_turns`
        - `list_recent_ask_session_turns`
        - `append_ask_session_turn`
        - `rename_ask_session`
        - `clear_ask_session_turns`
        - `delete_ask_session`
      - 新增 2 条 DB 单测（会话创建/轮次读写、重命名/删除）。
    - `src-tauri/src/state.rs`
      - 新增会话管理实现：
        - `create_ask_session_impl`
        - `list_ask_sessions_impl`
        - `list_ask_session_turns_impl`
        - `rename_ask_session_impl`
        - `delete_ask_session_impl`
      - `query_ask_session` 改为持久化链路：
        - 查询前读取持久化历史作为上下文；
        - 用户轮与助手轮落库；
        - 取消/检索失败时回滚刚写入的用户轮，避免污染历史。
      - `clear_ask_session` 同步清理 DB 会话轮次。
    - `src-tauri/src/commands.rs` + `src-tauri/src/main.rs`
      - 新增 Tauri 命令：
        - `create_ask_session`
        - `list_ask_sessions`
        - `get_ask_session_turns`
        - `rename_ask_session`
        - `delete_ask_session`
    - `src-tauri/src/models.rs`
      - 新增模型：
        - `AskSessionItem`
        - `AskSessionTurnItem`
  - 前端（React/TS）：
    - `web/src/types.ts`
      - 新增 `AskSessionItem` / `AskSessionTurnItem` 类型。
    - `web/src/tauri-client.ts`
      - 新增会话管理 API 封装：
        - `createAskSession`
        - `listAskSessions`
        - `fetchAskSessionTurns`
        - `renameAskSession`
        - `deleteAskSession`
    - `web/src/App.tsx`
      - Ask 模块新增会话管理 UI：
        - 会话列表（左侧）
        - 筛选
        - 切换会话
        - 新建会话
        - 重命名
        - 删除
        - 导出 `.md`
      - 新增 `filterAskSessions`、`buildAskSessionExportMarkdown` 工具函数。
      - 启动时自动加载持久化会话；空会话集自动创建默认会话。
      - Query 完成后刷新会话列表，保证更新时间/轮次数可见。
    - `web/src/styles.css`
      - 新增 Ask 会话面板样式与移动端响应式布局。
    - `web/src/app-utils.test.ts`
      - 新增 2 条前端单测（会话过滤、会话导出 markdown）。
- 并行实施说明（§14）：
  - 本轮未使用子代理并行。原因：当前执行策略限制本轮直接委派；改动集中在同一会话链路（`state/db/App.tsx`）且前后端接口耦合高，采用主控串行落地并在记录中明确。
- 自动化验证结果（本机 WSL）：
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（已知环境问题：缺失 `@rollup/rollup-linux-x64-gnu` 可选依赖）
  - `cd src-tauri && cargo check`：未执行 ❌（当前环境缺少 `cargo` 命令）
- 用户侧 PowerShell 验证命令：
  - `Set-Location E:\llm-wiki\src-tauri; cargo check; cargo test`
  - `Set-Location E:\llm-wiki\web; npm run typecheck; npm run test -- --run`
  - `Set-Location E:\llm-wiki; npm run tauri:dev`
  - 应用内验证：
    1. 进入 Ask 模块，确认左侧出现"会话管理"列表。
    2. 新建会话并提问 1-2 轮，重启应用后会话与消息仍可加载。
    3. 对会话执行重命名/删除/导出，确认行为与提示一致。
     4. 切换不同会话，确认消息上下文与当前会话一致。

## 2026-04-25 — 非 md 文件摄入 display_name 泄漏修复（增量）

- **触发**：上轮（图谱节点命名改进）完成后，PDF/TXT/DOCX/URL 经 `ingest_text_via_temp_markdown` 写入临时文件 `llm_wiki_{route_tag}_{uuid}.md`，该临时路径被 `complete_ingest_with_precomputed_analysis` 当作 `display_source_path`，触发 `extract_wiki_display_name` 的 `llm_wiki_` 前缀拒绝逻辑，最终回退到 `ingest-{timestamp}`，**抵消**了上轮修复效果。
- **改动**（`src-tauri/src/state.rs`）：
  - `ingest_markdown` 新增第二个参数 `display_name: Option<String>`。当 `Some` 时，`display_path = display_name` 传入下游；否则沿用 `source_path_text`（旧行为）。
  - 各调用者适配：
    - `ingest_file_impl`（直接 .md）：传 `None`，无行为变化。
    - `ingest_text_via_temp_markdown`：从 `source_path.file_stem()` 提取原始文件名作为 `display_name` 传入，确保 PDF/TXT/DOCX 以原始文件名而非 `llm_wiki_` 临时名呈现。
    - `ingest_url_impl`：从 URL 路径最后一段（`path_segments().last()`）提取显示名，回退到 `host_str()` 去掉 `www.` 前缀。
    - 队列 worker、"markdown" 类型、research 任务、`commands.rs` Tauri 命令、`clip_server.rs`：均传 `None`（直接文件路径）。
- **验证**：
  - Rust `cargo test`：**173 passed** ✅
  - Frontend `npm run test -- --run`：**174 passed** ✅
  - `npm run typecheck`：**0 errors** ✅
