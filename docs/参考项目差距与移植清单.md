# 参考项目差距与移植清单（对标 `nashsu/llm_wiki`）

> 更新时间：2026-04-25  
> 对标对象（本地副本）：`/mnt/e/llm-wiki/refer_project`  
> 我方项目：`/mnt/e/llm-wiki`  
> 用途：作为 Claude Code / Codex / Gemini 三方协作时的统一对标基线与迭代方向依据。

---

## 1. 结论摘要（先看）

我方项目当前优势在于：
- 本地优先治理更严格（`StrictLocal/Hybrid`、Provider 路由约束、Outbox 可审计事件流）。
- Rust 后端契约更统一（Tauri 命令 + SQLite + FTS5 + Lint Patch 闭环）。
- 多格式摄入基础更强（`md/pdf/url/docx/pptx/txt/图片OCR`）。

对标项目领先点在于：
- 产品闭环完整度更高（项目模板化 + 摄入队列 + Deep Research + 图谱洞察 + Clipper）。
- 图谱不是“展示层”，而是“分析层”（社区检测、惊奇连接、知识空白、可操作建议）。
- 查询阶段的上下文预算管理更细（检索、扩展、预算、组装分层）。

建议我方下一阶段主线：
1. 先补“稳定性与吞吐”能力：**持久化 ingest 队列**。  
2. 再补“知识增益”能力：**图谱洞察层 + Embedding 检索融入 RRF**。  
3. 最后补“入口扩张”能力：**Deep Research + Web Clipper**。

---

## 2. 对标证据（关键文件）

### 2.1 参考项目关键能力证据
- 持久化摄入队列：`refer_project/src/lib/ingest-queue.ts`
- 两步摄入与缓存：`refer_project/src/lib/ingest.ts`
- 图谱关联与社区：`refer_project/src/lib/graph-relevance.ts`、`refer_project/src/lib/wiki-graph.ts`
- 图谱洞察：`refer_project/src/lib/graph-insights.ts`
- Deep Research：`refer_project/src/lib/deep-research.ts`、`refer_project/src/lib/web-search.ts`
- 多会话与持久化：`refer_project/src/stores/chat-store.ts`、`refer_project/src/lib/persist.ts`
- 向量检索（LanceDB）：`refer_project/src-tauri/src/commands/vectorstore.rs`
- Clipper（扩展 + 本地服务）：`refer_project/extension/manifest.json`、`refer_project/src-tauri/src/clip_server.rs`
- 模板化项目初始化：`refer_project/src/lib/templates.ts`

### 2.2 我方项目关键能力证据
- 运行模式与 Provider 路由：`src-tauri/src/state.rs`
- 命令层与能力面：`src-tauri/src/commands.rs`
- 数据与索引：`src-tauri/src/db.rs`
- 前端主界面与模块：`web/src/App.tsx`
- 交接与阶段状态：`agents.md` §18

---

## 3. 精细差距矩阵（功能优先）

| 领域 | 参考项目现状 | 我方现状 | 差距判断 | 可移植方案（建议） |
|---|---|---|---|---|
| 摄入任务编排 | 持久化串行队列、失败重试、取消、恢复（重启后继续） | 以命令式摄入为主，缺少统一任务队列层 | 高 | 新增 `ingest_queue`（DB 表 + 状态机 + UI 队列面板） |
| 两步摄入质量 | 分析→生成双阶段明确拆分 | 已有 LLM 摄入与实体抽取，但“阶段产物可视化/可审核”弱 | 中 | 增加“分析阶段产物”持久化与可视化 |
| 图谱能力 | 关联权重模型 + Louvain + 洞察（惊奇连接/知识空白） | 已有图谱、筛选、局部子图（C2进行中） | 高 | 在现有图谱上叠加“社区检测 + 洞察卡片 + 高亮联动” |
| 查询检索策略 | 分词 + 向量 + 图扩展 + 上下文预算组装 | FTS + RRF（三路）已具备，embedding 已落库但未参与检索融合 | 高 | 增加 RRF 第4路：embedding 相似召回 |
| Deep Research | 多查询 web search → 综合 → 写回 wiki → 自动再摄入 | 暂无 | 高 | 新增 `research_tasks` 与 `ingest_url` 复用链路 |
| 审核流（HITL） | Review 项结构化、可异步处理 | Lint patch 能力强，但“摄入审核队列”未形成产品面 | 中 | 复用 patch 思想扩展到 ingest 审核项 |
| 多项目体验 | 模板化项目创建（purpose/schema预置） | 单项目/单vault工作流为主 | 中 | 引入“项目模板初始化器” |
| 外部采集入口 | Chrome Clipper + 本地守护服务 | 暂无原生剪藏链路 | 中高 | 先实现简版 HTTP clip 接口，再补扩展 |
| 会话持久化 | 多会话、引用信息持久化完善 | Ask 多轮会话有，但长期存储与会话治理可加强 | 中 | 增加会话实体化存储与会话管理 UI |
| 安全与治理 | 功能完整但本地治理策略较宽 | StrictLocal 策略与模式约束更强 | 我方优势 | 保持现有模式治理，不回退 |

---

## 4. 可借鉴移植包（按优先级）

## 4.1 移植包 A（P0，先做）
- 目标：补齐“稳定吞吐”短板，支持大批量源文件持续摄入。
- 范围：
  - 后端：新增 `ingest_queue_items` 表 + 队列命令（enqueue/list/cancel/retry/start_worker）。
  - 前端：新增队列面板（排队中/处理中/失败/已完成）。
  - 事件：复用 outbox，新增 `ingest_queue_*` 事件。
- 验收：
  - 重启后任务可恢复。
  - 失败任务可重试，处理中任务可取消。
  - 不破坏现有 `ingest_file/ingest_pdf/ingest_url` 路径。

## 4.2 移植包 B（P1，次优先）
- 目标：把图谱从“可视化”升级为“洞察引擎”。
- 范围：
  - 后端：社区检测结果计算（可先纯前端计算，后续后端化）。
  - 前端：新增洞察卡片（孤立页、桥接节点、稀疏社区、异常连接）。
  - 交互：点击洞察高亮图谱节点/边。
- 验收：
  - 至少 3 类洞察可稳定展示。
  - 洞察与图谱联动可回退。

## 4.3 移植包 C（P1，与B可并行）
- 目标：完成 embedding 检索闭环，提升召回覆盖。
- 范围：
  - 后端：从 `page_embeddings` 做相似度检索，返回 top-k。
  - 查询：并入现有 RRF，形成第4路召回。
  - 前端：展示检索策略标签与命中来源（可选）。
- 验收：
  - 关闭 embedding 时行为不退化。
  - 开启后召回页数与命中率可观测提升。

## 4.4 移植包 D（P2，后续）
- 目标：扩展知识输入与探索能力。
- 方向：
  - D1：Deep Research 任务流（依托现有 `ingest_url`）。
  - D2：项目模板（purpose/schema/index/log 快速初始化）。
  - D3：Clipper（先本地接口，后浏览器扩展）。

---

## 5. 非目标与约束

- 不照搬参考项目技术栈：我方继续坚持 `Tauri + Rust + React + SQLite + Markdown Vault`。
- 不牺牲 Strict Local：新增能力必须尊重本地优先与模式治理。
- 不破坏现有契约：`index.md` / `log.md` / citations / task events 一致性必须保持。

---

## 6. 下一轮立项建议（供主控决策）

建议在下一轮只做一个收口目标，避免范围蔓延：
- 推荐：优先立项 **移植包 A（持久化 ingest 队列）**。  
- 备选：若你更看重“可见效果”，可先做 **移植包 B（图谱洞察）**。

---

## 7. 三方协作读取规则（执行约定）

本文件为对标差距的单一事实源。  
Claude Code / Codex / Gemini 在每轮开始前，必须同步读取本文件，并在实施记录中说明本轮选择了哪一个移植包与原因。

---

## 8. 新开发方向 H（2026-04-25 补充）：Agent Studio 移植蓝图

> 背景：A/B/C/D 与 E/F/G 已具备基础能力，下一轮进入“主动创作 + 可持续演进 + 可审计”的新模块建设。  
> 本节作为三方协作的统一蓝图基线，后续实现与验收均以本节为准。

### 8.1 目标与边界

- 模块代号：**H - Agent Studio**（后端 Agent Hub + 前端 Agent Studio）。
- 目标：补齐主动创作路径（主题 → 草稿 → 审批 → 写入 Wiki），并保留完整证据链与事件日志。
- 约束：
  - 严格本地模式下仅允许本地 Provider（Ollama），禁止外部模型 API。
  - 写盘必须走审批流程，禁止静默覆盖。
  - 生成结论必须可追溯到 citations 与来源页面。

### 8.2 总体策略（Rust 80/20）

- 原则：**Rust 优先，不 Rust-only**。
- 80%：选 Rust 参考项目，优先复用架构与实现模式，降低移植摩擦。
- 20%：吸收非 Rust 生态中的机制设计（仅借鉴机制，不搬代码）。

### 8.3 Rust 参考候选与取舍（按优先级）

#### P0（直接借鉴）

1. `0xPlaygrounds/rig`
   - 借鉴点：Provider 抽象、Agent 工作流组织。
   - 取舍：用于统一 Agent 侧 LLM 调用抽象，不替换现有 `LlmProvider` 主链路。
2. `modelcontextprotocol/rust-sdk`
   - 借鉴点：MCP 协议一致性与工具接入模型。
   - 取舍：优先用于 H 模块工具层，不强制改造现有命令体系。
3. `sqliteai/sqlite-memory`
   - 借鉴点：Markdown + SQLite 的记忆层范式（本地优先、可检索）。
   - 取舍：先做 AAAK-lite，不一次性引入复杂同步体系。

#### P1（机制借鉴）

1. `liquidos-ai/AutoAgents`
   - 借鉴点：多 Agent 编排与角色化组织。
2. `yarenty/kowalski`
   - 借鉴点：persona/模板化 agent 结构。
3. `RightNow-AI/openfang`、`smartcomputer-ai/agent-os`
   - 借鉴点：事件审计、长期运行与治理模型。

### 8.4 里程碑（H0 → H4）

1. H0（脚手架）
   - 后端：`agent_runs / agent_run_events / agent_drafts / agent_memories` 表与命令骨架。
   - 前端：左侧导航新增独立分组 `Agent Studio`（与既有模块显著分隔）。
   - 验收：可创建 run、可查看事件流、可结束 run。
2. H1（主动新建 MVP）
   - 主题输入 → 召回相关页面 → 生成结构化草稿（不直接写盘）。
   - 审批通过后才写入 `vault/wiki/`，并更新 `index.md` / `log.md`。
3. H2（记忆增强）
   - 引入 AAAK-lite 记忆压缩层，提升跨轮稳定性与复用率。
4. H3（技能化）
   - 技能模板与版本历史（先人工维护，后续再考虑自动演化）。
5. H4（深度融合）
   - 与 Ask/Research/Lint 联动，形成跨模块任务入口。

### 8.5 导航编排约束（前端）

- 左侧导航分三组：
  - 核心：Inbox/Wiki/Ask/Lint/Graph
  - 运营：Queue/Research/Stats
  - 新增：Agent Studio（单独分组，使用分割线与更大 vertical gap）
- 目标：让用户明确感知“新模块是独立工作区，不是普通子功能”。

### 8.6 第一轮执行约束（H0）

- 必须并行拆分（§14）：
  - 后端子任务：仅改 `src-tauri/src/{db,models,state,commands,main}.rs`
  - 前端子任务：仅改 `web/src/{types,tauri-client,App,styles}.ts(x)`
  - 文档子任务：仅改 `docs/{实施过程记录,交接状态卡,dev-status}.md` 与必要交接文件
- 每轮结束必须产出：
  - 改动文件清单
  - 自动化验证结果
  - Windows PowerShell 手动验证步骤
  - 未完成项与残余风险
