# 交接状态卡

> 最后更新：2026-04-23 CST（Codex）
> 说明：本文件用于 Claude Code / Codex / Gemini 快速接手；详细过程以 `docs/实施过程记录.md` 为准。

## 当前接力状态

- round_id: R20260423-A1（Codex）
- current_agent: Codex
- status: reviewing（已完成方向 A 开发，待用户侧 Windows 手动验证）
- objective: 方向 A「摄入质量审核」收口：Inbox 直摄入改为 preview 审批后落盘

## 本轮完成项（Direction A）

- ✅ 后端新增预览审批链路：
  - `preview_ingest_file(source_type, source_path, ocr_provider?)`
  - `apply_ingest_preview(preview_id)`
  - `IngestPreview` / `CachedIngestPreview` 与一次性缓存消费逻辑
- ✅ 后端复用落盘主流程：
  - `complete_ingest_with_precomputed_analysis(...)`
  - `ingest_markdown` 与 preview-apply 路径共用写盘/链接/embedding/outbox 逻辑
- ✅ 前端新增「摄入分析卡」对话框：
  - 展示摘要、实体、拟更新页
  - 支持取消/确认写入
- ✅ Inbox 直摄入接入审批：
  - 示例摄入、URL 摄入、文件摄入、拖拽 direct 模式均改为先预览再 apply
  - queue 路径保持不变
- ✅ 最小测试补充：
  - Rust: preview_id 不存在报错、preview 后 apply 成功且缓存消费
  - 前端: preview/apply 参数映射与浏览器回退行为

## 本轮改动文件

- 后端：
  - `src-tauri/src/models.rs`
  - `src-tauri/src/state.rs`
  - `src-tauri/src/commands.rs`
  - `src-tauri/src/main.rs`
- 前端：
  - `web/src/types.ts`
  - `web/src/tauri-client.ts`
  - `web/src/App.tsx`
  - `web/src/styles.css`
  - `web/src/app-utils.test.ts`
- 文档：
  - `docs/实施过程记录.md`
  - `docs/交接状态卡.md`
  - `agents.md`

## 验证摘要

- WSL（本轮已执行）：
  - `cd web && npm run typecheck`：通过 ✅
  - `cd web && npm run test -- --run`：失败 ❌（缺失 `@rollup/rollup-linux-x64-gnu`）
  - `cd web && npm run build`：失败 ❌（同上）
  - `cd src-tauri && cargo check`：失败 ❌（当前环境无 cargo）
- Windows（待用户复核）：
  - `cd src-tauri; cargo check; cargo test`
  - `cd web; npm run typecheck; npm run test -- --run; npm run build`

## 未完成项（下一轮优先）

- TODO-1：方向 B（项目模板/多项目体验）收口：模板初始化流程与默认骨架的端到端体验打磨。
- TODO-2：方向 C（会话持久化增强）立项：Ask 会话实体化存储 + 会话管理 UI。
- TODO-3：补齐 Windows 端 Rust 与前端全量验证结果回传。

## 风险提示

- `test-vault-template` 目录下运行产物（raw/wiki/meta.db/log/index）为本地数据，不应提交。
- 本轮新增 Tauri 命令后需完整重启 `npm run tauri:dev` 才能验证最新命令注册。
