# 交接状态卡

> 最后更新：2026-04-22 CST（Codex）
> 说明：本文件用于 Claude Code / Codex / Gemini 快速接手；详细过程以 `docs/实施过程记录.md` 为准。

## 当前接力状态

- round_id: R20260422-01（Codex）
- current_agent: Codex
- status: completed（本轮收口完成，待用户手动推送）
- objective: 已完成本轮收口（Deep Research 历史导出、Clipper 复核、SearXNG 启动/自检修复）

## 已完成（本轮前序）

- ✅ P1-1 任务状态同步：对话框 mount 后轮询后端，修正陈旧运行态（Claude 已提交）
- ✅ P1-2 搜索去重：后端 `seen_urls` + prompt-level learnings 去重（Claude 已确认）
- ✅ P1-3 Embed 健康提示：`ModelNotFound` 时给出 `nomic-embed-text:latest` 拉取指引（Claude 已提交）
- ✅ P2-1 Research 导出：Footer 增加“导出 `.md`”按钮（Claude 已提交）
- ✅ P2-2 图谱双击跳转：400ms 内双击节点跳转 Wiki（Claude 已提交）
- ⏸ P2-3 Chrome 扩展：本轮暂缓（按计划）

## 本轮接手补丁（Codex）

- ✅ `web/src/App.tsx`
  - 保留并收口 Claude 未提交改动：`handleExportMd` 历史任务兜底读取 `fetchWikiPageDetail(doneSavedPath).content`
  - 修复按钮显示条件：`phase === "done" && (synthesisContent || doneSavedPath)`，历史任务可见“导出 .md”按钮
- ✅ Clipper 复核增强：
  - `src-tauri/src/clip_server.rs`：`/status` 新增 `vault_open`、`vault_path`
  - `extension/popup.js`：未打开 Vault 时显示明确提示并禁用 Clip 按钮
  - `scripts/verify_clipper_windows.ps1`：Windows 一键自检脚本
- ✅ Clipper 实测收口：
  - 用户已回传 Windows 自检通过（含 `POST /clip` 写入成功）并确认扩展端按钮链路可用
  - `get_clip_server_status` 已从“固定 running”改为真实运行态
  - 新增 `scripts/verify_searxng_windows.ps1` 作为下一步 SearXNG 自检脚本
- ✅ SearXNG 参数优化补丁（本轮）：
  - `src-tauri/src/state.rs`：Deep Research 搜索改为多档位自动回退（语言优选 + `all/general,news`）并合并去重结果
  - `src-tauri/src/state.rs`：搜索错误增加 `unresponsive_engines` 摘要，便于区分“链路通但引擎质量低”
  - `scripts/verify_searxng_windows.ps1`：支持 primary/fallback 参数档位探测并输出最优档位
- ✅ README 文档收口：
  - 新增“模块依赖与服务速查”与 Windows 最小安装/配置命令
  - 补充 SearXNG/Clipper 一键自检入口，便于三方 Agent 快速对齐运行环境
- ✅ SearXNG Windows 优化方案（本轮）：
  - 已完成中英文双主题实测：中文 best=24，英文 best=23（`results.count`）
  - 新增 `configs/searxng/settings.windows.example.yml` 与 `scripts/run_searxng_windows.ps1`（PS7 一键重建+自检）
- ✅ SearXNG 脚本健壮性修复（本轮跟进）：
  - `verify_searxng_windows.ps1` 输出 inner exception 细节（不再只有 `GetResult` 泛化错误）
  - `run_searxng_windows.ps1` 增加容器就绪等待、失败日志输出与默认配置自动回退
  - 用户已在 PS7 回传 `run + verify` 自检通过

## 最新提交（代码主线）

- `d1ab47c` feat: 完成 P1+P2 功能收口
- `c4205fa` fix: 修复 ResearchDialog 事件重复订阅导致 queries/done 消息重复
- `3362e57` fix: ResearchDialog 操作按钮收口为底部统一 Footer
- `47048dd` fix: 修复 ResearchDialog UI 配色与交互问题
- `5f4440b` fix: 历史任务对话框显示正确状态与重试
- `6cbfbb0` chore: 优化 searxng 检索参数并收口文档与运行产物

## 基线与验证摘要

- WSL（本轮已执行）：
  - `cd web && npm run typecheck`：通过 ✅
- Windows（待复核）：
  - `cd src-tauri && cargo check && cargo test`
  - `cd web && npm run test -- --run`

## 未完成项（接手后优先）

- TODO-1：Windows 端复核 Rust 基线（当前 Codex 在 WSL 无 cargo）
- TODO-2：按后续轮次需要再做应用内 Deep Research 实跑（本轮按用户指令跳过）

## 风险提示

- `test-vault-template` 目录下运行产物（raw/wiki/doc/meta.db）均为本地测试数据，不应提交入仓。
- 若 Tauri 命令签名发生变化，需完全重启 `cargo tauri dev` 进程再验证。
