# 交接状态卡

> 最后更新：2026-05-04 CST（Codex）
> 说明：本文件用于 Claude Code / Codex / Gemini 快速接手；详细过程以 `docs/实施过程记录.md` 为准。
> **当前轮次为 H7 拆分重构：完整执行计划在 `docs/h7-app-tsx-refactor-plan.md`**

---

## 当前接力状态

- round_id: R20260504-H7-AppTsx-Refactor-Codex-Phase1
- current_agent: **Codex（接力 H7 Phase 1）**
- status: Phase 0 已完成；Phase 1.1~1.5 已完成；Phase 2.1 Settings / 2.2 Operations 已完成；`npm run typecheck` 通过
- objective: 继续按 `docs/h7-app-tsx-refactor-plan.md` Phase 2 → Phase 3 增量执行 App.tsx 拆分

### H7 接力起手指引（Sonnet 4.6 必读）

按以下顺序读：
1. `docs/h7-app-tsx-refactor-plan.md` §0-§3（理解架构，约 250 行）
2. `docs/h7-app-tsx-refactor-plan.md` §4-§7（执行步骤 + 进度勾选 + 异常处理）
3. `agents.md` §11、§16（中文注释 + 多 Agent 交接）
4. `docs/dev-status.md`（确认基线数字）

执行约束（详见 plan §7.2）：
- ❌ 不要一次提交多个 step；每个 step 独立 commit
- ❌ 不要改后端代码（src-tauri/）
- ❌ 不要引入新 npm 依赖
- ✅ 下一步：Phase 2.3 — 提取 `InboxModule`
- ✅ 每个 commit 之前必跑：`npm run typecheck`

### 已完成（2026-05-04 Opus 4.7 收口）

- 架构计划：`docs/h7-app-tsx-refactor-plan.md`（约 540 行，含进度勾选表）
- write_wiki / edit_wiki E2E：用户决定跳过手测，以已有 4 条 Rust 测试为准（决策记录入 plan §0.1）
- dev-status.md / 实施过程记录.md / 本文件 三方协作文档已同步

---

## 已完成（H3~H5，全部验证通过）

- ✅ H3: agent_skills CRUD + skill prompt 注入 + 草稿头 badge
- ✅ H3 fix: 记忆键可选，记忆表单布局修复，正则修复
- ✅ H4: research_mode 检索增强
- ✅ H5-B: 审批后自动提炼全局记忆
- ✅ H5-A: 基于批注重写草稿
- ✅ H5-C: Ask 联动（ask_first）
- ✅ H5-D: Skill 模板变量 `{{topic}}` `{{memories}}`
- ✅ H5 Windows 全绿验证（cargo test + typecheck）

---

## H6 计划总览（详见 dev-status.md §H6详细计划）

### H6-S1：Shell Tool MVP（收口）

**新增文件/改动**：

| 文件 | 改动类型 | 核心内容 |
|------|----------|----------|
| `src-tauri/src/models.rs` | 已扩展 | `ShellResult` 增加策略元信息 |
| `src-tauri/src/state.rs` | 已扩展 | `run_shell_impl(command, timeout_ms, source)` + 策略分类 |
| `src-tauri/src/commands.rs` | 已扩展 | `run_shell(command, timeout_ms?, source?, state)` |
| `src-tauri/src/main.rs` | 注册命令 | invoke_handler 加 `run_shell` |
| `web/src/types.ts` | 新增接口 | `ShellResult`, `ShellHistoryEntry` |
| `web/src/tauri-client.ts` | 已扩展 | `runShell(command, timeoutMs?, source?)` |
| `web/src/App.tsx` | 已收口 | shell 面板 + 策略元信息展示 |
| `web/src/styles.css` | 已收口 | `.agent-studio__shell*` 样式补齐 |

**验收**：
1. `cargo test` 全绿
2. `npm run typecheck` 0 errors
3. `Get-Date` → 返回当前时间
4. `rm -rf /` → ⛔ 黑名单拦截
5. 不存在命令 → 红色 stderr + 非零 exit code

### H6-S1.5：安全前置（S2 前必做）

- `destructive -> deny`
- `source=agent 且 write/unknown -> require_approval`
- `manual 非 destructive -> auto_allow`
- 前端显示 `executor/action/decision`，便于验收与调试

### H6-S2：Agentic Loop（当前进行中）

从 `refer-rust-daerwen-agent/` 移植：
- `agent_tools.rs`（新建）：ReadFile/WriteFile/EditFile/ShellTool + ToolHandler trait
- `agent_policy.rs`（新建）：PathGuard 5区分级
- `agent_loop.rs`（新建）：多迭代 tool-call loop
- 新增 Tauri 命令 `run_agent_task`
- 前端：任务模式标签 + 工具调用可视化

当前已完成：
- 后端 `run_agent_task` 命令与 `run_agent_task_impl`（LLM 规划 JSON -> 受控 run_shell -> 总结）
- run 状态机修复：`reviewing` 合法，且不写 `completed_at`
- 前端任务模式 Beta 面板（指令、预算轮次、结果区）
- 后端升级为多轮决策：支持 `run_shell/search_wiki/read_wiki`
- 策略函数统一到 `agent_policy.rs`，删除 `state.rs` 重复实现
- Windows 图标修复：替换 1x1 占位 `icon.ico`，并在 `tauri.conf` 显式设置窗口 icon
- S2 模块化第一步：新增 `agent_tools.rs` 并迁移任务决策解析逻辑
- S2 模块化推进：新增 `agent_loop.rs` 并迁移任务 prompt 组装逻辑
- S2 主循环收敛：`run_agent_task_loop` + `AgentLoopRuntime` 已落地，state 入口显著瘦身
- S2 总结收敛：`summarize_agent_task` 已落地，state 不再直连总结 prompt
- S2 runtime 收敛：`AgentLoopRuntime` 实现迁移到 `agent_runtime.rs`
- S2 runtime 解耦：工具执行分支拆成独立 helper（shell/search/read）
- S2 PathGuard(read)：`read_wiki` 仅允许读取 `vault/wiki/*.md`
- S2 state 瘦身：`read_wiki_page_for_agent` 已迁到 `agent_runtime.rs`
- S2 runtime 统一：`ToolExecOutcome` + `read_wiki` 集成测试已补齐
- S2 主服务下沉：新增 `agent_service.rs`，`run_agent_task_impl` 已薄化
- S2 PathGuard(write) 前置：新增 `validate_agent_write_path`
- S2 write_wiki 工具接入：已支持 action 解析 + tool_start/tool_end 展示 + PathGuard(write) 校验
- S2 write_wiki 当前策略：统一 `require_approval`（不落盘），先保证安全与可观测性
- S2 审批暂停语义：当工具返回 `decision=require_approval` 时，循环写入 `awaiting_approval` 并暂停后续迭代
- Agent Studio 布局收口：左侧“记忆/技能”已改为可折叠上下文区，右侧 Shell 改为“调试 Shell（可选）”并默认收起
- 任务模式上下文注入已打通：前端 `memory_context` -> Tauri 命令 -> `build_loop_prompt`，技能模板变量 `{{topic}}/{{memories}}` 已在任务模式生效
- 任务执行日志已结构化：工具时间线支持 `tool_start/tool_end` 配对、耗时计算、详情折叠展开
- 任务续跑入口已上线：任务模式新增“继续任务”按钮，基于最近轨迹快速续跑
- 任务模式新增 E2E 验证指令快捷填充：写入审批验证 / 编辑审批验证
- Agent Studio 右侧布局已按“主内容 + 底部 Shell 抽屉”重排，Shell 输入区在常规窗口高度下可直接使用

---

## 接手指引

任何 Agent 接手当前轮只需：
1. 先跑 `npm run typecheck`，确认前端基线；
2. Windows 端跑 `cargo test`，确认后端基线；
3. 在 Agent Studio 完成 H6-S3 手测（见下方 Handoff Packet）；
4. 更新 `docs/dev-status.md` + `docs/实施过程记录.md` + 本文件后再提交。

## 自动化验证（本轮）

- `web`: `npm run typecheck` ✅
- `src-tauri`: `cargo fmt` ✅
- `web`: `npm test -- --run` ❌（缺少 `@rollup/rollup-linux-x64-gnu`）
- `src-tauri`: `cargo test` ❌（WSL 缺 `pkg-config/gtk/glib`，非业务逻辑错误）
- 用户 Windows：`cargo test` 报 `tokio::process` feature 缺失，已热修复 `src-tauri/Cargo.toml`

---

## Handoff Packet

- round_id: R20260428-H6-S2-MultiTool
- from_agent: Codex
- to_agent: Claude Code / Codex / Gemini（任意）
- scope_done: H1~H5 全部
- open_todos:
  - H6-S3: Windows 手测 run 归档/恢复（含 running/reviewing 禁归档约束）
  - H6-S3: Windows 手测 5 维 shell 策略（manual_write/agent_read 新增维度）
  - H6-S2: write_wiki / edit_wiki 审批端到端（批准写盘、拒绝不改）
  - H6-S2: 审批后事件可见性与状态同步（任务区+历史 run）回归
- next_first_step: 先在 Windows 跑“写入审批验证/编辑审批验证”快捷指令，补齐通过/拒绝两条证据链
