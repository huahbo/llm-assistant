# 交接状态卡

> 最后更新：2026-04-20 HH:mm CST (Gemini)
> 说明：本文件用于三方 Agent 快速接手；详细过程以 `docs/实施过程记录.md` 为准。

## 当前接力状态

- round_id: R20260420-16 (Gemini)
- current_agent: Gemini
- status: ready_for_test（代码已就位，等待用户本地验证）
- objective: 已完成 Deep Research 模块全面对标 `dzhng/deep-research` (迭代式搜索、强化 UI) & 移植包 D (项目模板、网页剪藏、Word 下载) 全部收口。

## 本轮文件所有权 (R20260420-16)

- **后端 (Rust)**：
  - `src-tauri/Cargo.toml` (新增 `tiny_http`, `chrono`)
  - `src-tauri/src/models.rs` (ResearchTaskItem 新增 `depth`, `breadth`)
  - `src-tauri/src/db.rs` (research_tasks 表新增 `depth`, `breadth` 字段，更新 CRUD 函数)
  - `src-tauri/src/state.rs` (重构 `start_research_task` 迭代逻辑，更新 `search_tavily` 开启 `include_raw_content`)
  - `src-tauri/src/commands.rs` (新增 `init_vault_with_template`, `get_clip_server_status`)
  - `src-tauri/src/main.rs` (注册新命令，启动 `clip_server`)
  - `src-tauri/src/clip_server.rs` (新增剪藏 HTTP 服务)
- **前端 (TS/React)**：
  - `web/src/types.ts` (ResearchTaskItem 新增 `depth`, `breadth`)
  - `web/src/templates.ts` (新增模板定义)
  - `web/src/tauri-client.ts` (新增 `initVaultWithTemplate` 封装)
  - `web/src/App.tsx` (重构 `ResearchPanel` UI、模板选择器、Word 下载按钮、`useCallback` 导入补全)
  - `extension/` (完整移植 `refer_project/extension` 内容)
- **文档**:
  - `docs/实施过程记录.md` (已更新)
  - `docs/交接状态卡.md` (已更新)

## 基线与环境摘要 (本轮)

- **Windows 环境**:
  - `cd src-tauri; cargo check`: 通过 ✅ (2 warnings about unused DB functions)
  - `cd web; npm run typecheck`: 通过 ✅ (0 errors)

## 未完成项 (交接后继续)

- TODO-1 (高优): 验证 Deep Research 模块 (迭代搜索、Log 终端、Word 下载) 端到端功能。
- TODO-2 (高优): 验证项目模板功能 (初始化新 Vault 并选择模板)。
- TODO-3 (高优): 验证网页剪藏功能 (启动 Clipper 浏览器扩展，剪藏页面)。
- TODO-4: 移植包 A (持久化 ingest 队列) 的重启恢复逻辑。
- TODO-5: Deep Research 的 LLM 综合改为流式输出，提升响应速度。

## 风险提示

- Clipper 浏览器扩展需用户手动安装。

---
