# v1 接口契约草案（第 1 轮）

本文用于对齐 `web` 与 `src-tauri` 的命令交互结构，当前为骨架阶段契约。

## 1. 命令列表

1. `get_app_overview() -> AppOverview`
2. `set_mode(mode: AppMode) -> ModeChangeResult`
3. `get_recent_logs() -> LogEntry[]`
4. `run_lint() -> LintReport`
5. `init_vault(vault_path: string) -> VaultInitResult`
6. `ingest_markdown(source_path: string) -> IngestResult`
7. `get_default_paths() -> DefaultPaths`
8. `query_ask(question: string) -> QueryAnswerResult`
9. `query_ask_with_options(question: string, options?: QueryAskOptions) -> QueryAnswerResult`

## 2. 类型约定

### `AppMode`
- `Hybrid`
- `StrictLocal`

### `AppOverview`
- `app_name: string`
- `mode: AppMode`
- `vault_path: string`
- `recent_log_count: number`
- `pending_tasks: number`
- `supported_modes: AppMode[]`

### `ModeChangeResult`
- `previous_mode: AppMode`
- `current_mode: AppMode`
- `strict_local_enabled: boolean`

说明：
- `set_mode` 调用成功后，运行模式必须持久化到本地配置文件。

### `LogEntry`
- `id: number`
- `level: "Info" | "Warn" | "Error"`
- `message: string`
- `created_at: string` (`ISO-8601`)

说明：
- 第 2 轮仅要求可读最近日志与模式切换日志，不要求完整任务审计字段。

### `LintIssue`
- `code: string`
- `severity: string`
- `message: string`
- `path?: string`
- `suggestion: string`

约定：
- `severity` 使用 `info | warning | error`。

### `LintReport`
- `mode: AppMode`
- `checked_at: string` (`ISO-8601`)
- `summary: string`
- `issues: LintIssue[]`

### `VaultInitResult`
- `vault_path: string`
- `created_paths: string[]`
- `message: string`

### `IngestResult`
- `source_path: string`
- `raw_path: string`
- `wiki_path: string`
- `message: string`

### `DefaultPaths`
- `vault_path: string`
- `ingest_source_path: string`

### `QueryCitation`
- `page_path: string`
- `score: number`
- `excerpt: string`

### `QueryAnswerResult`
- `question: string`
- `answer: string`
- `citations: QueryCitation[]`
- `matched_pages: string[]`
- `mode: AppMode`
- `checked_at: string`（Unix ms 字符串）

### `QueryAskOptions`
- `top_k?: number`（范围 1..=8，默认 3）

说明：
- `query_ask` 在 v1.1 起优先使用 SQLite `fts_pages` 召回候选页，再生成结果中的 `citations` 与 `matched_pages`。
- 当 FTS 不可用或候选为空时，后端可降级为文件扫描，字段结构保持不变。
- v1.2 起检索 token 支持中英混合：英文 token + 连续中文片段与 2-gram；排序分数包含正文命中、标题命中加权、短语命中加权。
- v1.3 起支持 `query_ask_with_options`，可配置 `top_k`，并保持对旧命令 `query_ask` 的兼容。

## 2.1 本轮 PowerShell 验证命令（FTS5）

```powershell
Set-Location E:\llm-wiki\src-tauri
cargo test
cargo tauri dev --config .\tauri.conf.json
```

应用内手动验证：
1. 初始化 Vault。
2. 执行至少一次“示例摄入”。
3. 在 Ask 面板输入问题并执行 Query，确认返回命中页与引用。
4. 重新执行 Query，确认结果稳定返回（验证索引路径可复用）。

## 3. 演进规则

- 第 2 轮开始，前端类型定义与 Rust `serde` 模型必须保持字段同名。
- 新增字段采用向后兼容方式，不直接删除旧字段。
- 结构变更必须同步更新：
  - `src-tauri/src/models.rs`
  - `web/src/types.ts`
  - 本文档
